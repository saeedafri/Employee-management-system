# Frontend Follow-ups (file back to the ems-frontend team)

> Issues discovered during backend end-to-end (MSW-off) verification that are **frontend-side**.
> The backend is correct and live-verified for each item below. These require changes in
> `/Users/mohdsaeedafri/All-Code-Base/ems-frontend`, not this repo. Backend work is unblocked.

| # | Date | Area | Issue | Backend status | FE action required |
|---|------|------|-------|----------------|--------------------|
| FE-1 | 2026-06-22 | Holidays (Phase 7.3) | FE Holidays screen calls `GET /api/holidays?year=2026` **without `countryCode`**. It still runs the client-side `resolveApplicableHolidays` workaround that 7.3 was meant to retire. | ✅ Done & live-verified — `?countryCode=` server-side scoping works (commit `fdce518`): `IN`→17, `US`/`JP`/`ID`→12, no substring false-match, AND-composes with legacy `country`. | Pass the employee's legal-entity country as `countryCode` to `GET /holidays`; remove/retire the client-side filter once swapped. |
| FE-2 | 2026-06-22 | Payroll run detail (`/payroll/[runId]`) | React console error "Encountered two children with the same key" on the run-detail screen (ids `cmqjm5db1…`, `cmqjm5c6u…`, `cmqjm5b05…`). | ✅ Backend clean — `GET /payroll/runs/:id/payslips` returns **3 unique payslips** (unique `id` + `employeeId`, nets ₹79k/₹88k/₹66k). The warning ids differ from the payslip ids, so it's a FE list (component/earnings breakdown) using a non-unique `key`. | Use a stable unique `key` (e.g. `component.id`+index) in the offending list on the run-detail page. Non-blocking (warning, not a crash). |
| FE-3 | 2026-06-22 | Payroll dashboard (`/payroll`) | Summary cards render **hardcoded ₹** — a KWD tenant (country KW, `defaultCurrency: KWD`) shows "Total Paid (This Year) ₹0" instead of "KD 0.000". Truly-global litmus violation (assumes rupee/2dp). | ✅ Backend correct — `/settings/tenant` returns `currency: KWD`; `/payroll/runs` returns no currency for the empty aggregate (FE computes the card). Backend run-currency hardcode was a separate bug, now fixed (commit below). | Format the payroll summary cards (Total Paid / Last Run Net) with the **tenant currency** from `/settings/tenant` via `Intl.NumberFormat(locale, {style:'currency', currency})` — never a hardcoded `₹`/2dp. Verified live with a KWD tenant (`admin@kwd.test`). |

| FE-4 | 2026-06-23 | Settings / work-week (attendance grid, all date pickers) | The settings registry descriptor `weekStartsOn` is hardcoded `default: 1` (Monday) with **no `fromTenant`/`fromEntity`** (`src/modules/settings/engine/registry.ts`). So `useFormat().weekStartsOn` is always Monday — even for a `SUN-THU` tenant — and `TeamWeeklyAttendanceGrid.tsx` sends a Monday `weekStart` + caps columns at `.slice(0, 5)` with a Mon–Fri `DAY_LABELS` fallback. | ✅ Backend correct & live-verified — `/settings/tenant` now returns `work_week_days`/`work_week_pattern`; `/attendance/team/weekly` builds the real working-day window (snaps a Monday weekStart back to the tenant's first working day, KWD `SUN-THU` → Sunday start, no Sunday-as-`O`); `/timesheets/week-config` returns `weekStartDay` 0 for Sun-start. Verified live on `admin@kwd.test`. | Derive `weekStartsOn` from `workWeekDays[0]` (e.g. add `fromTenant`/`fromEntity` to the registry descriptor, or compute from the resolved `workWeekDays`), and render `member.days.length` columns (not a fixed 5) with date-derived labels so MON-SAT (6 days) isn't truncated. The grid already labels from dates, so this is mostly the `weekStartsOn` derivation + dropping `.slice(0,5)`. |

| FE-5 | 2026-06-23 | Performance (all screens) | App feels slow ("seconds") — e.g. `GET /api/auth/me`. The backend processes `/auth/me` in **~9 ms** (full per-API timing in `BACKEND_PERFORMANCE_REPORT.md`); the latency is the **Vercel BFF function running in US-East (`iad1`) calling the Mumbai backend** — every call crosses US↔India (~250 ms RTT) + cold start + a fresh TLS handshake (`cache:'no-store'`), compounded across a page's many calls. | ✅ Backend fast & live-measured — every endpoint <250 ms server-side, most <30 ms; nothing is "seconds." | **Pin Vercel functions to Mumbai:** add `vercel.json` `{ "regions": ["bom1"] }` (or set Vercel → Settings → Functions → Region = Mumbai). This is the primary fix — co-locates the BFF with the backend, RTT ~250 ms → ~5–20 ms. See `docs/BACKEND_PERFORMANCE_REPORT.md`. |
| FE-6 | 2026-06-23 | RBAC-gated settings screens | HR_ADMIN visiting **SUPER_ADMIN-only** screens (`/permissions`, `/settings/authentication`, `/settings/integration-*`, `/settings/billing-*`, `/settings/branding`) renders a generic **"something went wrong" error boundary** instead of a clean "no access" gate. | ✅ Backend correct — these screens work for SUPER_ADMIN (live-verified 7/7); the backend correctly restricts them. The FE gates client-side and throws to the error boundary rather than showing an access message. | Render a friendly "you don't have access" state for role-gated screens instead of throwing to the error boundary (the `PermissionWrapper` should degrade gracefully, not crash). Non-blocking. |

## Notes
- Add new rows as backend phases are verified end-to-end with MSW off.
- "Backend status ✅" means the endpoint matches the contract wire shape and was verified against the live stack — the remaining work is purely in the frontend.
