Now Can we proceed to next Pages Please , With properly, can You Please review the wireframes and Then You need to work on the Creation of the APIs , proeprlt testing cass , End to end cases and everything please

Now You have very very Important Task to built , You need to build a Employe management system , and You just need to work on the back end part , front end will be hanlded by My Other team , so we need to creat the APIs and They will just fire those API and get the details That need to shown in the UI , So first I need you to Analyze the Wireframes , What need to shwon in the UI and Same way We need to buold the API , and Please it should RObot and Production grade always , and It should be always scalable . Please we want fast and best platforms ,

So keep in mind few things

- Employee should be able to view Details And a[ply for Leave and Those thing and Can widraw as well , risigh as ell , But shoukd not be able to edit the company detailes Which is filled by the HR < and
- we should be having two Login Admin Login and Employee login , In Employee login We will be shpwing Wha the wireframe is shwoing and In the Admin login we will only enable for admin and That will be used to onboard a New Employe and Those detailes will be pushed to the Mysql db and Same will be shown , So it will be a Simple process ,

So we will start Step by step in This PLease Enable GSD for this , Plan everything proeprly , Use Node js , Javascript and Mysql DB , And best ORM . Swagger , etc etc

- Later on we will add the Payroll management also so everything should be there ,

- Please We will have Session management , Cookies managerment , login management , E,mployee login management to check what is going on who has loggedn in and what he has done everything Please , and MemberType as well like EMployee or Admin becasue based on That only we can Allow him the Admin access if he is the HR and A new Employee is onboarding ,

now Now My Agent will work on This , But I want You to first gor thorugh every details And Give me Best prompt for the AI ciding evenrt to work on this propelry and Best way , Start with One page a a Time , Create APIs for that so that My Frontend team can use it proeprly , I can test it using postman , proeprly , , It shoukd be really secure and Fast and robost

and Please I am Giving tne github link , please create a New Repositoy and Push and Do these things there

and Please Ask him To name the function name Human like readable and Understandable , Not Like Underscorea d Then name Like ( \_function1 , \_functikn_3 ) , Please avoid These stricty

please use Human readble FUnction name which lokks like it is developed by a Himan code not a AI Coding agents

So first go throught the Wireframe Understand what we want to do and buidl And then Give me the AI coding agent promt Please

it should be realllY good prompt , Accurate Pricise towhat we want to develop and highly scable product , and fully customization Please

my github - https://github.com/saeedafri?tab=repositories

Plese add new repo and start

and Folder please use this - /Users/mohdsaeedafri/All-Code-Base/EMS

You are a senior backend engineer. Build a production-grade backend for an Employee Management System called EMS.

Important:

- Work backend only. Do not create frontend screens.
- Frontend team will consume REST APIs.
- Build one wireframe page/module at a time.
- Every API must be testable from Postman.
- Code must be secure, scalable, clean, production-grade, and easy for humans to maintain.
- Use meaningful human-readable function names in camelCase.
- Strictly avoid AI-looking names like \_function1, \_function_3, tempFunc, dataHandler1, etc.
- Do not hardcode business logic in controllers.
- Use services, repositories, validators, middleware, policies, and clean module boundaries.

Repository setup:

- Local folder: /Users/mohdsaeedafri/All-Code-Base/EMS
- Create a new GitHub repo under: https://github.com/saeedafri
- Repo name: employee-management-system-backend
- If GitHub CLI is available, use:
  gh auth status
  gh repo create saeedafri/employee-management-system-backend --private --source=. --remote=origin --push
- If repo already exists, do not overwrite. Pull latest and continue safely.
- Commit after each completed module with clear commit messages.

Tech stack:

- Node.js 20+
- JavaScript with ES Modules
- Fastify as the API framework
- MySQL 8
- Prisma ORM
- Zod for validation
- Swagger/OpenAPI for API docs
- Redis for caching, session support, rate limiting, and queues
- BullMQ for background jobs
- Argon2id for password hashing
- JWT access tokens
- HttpOnly secure refresh-token cookies
- Pino logger
- Jest or Vitest for tests
- Docker Compose for local MySQL + Redis
- ESLint + Prettier
- Helmet, CORS, rate limiting, request IDs, and centralized error handling

Architecture requirements:

- Use `/api/v1` as API prefix.
- Use layered architecture:
  src/
  app.js
  server.js
  config/
  modules/
  auth/
  users/
  employees/
  departments/
  attendance/
  leave/
  holidays/
  permissions/
  settings/
  analytics/
  auditLogs/
  notifications/
  files/
  resignations/
  plugins/
  middleware/
  policies/
  utils/
  jobs/
  prisma/
  docs/
  tests/

Response format:

- Success:
  {
  "success": true,
  "data": {},
  "meta": {}
  }

- Error:
  {
  "success": false,
  "error": {
  "code": "VALIDATION_ERROR",
  "message": "Human readable message",
  "details": {},
  "requestId": "..."
  }
  }

Core security rules:

- Two login modes are required:
  1. Employee login
  2. Admin login
- Admin login must only allow SUPER_ADMIN and HR_ADMIN users.
- Employee login may allow EMPLOYEE, MANAGER, HR_ADMIN, and SUPER_ADMIN, but dashboard/data access must be role-based.
- Store `memberType` on user/profile records:
  SUPER_ADMIN, HR_ADMIN, MANAGER, EMPLOYEE, AUDITOR
- Also implement proper RBAC with roles and permissions.
- Never rely only on frontend hiding buttons.
- Every protected route must check authorization on the backend.
- Employee must be able to:
  - View own profile/details.
  - View own attendance.
  - Apply for leave.
  - Withdraw leave if still pending.
  - Submit resignation request.
  - Withdraw resignation request if still pending.
  - View own documents.
  - View company/holiday info read-only.
- Employee must not be able to edit company details, HR-filled job details, roles, permissions, payroll/company settings, department structure, or other employee records.
- HR Admin must be able to onboard employees and manage company/HR data.
- Manager must be able to see team data and approve/deny team leave/regularization only.
- Super Admin can manage roles and permissions.
- All mutations must write audit logs.

Session and login management:

- Create `sessions` table.
- Track:
  userId, refreshTokenHash, ipAddress, userAgent, deviceName, loginAt, lastSeenAt, expiresAt, revokedAt, revokeReason.
- Refresh tokens must rotate.
- Detect refresh token reuse and revoke the whole session family.
- Add APIs:
  GET /api/v1/auth/me
  GET /api/v1/auth/sessions
  DELETE /api/v1/auth/sessions/:sessionId
  POST /api/v1/auth/logout
  POST /api/v1/auth/logout-all
- Admin/HR session visibility:
  GET /api/v1/admin/sessions?userId=&status=
- Log login, logout, failed login, token refresh, session revoke in audit_logs.

Database design:
Use Prisma migrations. Create these core models:

1. tenants

- id
- name
- legalName
- displayName
- country
- defaultCurrency
- timezone
- fiscalYearStart
- primaryContactEmail
- supportPhone
- logoUrl
- createdAt
- updatedAt
- deletedAt

2. users

- id
- tenantId
- employeeId nullable
- email unique per tenant
- passwordHash
- memberType enum
- status enum: ACTIVE, DISABLED, INVITED, LOCKED
- mfaEnabled
- lastLoginAt
- createdAt
- updatedAt
- deletedAt

3. employees

- id
- tenantId
- userId nullable
- employeeCode unique per tenant
- firstName
- lastName
- workEmail
- personalEmail
- phone
- dateOfBirth
- gender
- address
- emergencyContactName
- emergencyContactPhone
- designation
- departmentId
- managerId
- joinedOn
- employmentType enum
- employmentStatus enum: ACTIVE, INACTIVE, ON_LEAVE, RESIGNED, TERMINATED
- location
- payCurrency
- createdBy
- updatedBy
- createdAt
- updatedAt
- deletedAt

4. departments

- id
- tenantId
- parentId nullable
- name
- departmentCode
- headEmployeeId nullable
- depth
- createdAt
- updatedAt
- deletedAt

5. roles

- id
- tenantId nullable for global/system roles
- name
- key
- isSystem
- createdAt
- updatedAt

6. permissions

- id
- key
- module
- description

7. role_permissions

- roleId
- permissionId

8. user_roles

- userId
- roleId

9. sessions

- id
- userId
- tenantId
- refreshTokenHash
- ipAddress
- userAgent
- deviceName
- loginAt
- lastSeenAt
- expiresAt
- revokedAt
- revokeReason

10. password_reset_tokens

- id
- userId
- tokenHash
- expiresAt
- usedAt
- createdAt

11. otp_challenges

- id
- userId
- challengeId
- codeHash
- attempts
- expiresAt
- consumedAt
- createdAt

12. employee_documents

- id
- tenantId
- employeeId
- documentType
- fileName
- fileUrl
- storageKey
- mimeType
- sizeBytes
- verificationStatus enum: PENDING, VERIFIED, REJECTED
- uploadedBy
- verifiedBy nullable
- createdAt
- updatedAt

13. attendance_records

- id
- tenantId
- employeeId
- attendanceDate
- checkInAt
- checkOutAt
- workMode enum: OFFICE, WFH, HYBRID
- status enum: PRESENT, ABSENT, LEAVE, WFH, HALF_DAY, HOLIDAY
- totalMinutes
- locationJson nullable
- notes
- createdAt
- updatedAt

14. attendance_regularization_requests

- id
- tenantId
- employeeId
- attendanceDate
- reason
- status enum: PENDING, APPROVED, DENIED, WITHDRAWN
- reviewerId nullable
- reviewerComment nullable
- createdAt
- updatedAt

15. leave_types

- id
- tenantId
- name
- code
- annualAllowance
- carryForwardAllowed
- isPaid
- isActive

16. leave_balances

- id
- tenantId
- employeeId
- leaveTypeId
- balance
- used
- pending

17. leave_requests

- id
- tenantId
- employeeId
- leaveTypeId
- startDate
- endDate
- totalDays
- reason
- status enum: PENDING, APPROVED, DENIED, WITHDRAWN, CANCELLED
- approverId nullable
- approverComment nullable
- submittedAt
- decidedAt
- createdAt
- updatedAt

18. holidays

- id
- tenantId
- name
- holidayDate
- location
- isOptional
- createdAt
- updatedAt

19. resignations

- id
- tenantId
- employeeId
- reason
- preferredLastWorkingDate
- status enum: PENDING, APPROVED, REJECTED, WITHDRAWN
- reviewerId nullable
- reviewerComment nullable
- submittedAt
- decidedAt
- createdAt
- updatedAt

20. audit_logs

- id
- tenantId
- actorUserId
- action
- entityType
- entityId
- oldValuesJson
- newValuesJson
- ipAddress
- userAgent
- createdAt

21. notifications

- id
- tenantId
- userId
- type
- title
- message
- metadataJson
- readAt
- createdAt

22. saved_views

- id
- tenantId
- userId
- module
- name
- filtersJson
- columnsJson
- createdAt
- updatedAt

23. settings

- id
- tenantId
- groupKey
- settingKey
- valueJson
- updatedBy
- updatedAt

Indexes:

- tenantId on every tenant-scoped table.
- users: tenantId + email.
- employees: tenantId + employeeCode, tenantId + departmentId, tenantId + managerId.
- attendance_records: tenantId + employeeId + attendanceDate.
- leave_requests: tenantId + employeeId + status.
- audit_logs: tenantId + entityType + entityId + createdAt.
- sessions: userId + revokedAt + expiresAt.

Swagger:

- Swagger UI at /docs.
- OpenAPI JSON at /docs/json.
- Every endpoint must include:
  request body schema
  response schema
  auth requirements
  error examples
  permission requirements

Postman:

- Generate a Postman collection in docs/postman/EMS.postman_collection.json.
- Include environment variables:
  baseUrl
  accessToken
  employeeEmail
  adminEmail

Local setup:

- Add docker-compose.yml for MySQL and Redis.
- Add .env.example.
- Add seed script.
- Seed users:
  Super Admin: superadmin@acme.test / ChangeMe123!
  HR Admin: hr@acme.test / ChangeMe123!
  Manager: aman@acme.test / ChangeMe123!
  Employee: priya@acme.test / ChangeMe123!
- Seed departments, employees, roles, permissions, attendance, leave balances, holidays.

Wireframe-driven API implementation plan:

PAGE 01 - Login
Implement:
POST /api/v1/auth/login
POST /api/v1/auth/admin/login
POST /api/v1/auth/employee/login
POST /api/v1/auth/refresh
POST /api/v1/auth/logout
GET /api/v1/auth/me

Rules:

- Validate email and password.
- Rate limit login.
- Use Argon2id password verification.
- Admin login rejects non-admin users.
- On success, return accessToken and user profile.
- Set refresh token in HttpOnly, Secure, SameSite cookie.
- If MFA required, return mfaRequired with challengeId instead of full login.
- Write audit logs.

PAGE 02 - Forgot Password
Implement:
POST /api/v1/auth/forgot-password
POST /api/v1/auth/reset-password
GET /api/v1/auth/reset-password/validate?token=

Rules:

- Forgot password always returns 202.
- Token stored hashed.
- Token valid 30 minutes.
- Reset password revokes all sessions.
- Queue email job with BullMQ.

PAGE 03 - OTP Verification
Implement:
POST /api/v1/auth/verify-otp
POST /api/v1/auth/resend-otp

Rules:

- 6-digit OTP.
- 10 minute TTL.
- 5 failed attempts locks challenge.
- 60 second resend cooldown.
- Max 3 resends.

PAGE 04 - HR Admin Dashboard
Implement:
GET /api/v1/analytics/summary
GET /api/v1/analytics/attendance?range=30d
GET /api/v1/analytics/headcount-by-department
GET /api/v1/audit-logs?limit=10

Rules:

- Require analytics:read.
- Cache summary in Redis for 60 seconds.
- Data scoped by tenant.
- Super Admin can switch tenant later; prepare structure.

PAGE 05 - Manager Dashboard
Implement:
GET /api/v1/manager/dashboard
GET /api/v1/manager/team
GET /api/v1/manager/team-attendance?week=
GET /api/v1/manager/pending-approvals
PATCH /api/v1/leave-requests/:id
PATCH /api/v1/leave-requests/bulk

Rules:

- Manager only sees direct reports.
- Approve/deny only team requests.
- Every state transition writes audit log.
- Add SSE endpoint:
  GET /api/v1/events

PAGE 06 - Employee Dashboard
Implement:
GET /api/v1/employee/dashboard
GET /api/v1/attendance/today
POST /api/v1/attendance/check-in
POST /api/v1/attendance/check-out
GET /api/v1/leave/balance/me
GET /api/v1/holidays/upcoming?limit=3
GET /api/v1/employees/me/documents
GET /api/v1/employees/me/team

Rules:

- Employee sees own data only.
- Check-in/out must prevent duplicate active check-in.
- Geo metadata optional and configurable.
- Employee documents are read-only unless upload permission is explicitly granted.

PAGE 07 - Employees List
Implement:
GET /api/v1/employees
POST /api/v1/employees/export
GET /api/v1/employees/next-code
POST /api/v1/saved-views
GET /api/v1/saved-views?module=employees

Filters:

- search
- departmentId
- status
- employmentType
- joinedFrom
- joinedTo
- page
- limit
- sortBy
- sortOrder

Rules:

- HR Admin sees all tenant employees.
- Manager sees team employees only.
- Employee cannot list all employees.
- Server-side pagination.
- Export requires employees:export and runs as BullMQ job.

PAGE 08 - Employee Profile
Implement:
GET /api/v1/employees/me
GET /api/v1/employees/:id
GET /api/v1/employees/:id/documents
GET /api/v1/audit-logs?entity=employee&id=:id&limit=4
PATCH /api/v1/employees/:id/status
DELETE /api/v1/employees/:id

Rules:

- Self-view always allowed.
- Manager can read team profiles.
- HR Admin can read/write HR fields.
- Employee cannot edit company-filled fields.
- Deactivate is soft delete and revokes sessions.
- Require typing employee code for deactivation in request body.

PAGE 09 - Employee Create/Edit
Implement:
POST /api/v1/employees
PATCH /api/v1/employees/:id
GET /api/v1/departments?fields=id,name
GET /api/v1/employees/search-managers?q=
POST /api/v1/files/presign-upload
POST /api/v1/employees/:id/documents
PATCH /api/v1/employees/:id/access

Rules:

- HR Admin and Super Admin only.
- Validate with Zod.
- Work email unique per tenant.
- Employee code auto-generation must be safe under concurrency.
- Create user account if access step enabled.
- Optional invite email through BullMQ.
- File upload should use presigned URL abstraction. For local dev, mock storage provider.
- Keep payroll-related fields out of Phase 1, but make Job section extensible for Phase 2 payroll.

PAGE 10 - Departments
Implement:
GET /api/v1/departments
GET /api/v1/departments/tree
GET /api/v1/departments/:id
POST /api/v1/departments
PATCH /api/v1/departments/:id
DELETE /api/v1/departments/:id

Rules:

- parentId self-reference.
- Prevent cycles.
- Enforce depth limit of 5.
- Cannot delete department with active employees unless reassign flow is provided.
- Cache tree for 5 minutes.

PAGE 11 - Attendance Records
Implement:
GET /api/v1/attendance?employeeId=&month=YYYY-MM&view=
GET /api/v1/attendance/:id
POST /api/v1/attendance/regularization-requests
GET /api/v1/attendance/regularization-requests
PATCH /api/v1/attendance/regularization-requests/:id
POST /api/v1/attendance/export

Rules:

- Employee sees own records.
- Manager sees team records.
- HR Admin sees tenant records.
- Future dates are read-only.
- Export runs through BullMQ.
- Regularization approvals go to manager/HR based on policy.

PAGE 12 - Leave Requests & Approvals
Implement:
POST /api/v1/leave-requests
GET /api/v1/leave-requests/me
GET /api/v1/leave-requests/approvals
GET /api/v1/leave-requests/team-calendar
GET /api/v1/leave/balances
PATCH /api/v1/leave-requests/:id
PATCH /api/v1/leave-requests/:id/withdraw
PATCH /api/v1/leave-requests/bulk
GET /api/v1/leave-types

Rules:

- Employee can request leave.
- Employee can withdraw only own pending leave.
- Manager can approve/deny team requests.
- HR Admin can approve/deny depending on tenant policy.
- Detect overlapping leave.
- Detect low team coverage and return warning.
- Update leave balances transactionally.
- Write audit log for every transition.
- Notify requester through email and in-app notification.

PAGE 13 - Holiday Calendar
Implement:
GET /api/v1/holidays?year=&location=
GET /api/v1/holidays/upcoming?limit=
POST /api/v1/holidays
PATCH /api/v1/holidays/:id
DELETE /api/v1/holidays/:id
POST /api/v1/holidays/import-ics

Rules:

- All authenticated users can read holidays.
- Employee sees holidays for assigned location.
- HR Admin can manage holidays.
- Cache yearly holidays for 1 hour.
- ICS import runs via BullMQ and should support preview before commit.

PAGE 14 - Permissions Matrix
Implement:
GET /api/v1/permissions
GET /api/v1/roles
POST /api/v1/roles
PATCH /api/v1/roles/:id
PATCH /api/v1/roles/:id/permissions
GET /api/v1/users/:id/roles
PATCH /api/v1/users/:id/roles

Rules:

- Super Admin only for permissions matrix.
- Permission catalog is product-owned/read-only.
- Do not allow Super Admin role to lose permissions:manage.
- Do not allow last Super Admin user to be downgraded.
- Every permission change writes audit log with diff.

PAGE 15 - Settings
Implement:
GET /api/v1/settings
GET /api/v1/settings/:groupKey/:settingKey
PATCH /api/v1/settings/:groupKey/:settingKey
PATCH /api/v1/settings/company-profile
GET /api/v1/audit-logs

Settings groups:

- workspace/company
- workspace/branding
- workspace/locale-timezone
- people/leave-types
- people/holiday-calendar
- people/attendance-rules
- security/authentication
- security/sessions-devices
- security/audit-log
- notifications/email-templates
- notifications/in-app-preferences
- integrations/email
- integrations/storage
- integrations/webhooks

Rules:

- HR Admin and Super Admin only for most settings.
- Employees must not edit company details.
- Every save writes audit diff.
- Saving timezone invalidates cached date-related analytics.
- Prepare People group for future payroll settings.

Additional user-mentioned requirement: Resignation
Implement:
POST /api/v1/resignations
GET /api/v1/resignations/me
PATCH /api/v1/resignations/:id/withdraw
GET /api/v1/resignations
PATCH /api/v1/resignations/:id

Rules:

- Employee can submit resignation.
- Employee can withdraw only own pending resignation.
- HR Admin can approve/reject.
- Manager can view team resignation requests if policy allows.
- Write audit logs and notifications.

Payroll future-proofing:

- Do not implement payroll calculations now.
- Design modules so payroll can be added later without rewriting employees.
- Add placeholder module folder:
  src/modules/payroll/
  README.md
- In README, document future entities:
  payrollRuns
  salaryStructures
  payComponents
  statutoryDeductions
  payslips
  taxDeclarations
- Keep salary/payroll permissions separate:
  payroll:read
  payroll:write
  payroll:approve

Coding standards:

- Use camelCase function names:
  createEmployee
  getEmployeeProfile
  approveLeaveRequest
  calculateLeaveBalance
  createAuditLog
  validateDepartmentHierarchy
  revokeUserSessions
- Do not use names like:
  \_function1
  handleData
  processStuff
  doSomething
  temp
  finalData
- Controllers should be thin.
- Services should hold business logic.
- Repositories should hold Prisma queries.
- Policies should hold permission checks.
- Validators should hold Zod schemas.
- Use transactions for multi-step writes.
- Use optimistic-safe server logic, not frontend trust.

Testing requirements:

- Unit tests for services.
- Integration tests for auth, employees, leave, attendance, roles.
- Test RBAC:
  employee cannot access admin data
  manager cannot approve outside team
  HR can onboard employee
  Super Admin can manage permissions
- Test session revocation.
- Test leave withdrawal.
- Test audit log creation.
- Test employee cannot edit HR/company fields.

Performance requirements:

- Use pagination everywhere for list APIs.
- Use Redis caching for analytics, department tree, holiday calendar.
- Add database indexes.
- Avoid N+1 queries.
- Use select/include carefully in Prisma.
- Keep list APIs lightweight.
- Use background jobs for exports, emails, imports, file scanning.

Deliverables:

1. Working backend in /Users/mohdsaeedafri/All-Code-Base/EMS
2. GitHub repo pushed to saeedafri/employee-management-system-backend
3. README.md with setup instructions
4. .env.example
5. docker-compose.yml
6. Prisma schema and migrations
7. Seed script
8. Swagger docs at /docs
9. Postman collection
10. Tests
11. API module documentation in docs/api/
12. Clear commit history, one module/page at a time

Start now with:

1. Initialize project.
2. Add Fastify, Prisma, MySQL, Redis, Swagger, Zod, auth foundation.
3. Implement Page 01 Login APIs completely.
4. Add tests and Postman examples for Page 01.
5. Commit with message: "feat(auth): implement login APIs and session foundation"
6. Then continue to Page 02 only after Page 01 is working.

STRICT REVIEW RESULT: DO NOT PROCEED TO PAGE 02 YET.

The uploaded EMS.zip has been reviewed. The direction is acceptable, but the implementation is not production-grade yet. You must stop building new modules and fix the foundation first.

Current approval status: NOT APPROVED.

You must complete “Foundation Hardening Sprint 0” before implementing any new wireframe page.

Critical rules:

- Do not move to Page 02 until all Sprint 0 requirements below are completed.
- Do not say something is done unless it is implemented, tested, documented, and visible in the repo.
- Share full command outputs and test reports after completion.
- No console.log or console.error in runtime backend code.
- Use human-readable camelCase function names only.
- No AI-looking names like \_function1, tempHandler, processData1, doStuff, finalData, etc.
- Every API must be documented, tested, and Postman-ready.

==================================================
SPRINT 0: FOUNDATION HARDENING
==================================================

1. Clean repository

- Remove \_\_MACOSX folders.
- Remove .DS_Store files.
- Add .gitignore with:
  node_modules
  .env
  .DS_Store
  \_\_MACOSX
  coverage
  logs
  dist
  .nyc_output
- Add .env.example.
- Ensure project works from:
  /Users/mohdsaeedafri/All-Code-Base/EMS
- Ensure GitHub repo is clean and pushed.

2. Fix test stack
   Business team requested Mocha/Chai. Replace Vitest with:

- mocha
- chai
- sinon
- c8
- supertest or Fastify inject-based testing
- testcontainers or dockerized MySQL/Redis test setup

Add scripts:

- npm run test
- npm run test:unit
- npm run test:integration
- npm run test:e2e
- npm run test:coverage
- npm run test:watch

Minimum coverage:

- 90% statements
- 90% branches
- 90% functions
- 90% lines

3. Add mandatory tests for Auth/Page 01
   Create full tests for:

- employee login success
- admin login success
- employee rejected from admin login
- invalid email
- wrong password
- missing password
- disabled user
- locked user
- refresh token success
- refresh token missing
- refresh token rotation
- refresh token reuse detection
- logout current session
- logout all sessions
- list sessions
- revoke session
- get current user
- invalid access token
- expired access token
- RBAC permission included in token
- audit log created for login/logout/refresh/revoke
- no passwordHash returned in any response

4. Fix refresh token architecture
   Current refresh flow is wrong because /auth/refresh requires access token. Refresh must work using only the HttpOnly refresh cookie.

Implement secure opaque refresh token format:

- cookie value should be something like: sessionId.rawRefreshToken
- store only SHA-256 hash of rawRefreshToken in DB
- lookup session by sessionId
- compare hash using timing-safe comparison
- rotate token on each refresh
- revoke old token
- add sessionFamilyId column
- on reuse detection revoke entire session family
- refresh route must not require access token
- update audit logs for TOKEN_REFRESH, TOKEN_REUSE_DETECTED, SESSION_REVOKED

Update Session model:

- id
- tenantId
- userId
- sessionFamilyId
- refreshTokenHash
- ipAddress
- userAgent
- deviceName
- loginAt
- lastSeenAt
- expiresAt
- revokedAt
- revokeReason
- createdAt

Indexes:

- @@index([userId, revokedAt, expiresAt])
- @@index([tenantId, userId, revokedAt])
- @@index([sessionFamilyId, revokedAt])
- @@index([expiresAt])

5. Fix tenant resolution
   Do not use “first tenant from DB”.

Implement a tenant resolver:

- For local/dev, allow X-Tenant-Key header or default tenant key from env.
- For production, prepare subdomain/header-based tenant resolution.
- Add tenantKey/slug to Tenant table.
- All tenant-scoped queries must use tenantId from resolved tenant context.
- Add middleware/hook that attaches request.tenant.

6. Fix Prisma schema quality
   Make these schema corrections:

- Use Prisma Json type instead of String for:
  oldValuesJson
  newValuesJson
  metadataJson
  valueJson
  filtersJson
  columnsJson
  locationJson

- Make AuditLog.actorUserId nullable because failed login and system jobs may not have a user.

- Add unique:
  Employee: @@unique([tenantId, workEmail])

- Fix User/Employee relation:
  Either keep Employee.userId as the only relation, or make User.employeeId a real foreign key.
  Do not keep duplicate/confusing scalar relation fields.

- Add composite indexes for speed:
  User:
  @@index([tenantId, status, deletedAt])
  @@index([tenantId, memberType, status])

  Employee:
  @@index([tenantId, employmentStatus, deletedAt])
  @@index([tenantId, departmentId, employmentStatus])
  @@index([tenantId, managerId, employmentStatus])
  @@index([tenantId, joinedOn])
  @@index([tenantId, firstName, lastName])
  @@unique([tenantId, workEmail])

  Department:
  @@index([tenantId, parentId, deletedAt])
  @@index([tenantId, depth])

  AttendanceRecord:
  @@index([tenantId, attendanceDate])
  @@index([tenantId, employeeId, attendanceDate])
  @@index([tenantId, status, attendanceDate])

  LeaveRequest:
  @@index([tenantId, employeeId, status])
  @@index([tenantId, status, submittedAt])
  @@index([tenantId, employeeId, startDate, endDate])
  @@index([tenantId, approverId, status])

  Holiday:
  @@index([tenantId, holidayDate])
  @@index([tenantId, location, holidayDate])

  AuditLog:
  @@index([tenantId, entityType, entityId, createdAt])
  @@index([tenantId, actorUserId, createdAt])
  @@index([tenantId, action, createdAt])

  Notification:
  @@index([tenantId, userId, readAt, createdAt])

  Setting:
  @@unique([tenantId, groupKey, settingKey])

7. Fix logging properly
   Remove runtime console.log and console.error.

Use Pino structured logging everywhere.

Requirements:

- Add logger module.
- Add requestId to every log.
- Add tenantId, userId, sessionId when available.
- Redact sensitive fields:
  password
  newPassword
  token
  refreshToken
  accessToken
  authorization
  cookie
  otp
  passwordHash

Log levels:

- error
- warn
- info
- debug
- trace

Indian time display:

- Store timestamps in UTC.
- API should also return display timestamp in Asia/Kolkata format:
  DD/MM/YYYY hh:mm:ss A IST
  Example: 17/05/2026 04:50:23 PM IST

Add admin log page APIs:

- GET /api/v1/admin/logs
- GET /api/v1/admin/logs/:id
- GET /api/v1/admin/logs/export?format=csv|json
- GET /api/v1/admin/logs/stream

Filters:

- level
- module
- requestId
- actorUserId
- tenantId
- from
- to
- search
- page
- limit

Response must include:

- id
- level
- levelLabel
- levelColor
- module
- message
- requestId
- actorUserId
- tenantId
- metadata
- timestampUtc
- timestampIstDisplay

UI color mapping:

- error: red
- warn: orange
- info: blue
- debug: purple
- trace: gray

Important:

- Do not store every debug log synchronously in MySQL on hot paths.
- Use async queue/buffer for log persistence.
- Keep production design pluggable for OpenSearch/Loki later.
- Add downloadable log exports.

8. Add documentation
   Create this docs structure:

docs/
api/
ui-team-api-guide.md
auth-api.md
error-codes.md
postman-guide.md
backend/
backend-design-document.md
architecture.md
module-structure.md
auth-session-internals.md
rbac-permissions.md
database-design.md
logging-design.md
testing-strategy.md
deployment-guide.md
diagrams/
erd.md
system-architecture.md
auth-flow.md
refresh-token-flow.md
employee-onboarding-flow.md
leave-flow.md
attendance-flow.md
permission-flow.md
logging-flow.md
audit-log-flow.md
database-index-map.md
decisions/
adr-001-tech-stack.md
adr-002-fastify.md
adr-003-prisma.md
adr-004-auth-session-design.md
adr-005-logging-observability.md

Documentation requirements:

- UI team guide must explain every API:
  method
  URL
  auth required or not
  required headers
  request body
  success response
  error response
  Postman example
  frontend usage notes
  permission required

- Backend design document must explain:
  why Node.js
  why Fastify
  why Prisma
  why MySQL
  why Redis
  why BullMQ
  why Pino
  why Mocha/Chai
  how modules interact
  how auth works
  how sessions work
  how permissions work
  how audit logs work
  how logging works
  how errors work
  how scalability is handled
  how payroll can be added later

- Diagrams must be in Mermaid wherever possible.
- Include ERD.
- Include flowcharts for login, refresh, logout, employee onboarding, leave request, attendance, logging, audit log, RBAC.

9. Fix Swagger/OpenAPI
   Every route must define:

- request schema
- response schema
- error examples
- auth requirements
- permission requirements
- tags
- description
- examples

Swagger UI:

- /docs

OpenAPI JSON:

- /docs/json

Swagger must be useful to frontend team, not just auto-generated route names.

10. Fix Postman collection
    Postman must include:

- employee login
- admin login
- refresh
- logout
- logout all
- me
- sessions
- revoke session
- admin forbidden test
- invalid credentials test
- missing token test

Add Postman tests:

- status code checks
- response shape checks
- save accessToken automatically
- cookie-based refresh instructions
- negative test cases

Also add:
docs/postman/EMS.postman_environment.json

11. Add CI
    Add GitHub Actions:
    .github/workflows/ci.yml

CI must run:

- npm ci
- npm run lint
- npm run test:unit
- npm run test:integration
- npm run test:coverage
- prisma validate
- prisma migrate deploy against test DB
- npm audit or security check

12. Add result report
    After completing Sprint 0, share a full result report.

The report must include:

- Git commit hash
- GitHub repo URL
- Branch name
- List of files changed
- APIs completed
- Database migrations created
- Test command outputs
- Coverage report
- Postman/Newman output
- Swagger URL screenshot or confirmation
- Docs created
- Diagrams created
- Known limitations
- What is safe to build next

Required commands to run and share output:
npm ci
npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npx prisma migrate reset --force
npm run db:seed
npm run dev
newman run docs/postman/EMS.postman_collection.json -e docs/postman/EMS.postman_environment.json

Do not continue to Page 02 until every command passes.

Sprint 0 review result: NOT APPROVED.

You made progress, but the claim “100% complete / production-ready” is not accurate.

Do not start Page 02.

The following blockers must be fixed first.

==================================================
BLOCKER 1: FIX SESSION CREATION
==================================================

Prisma requires:

sessionFamilyId String

But login/adminLogin creates a session without sessionFamilyId and then tries to update it later.

That will fail.

Fix login and adminLogin so the first session is created with both:

id
sessionFamilyId

For first login:

const sessionId = createIdOrUuid();

create session with:

id: sessionId
sessionFamilyId: sessionId

Do not create then update required fields.

Add tests proving login creates a valid session with sessionFamilyId.

==================================================
BLOCKER 2: FIX REFRESH TOKEN REUSE DETECTION
==================================================

Current refresh logic exits early when session.revokedAt is not null.

That breaks token reuse detection.

When an already revoked/rotated session token is presented again, treat it as possible token reuse.

Required behavior:

1. Parse cookie sessionId.rawRefreshToken.
2. Find session by sessionId.
3. If session does not exist, return 401.
4. Hash provided raw token.
5. If session is revoked:
   - compare hash if possible
   - treat reuse of a rotated token as TOKEN_REUSE_DETECTED
   - revoke full sessionFamilyId
   - write audit log TOKEN_REUSE_DETECTED
   - return 401 TOKEN_REUSE
6. If session is active:
   - compare hash with timingSafeEqual
   - if mismatch, revoke full family and write TOKEN_REUSE_DETECTED
   - if valid, rotate refresh token
   - revoke old session as TOKEN_ROTATED
   - create new session in same sessionFamilyId
   - return new access token and set new refresh cookie

Add tests:

- refresh succeeds with cookie only
- old rotated token reuse returns TOKEN_REUSE
- old rotated token reuse revokes entire session family
- audit log TOKEN_REUSE_DETECTED is created
- active session with wrong token hash revokes full family

==================================================
BLOCKER 3: FIX TESTS
==================================================

Tests currently cannot be trusted.

Problems:

- authService.validateLogin is used in tests but not exported.
- login test expects body.data.refreshToken, but controller does not return refreshToken in body.
- e2e tests are empty.
- 90% coverage proof is missing.

Fix tests so they actually pass from a clean checkout.

Required commands must pass:

npm ci
npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage

Share actual terminal output.

Do not report “tests passing” without command output.

==================================================
BLOCKER 4: FIX PRISMA SCHEMA
==================================================

These are still String and must become Json:

SavedView.filtersJson
SavedView.columnsJson
Setting.valueJson

Add missing Employee unique constraint:

@@unique([tenantId, workEmail])

Add missing composite indexes exactly as requested earlier.

After changes run:

npx prisma validate
npx prisma migrate dev
npx prisma migrate reset --force
npm run db:seed

Share output.

==================================================
BLOCKER 5: FIX ADMIN LOGGING PAGE
==================================================

Current /admin/logs reads audit logs only. Business asked for application logs.

Implement a real LogEntry model or clearly separated logging storage.

Required response fields:

id
level
levelLabel
levelColor
module
message
requestId
actorUserId
tenantId
metadata
timestampUtc
timestampIstDisplay

IST display format:

DD/MM/YYYY hh:mm:ss A IST

Example:

17/05/2026 04:50:23 PM IST

Required filters:

level
module
requestId
actorUserId
tenantId
from
to
search
page
limit

Required APIs:

GET /api/v1/admin/logs
GET /api/v1/admin/logs/:id
GET /api/v1/admin/logs/export?format=csv|json
GET /api/v1/admin/logs/stream

Security:

- These APIs must require HR_ADMIN or SUPER_ADMIN.
- Normal EMPLOYEE must get 403.

Do not allow any authenticated employee to read logs.

==================================================
BLOCKER 6: REMOVE RUNTIME CONSOLE LOGGING
==================================================

Remove this from src/server.js:

console.error(...)

Use Fastify/Pino logger instead.

No eslint-disable workaround is allowed for runtime console usage.

==================================================
BLOCKER 7: FIX POSTMAN
==================================================

Current Postman collection has only 4 requests. This is incomplete.

Add:

employee login
admin login
refresh
logout
logout all
me
sessions
revoke session
admin forbidden test
invalid credentials test
missing token test
tenant header test
log APIs

Add Postman tests:

- status code checks
- response shape checks
- token save
- negative assertions

Add:

docs/postman/EMS.postman_environment.json

Run Newman and share output.

==================================================
BLOCKER 8: FIX SWAGGER
==================================================

Every route must have route-level schema:

body
headers
params
querystring
response
error examples
auth requirement
permission requirement
description
tags

Swagger must be useful for frontend developers, not just global definitions.

==================================================
BLOCKER 9: FIX CI
==================================================

CI currently uses Node 18 but package requires Node >=20.

Use Node 20.

Also CI calls missing scripts:

npm run db:generate
npm run build

Either add those scripts properly or remove those steps.

Security checks must not be continue-on-error for production readiness.

CI must fail on:

- lint failure
- test failure
- coverage below 90%
- Prisma validation failure
- migration failure
- security audit failure unless documented exception exists

==================================================
BLOCKER 10: DO NOT INCLUDE .env IN DELIVERY ZIP
==================================================

.env is not tracked in Git, good.

But it is included in the ZIP delivery. Do not include .env in shared ZIPs.

Only include:

.env.example

==================================================
REQUIRED RESULT REPORT
==================================================

After fixes, share:

1. GitHub repo URL
2. Branch name
3. Latest commit hash
4. Files changed
5. Database migration names
6. npm ci output
7. npm run lint output
8. npm run test:unit output
9. npm run test:integration output
10. npm run test:e2e output
11. npm run test:coverage output
12. npx prisma validate output
13. npx prisma migrate reset --force output
14. npm run db:seed output
15. Newman output
16. Swagger URL confirmation
17. Postman collection path
18. Confirmation that EMPLOYEE cannot access /admin/logs
19. Confirmation that refresh token reuse revokes session family
20. Confirmation that Page 02 has not started

Only after this passes should Page 02 begin.

Sprint 0 review result: NOT APPROVED for Page 02 yet.

You made strong progress, but the current delivery is not 100% complete and not production-ready.

Do not start Page 02 until the following blockers are fixed.

==================================================
BLOCKER 1: FIX PRISMA MIGRATIONS
==================================================

The Prisma schema and migrations are not in sync.

Schema has LogEntry model, but there is no migration creating the LogEntry table.

Schema has:

SavedView.filtersJson Json
SavedView.columnsJson Json?
Setting.valueJson Json

But the migration still creates them as VARCHAR.

Schema has:

Employee @@unique([tenantId, workEmail])

But the migration for this unique index is missing.

Create a new migration that includes:

1. CREATE TABLE LogEntry
2. Convert SavedView.filtersJson to JSON
3. Convert SavedView.columnsJson to JSON NULL
4. Convert Setting.valueJson to JSON
5. Add Employee unique index on tenantId + workEmail
6. Add all missing composite indexes from the Sprint 0 checklist

Then run and share output:

npx prisma validate
npx prisma migrate reset --force
npm run db:seed

A clean database must be created successfully from migrations only.

==================================================
BLOCKER 2: FIX COVERAGE
==================================================

You reported 74.62% coverage.

Required coverage is 90%.

This is not acceptable.

Fix tests until this passes:

npm run test:coverage

Required:

90% statements
90% branches
90% functions
90% lines

Do not lower the threshold.

==================================================
BLOCKER 3: ADD REAL UNIT AND E2E TESTS
==================================================

Currently, tests are mostly integration tests only.

Add real unit tests under:

tests/unit/

Add real E2E tests under:

tests/e2e/

The following commands must pass:

npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage

Do not report “unit tests passing” if there are no unit test files.

Minimum unit tests:

- validate login user not found
- wrong password
- disabled user
- locked user
- admin login rejects employee
- session creation
- refresh token hash validation
- token reuse detection
- logout
- logout all
- revoke session
- audit log creation
- log formatting
- log RBAC policy

Minimum E2E tests:

- employee login -> me -> refresh -> logout
- admin login -> logs -> export logs
- employee login -> logs returns 403
- old refresh token reuse revokes session family
- tenant isolation failure

==================================================
BLOCKER 4: FIX POSTMAN/NEWMAN
==================================================

You reported:

27 assertions, 24 passing

That is not complete.

Fix Postman tests until Newman returns:

27 assertions, 27 passing

Share the Newman output.

Also remove the old duplicate collection unless needed:

docs/postman/EMS.postman_collection.json

Keep one final clean collection:

docs/postman/EMS-API.postman_collection.json
docs/postman/EMS.postman_environment.json

==================================================
BLOCKER 5: FIX SWAGGER RESPONSE SCHEMAS
==================================================

Route schemas are still incomplete.

Every route must include:

body
headers
params
querystring
response schemas
error response schemas
auth requirements
permission requirements
examples

Add response schemas for:

POST /auth/login
POST /auth/admin/login
POST /auth/refresh
POST /auth/logout
POST /auth/logout-all
GET /auth/me
GET /auth/sessions
DELETE /auth/sessions/:sessionId
GET /admin/logs
GET /admin/logs/:id
GET /admin/logs/export
GET /admin/logs/stream

Frontend team must be able to use Swagger as the API contract.

==================================================
BLOCKER 6: FINISH LOGGING PAGE PROPERLY
==================================================

LogEntry model exists in schema, but migration is missing.

Also, logs are not fully production-grade yet.

Fix:

1. Add LogEntry migration.
2. Persist application logs into LogEntry.
3. Do not synchronously write every debug log to MySQL on hot paths.
4. Use async queue/buffer or clearly documented pluggable transport.
5. Add search filter.
6. Add page and limit pagination metadata.
7. Use from/to query params consistently instead of startDate/endDate.
8. Fix level color mapping:
   error = red
   warn = orange
   info = blue
   debug = purple
   trace = gray
9. Verify EMPLOYEE gets 403 on all log APIs.
10. Fix stream endpoint with correct Fastify streaming implementation.

Required log response fields:

id
level
levelLabel
levelColor
module
message
requestId
actorUserId
tenantId
metadata
timestampUtc
timestampIstDisplay

IST format:

DD/MM/YYYY hh:mm:ss A IST

==================================================
BLOCKER 7: COMPLETE DOCUMENTATION AND CHARTS
==================================================

Current documentation is not enough.

Create this structure:

docs/
api/
ui-team-api-guide.md
auth-api.md
error-codes.md
postman-guide.md
backend/
backend-design-document.md
architecture.md
module-structure.md
auth-session-internals.md
rbac-permissions.md
database-design.md
logging-design.md
testing-strategy.md
deployment-guide.md
diagrams/
erd.md
system-architecture.md
auth-flow.md
refresh-token-flow.md
employee-onboarding-flow.md
leave-flow.md
attendance-flow.md
permission-flow.md
logging-flow.md
audit-log-flow.md
database-index-map.md
decisions/
adr-001-tech-stack.md
adr-002-fastify.md
adr-003-prisma.md
adr-004-auth-session-design.md
adr-005-logging-observability.md

Use Mermaid diagrams.

Business asked for charts of how each thing is inter-related. Add diagrams properly.

==================================================
BLOCKER 8: CLEAN ZIP DELIVERY
==================================================

I understand \_\_MACOSX and .DS_Store come from Mac zip. Those can be ignored.

But do not include:

.env
coverage/
node_modules/
logs/
.nyc_output/

Shared ZIPs should include .env.example only.

==================================================
BLOCKER 9: SHARE REAL COMMAND OUTPUT
==================================================

After fixing, share actual terminal outputs for:

npm ci
npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npx prisma migrate reset --force
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

Do not mark Sprint 0 complete until all of these pass.

==================================================
NEXT PAGE AFTER APPROVAL
==================================================

Once these blockers are fixed and reviewed, start Page 02 only.

Page 02 is Forgot Password / Reset Password.

Do not start employee, leave, attendance, or dashboard modules yet.

For Page 02, implement:

POST /api/v1/auth/forgot-password
GET /api/v1/auth/reset-password/validate?token=
POST /api/v1/auth/reset-password

Rules:

- forgot-password always returns 202 to avoid email enumeration
- store only hashed reset token
- reset token TTL: 30 minutes
- reset token single-use only
- reset password revokes all active sessions
- write audit logs
- send reset email through BullMQ job
- document APIs in Swagger, Postman, UI guide, and backend internals
- add unit, integration, and E2E tests
- keep coverage above 90%

Do not begin Page 02 until Sprint 0 is fully approved.

Sprint 0 review result: NOT APPROVED for Page 02 yet.

You are close, but the current delivery is still not accepted as production-ready.

Do not start Page 02 until these final blockers are fixed.

==================================================
BLOCKER 1: COVERAGE IS BELOW 90%
==================================================

You reported:

Statements: 89.12%
Branches: 84.91%
Functions: 86.07%
Lines: 89.12%

This does not meet the 90% requirement.

Required:

90% statements
90% branches
90% functions
90% lines

Do not mark 89.12% as passing.

Fix coverage by adding meaningful tests, especially for:

src/config/redis.js
src/middleware/errorHandler.js
src/middleware/requestLogging.js
src/middleware/resolveTenant.js
src/modules/auth/auth.controller.js
src/modules/auth/auth.repository.js
src/modules/logs/logs.controller.js
src/modules/logs/logs.service.js
src/plugins/requestId.js
src/plugins/swagger.js

Run:

npm run test:coverage

It must pass with all four metrics >= 90%.

==================================================
BLOCKER 2: FIX POSTMAN/NEWMAN FAILURES
==================================================

Current Newman report shows:

27 assertions
24 passing
3 failing

Failures include:

- Revoke Specific Session expected 200 but got 404
- Employee Cannot Access Logs expected 403 but got 200
- Cannot read properties of undefined reading code

This is not acceptable.

Fix the Postman collection and/or API behavior until Newman returns:

27 assertions
27 passing
0 failing

Important:

- Employee must never access /admin/logs.
- Revoke session test must create or capture a valid revocable session ID.
- Negative tests must use the correct employee token, not admin token.
- Response-shape tests must handle actual error format safely.

Run and share:

newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

==================================================
BLOCKER 3: FIX LINT OUTPUT
==================================================

SPRINT0_COMPLETION_OUTPUT.txt still shows 17 ESLint errors.

You must provide fresh output proving:

npm run lint

returns 0 errors.

Do not include stale failed output in the completion report.

==================================================
BLOCKER 4: FIX TEST SCRIPT SEPARATION
==================================================

.mocharc.json currently has:

"spec": ["tests/**/*.test.js"]

This causes npm run test:unit, npm run test:integration, and npm run test:e2e to run the wrong scope.

Fix the Mocha configuration so each command runs only its category:

npm run test:unit

- only tests/unit/\*_/_.test.js

npm run test:integration

- only tests/integration/\*_/_.test.js

npm run test:e2e

- only tests/e2e/\*_/_.test.js

Then share separate outputs.

Do not report unit/integration/e2e as complete if each command is actually running all tests.

==================================================
BLOCKER 5: FIX SWAGGER RESPONSE SCHEMAS
==================================================

Route schemas still do not include complete response contracts.

Every route must include:

body
headers
params
querystring
response schemas
error response schemas
auth requirement
permission requirement
examples

Add proper response schemas for:

POST /api/v1/auth/login
POST /api/v1/auth/admin/login
POST /api/v1/auth/refresh
POST /api/v1/auth/logout
POST /api/v1/auth/logout-all
GET /api/v1/auth/me
GET /api/v1/auth/sessions
DELETE /api/v1/auth/sessions/:sessionId
GET /api/v1/admin/logs
GET /api/v1/admin/logs/:id
GET /api/v1/admin/logs/export
GET /api/v1/admin/logs/stream

Swagger must be good enough for frontend developers to build from it without guessing.

==================================================
BLOCKER 6: CLEAN ZIP DELIVERY
==================================================

I understand \_\_MACOSX and .DS_Store are Mac zip artifacts and can be ignored.

But this ZIP still includes:

.env
.git/
coverage/
newman/

Do not include those in the shared delivery ZIP.

.env contains real-looking credentials. Remove it from ZIPs and rotate the password if it is real.

Final ZIP should include:

- source code
- tests
- docs
- prisma schema/migrations
- package files
- .env.example

Final ZIP should not include:

- .env
- .git
- coverage
- newman output
- node_modules
- logs
- .nyc_output

==================================================
BLOCKER 7: COMPLETE REQUIRED DOCUMENTATION STRUCTURE
==================================================

Current docs are improved but not enough for the business requirement.

Create:

docs/
api/
ui-team-api-guide.md
auth-api.md
error-codes.md
postman-guide.md
backend/
backend-design-document.md
architecture.md
module-structure.md
auth-session-internals.md
rbac-permissions.md
database-design.md
logging-design.md
testing-strategy.md
deployment-guide.md
diagrams/
erd.md
system-architecture.md
auth-flow.md
refresh-token-flow.md
employee-onboarding-flow.md
leave-flow.md
attendance-flow.md
permission-flow.md
logging-flow.md
audit-log-flow.md
database-index-map.md
decisions/
adr-001-tech-stack.md
adr-002-fastify.md
adr-003-prisma.md
adr-004-auth-session-design.md
adr-005-logging-observability.md

Use Mermaid diagrams.

Business specifically asked for charts of how everything is interconnected.

==================================================
BLOCKER 8: COMPLETE DATABASE INDEX REQUIREMENTS
==================================================

Some indexes are still basic and not the full composite indexes requested.

Add missing composite indexes such as:

User:
@@index([tenantId, status, deletedAt])
@@index([tenantId, memberType, status])

Employee:
@@index([tenantId, employmentStatus, deletedAt])
@@index([tenantId, departmentId, employmentStatus])
@@index([tenantId, managerId, employmentStatus])
@@index([tenantId, joinedOn])
@@index([tenantId, firstName, lastName])

Department:
@@index([tenantId, parentId, deletedAt])
@@index([tenantId, depth])

AttendanceRecord:
@@index([tenantId, employeeId, attendanceDate])
@@index([tenantId, status, attendanceDate])

LeaveRequest:
@@index([tenantId, employeeId, status])
@@index([tenantId, status, submittedAt])
@@index([tenantId, employeeId, startDate, endDate])
@@index([tenantId, approverId, status])

Holiday:
@@index([tenantId, holidayDate])
@@index([tenantId, location, holidayDate])

AuditLog:
@@index([tenantId, entityType, entityId, createdAt])
@@index([tenantId, actorUserId, createdAt])
@@index([tenantId, action, createdAt])

Notification:
@@index([tenantId, userId, readAt, createdAt])

Then create migration and run:

npx prisma validate
npx prisma migrate reset --force
npm run db:seed

==================================================
BLOCKER 9: FIX LOGGING FINAL GAPS
==================================================

Logging is better, but still needs final polish.

Required fixes:

1. Use from/to filters instead of startDate/endDate.
2. Add search filter.
3. Return pagination metadata:
   page
   limit
   total
   totalPages
4. Ensure warn color is orange, not yellow.
5. Make stream endpoint Fastify-safe.
6. Clearly implement or document async/buffered log persistence.
7. Add tests for:
   - employee blocked from every log endpoint
   - level filter
   - module filter
   - search filter
   - from/to filter
   - pagination
   - CSV export
   - JSON export
   - NDJSON stream
   - IST timestamp format

==================================================
BLOCKER 10: SECURITY AUDIT
==================================================

The included npm output still shows:

12 vulnerabilities
1 critical
9 high

This cannot be called production-ready.

Run:

npm audit

Then either:

1. fix vulnerabilities, or
2. provide SECURITY_EXCEPTIONS.md explaining each remaining issue and why it is acceptable temporarily.

CI must fail on serious vulnerabilities unless a documented exception exists.

==================================================
FINAL ACCEPTANCE COMMANDS
==================================================

After fixes, share fresh output for:

npm ci
npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npx prisma migrate reset --force
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json
npm audit

Acceptance criteria:

- Lint: 0 errors
- Tests: all passing
- Coverage: >=90% statements, branches, functions, lines
- Newman: 0 failed assertions
- Prisma: schema valid and clean migration reset works
- No .env in ZIP
- No .git in ZIP
- No coverage/newman artifacts in ZIP
- Swagger has full route-level response schemas
- Docs and diagrams created
- Employee cannot access any admin log endpoint
- Page 02 has not started yet

Only after this is approved, start Page 02.

Sprint 0 review result: CONDITIONALLY CLOSE, BUT NOT APPROVED AS PRODUCTION-READY YET.

We are accepting the branch coverage exception because business has approved that rare logging/error branches do not need full coverage right now.

However, the uploaded ZIP does not match your latest summary.

You said:

- 212 passing tests
- 0 lint errors
- coverage updated
- ready for Page 02

But the actual uploaded ZIP still contains:

- SPRINT0_COMPLETION_OUTPUT.txt showing 17 ESLint errors
- reports showing 169 tests, not 212
- Newman report with 3 failing assertions
- .env included in ZIP
- .git included in ZIP
- coverage/ included in ZIP
- newman/ included in ZIP
- no complete docs/diagram structure
- no proper route-level Swagger response schemas

Do not call this production-ready yet.

==================================================
FAST FINAL GATE BEFORE PAGE 02
==================================================

Fix only these items now. Do not add extra scope.

1. Fix Newman/Postman failures

Current Newman failures:

- Revoke Specific Session expected 200 but got 404
- Employee Cannot Access Logs expected 403 but got 200
- Cannot read properties of undefined reading code

Required result:

27 assertions
27 passing
0 failing

Important:

- Employee must never access /api/v1/admin/logs, /api/v1/admin/logs/:id, /api/v1/admin/logs/export, or /api/v1/admin/logs/stream.
- Revoke session test must capture a real session ID before calling DELETE /auth/sessions/:sessionId.
- Negative tests must use the employee token, not admin token.
- Error test scripts must safely check response.error before reading response.error.code.

Run:

newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

2. Update real completion output

Replace stale SPRINT0_COMPLETION_OUTPUT.txt with fresh output showing:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json
npx prisma validate
npx prisma migrate reset --force
npm run db:seed

Do not include old failed lint output.

3. Handle branch coverage exception properly

Since branch coverage is accepted as an exception, document it clearly.

Add:

docs/backend/testing-strategy.md

Include:

- Statements, lines, and functions must remain above 90%.
- Branch coverage is temporarily accepted at >= 85% for Sprint 0 because remaining branches are rare logging/error fallback paths.
- This exception expires before production release.
- CI threshold must match the agreed standard, otherwise CI will fail.

Update .c8rc.json accordingly if needed:

{
"statements": 90,
"branches": 85,
"functions": 90,
"lines": 90
}

Do not claim 87.39% branches meets 90%. Say it is an approved exception.

4. Add route-level Swagger response schemas

Add response schemas for all current APIs:

POST /api/v1/auth/login
POST /api/v1/auth/admin/login
POST /api/v1/auth/refresh
POST /api/v1/auth/logout
POST /api/v1/auth/logout-all
GET /api/v1/auth/me
GET /api/v1/auth/sessions
DELETE /api/v1/auth/sessions/:sessionId
GET /api/v1/admin/logs
GET /api/v1/admin/logs/:id
GET /api/v1/admin/logs/export
GET /api/v1/admin/logs/stream

Every route should include:

- 200/201 response
- 400 response
- 401 response
- 403 response where applicable
- 404 response where applicable
- common error shape

5. Add minimum required documentation structure

Do not write huge docs now. Create the correct structure and enough useful content.

Create:

docs/api/ui-team-api-guide.md
docs/api/auth-api.md
docs/api/error-codes.md
docs/api/postman-guide.md

docs/backend/backend-design-document.md
docs/backend/auth-session-internals.md
docs/backend/rbac-permissions.md
docs/backend/database-design.md
docs/backend/logging-design.md
docs/backend/testing-strategy.md

docs/diagrams/erd.md
docs/diagrams/system-architecture.md
docs/diagrams/auth-flow.md
docs/diagrams/refresh-token-flow.md
docs/diagrams/logging-flow.md

docs/decisions/adr-001-tech-stack.md
docs/decisions/adr-002-fastify-prisma-mysql.md
docs/decisions/adr-003-auth-session-design.md
docs/decisions/adr-004-logging-observability.md

Use Mermaid diagrams.

6. Clean ZIP delivery

Final ZIP must not include:

.env
.git/
coverage/
newman/
node_modules/
logs/
.nyc_output/

It may include:

- .env.example
- source
- tests
- docs
- prisma schema/migrations
- package.json
- package-lock.json
- README

7. Commit everything

The actual uploaded ZIP shows many untracked files.

Commit final work:

git status
git add .
git commit -m "chore(sprint0): finalize foundation acceptance fixes"
git status

Final git status must be clean.

==================================================
AFTER THIS, START PAGE 02
==================================================

Once the above is done, start Page 02:

Forgot Password / Reset Password.

Implement only these APIs:

POST /api/v1/auth/forgot-password
GET /api/v1/auth/reset-password/validate?token=
POST /api/v1/auth/reset-password

Requirements:

- forgot-password always returns 202 to prevent email enumeration
- reset token must be random, opaque, and stored hashed only
- token TTL: 30 minutes
- token is single-use only
- reset password revokes all active sessions for the user
- write audit logs:
  PASSWORD_RESET_REQUESTED
  PASSWORD_RESET_TOKEN_VALIDATED
  PASSWORD_RESET_COMPLETED
  PASSWORD_RESET_FAILED
- add BullMQ email job for reset email
- do not leak whether an email exists
- add Swagger schemas
- add Postman requests and tests
- add UI docs
- add backend docs
- add unit, integration, and E2E tests
- update seed/test helpers as needed

Page 02 should be completed in one isolated commit:

feat(auth): implement forgot password and reset password flow

Sprint 0 review result: CONDITIONALLY APPROVED TO START PAGE 02 DEVELOPMENT.

Important:
Do not call this production-ready yet. It is approved to continue development only.

The core foundation is now good enough to begin Page 02, but these cleanup/proof issues must be fixed in the same branch before Page 02 is marked complete:

1. Remove from delivery ZIP:
   - .env
   - .git/
   - coverage/
   - newman/
   - node_modules/
   - logs/
   - .nyc_output/

2. The included Newman JSON report still shows old failures:
   - 27 assertions
   - 24 passing
   - 3 failing

   Replace it with the real fresh report showing 30/30 passing, or remove old newman reports from ZIP and include only command output.

3. Add actual route-level Swagger response schemas in route files, not only global Swagger definitions.

4. Use /docs consistently. The app registers Swagger at /docs, but the report mentions /documentation.

5. Update logs filters from startDate/endDate to from/to.

6. Return pagination metadata for GET /api/v1/admin/logs:
   {
   page,
   limit,
   total,
   totalPages
   }

7. Confirm Mocha scripts truly run separately:
   npm run test:unit
   npm run test:integration
   npm run test:e2e

Do not spend too much time on this now. Fix these while starting Page 02.

==================================================
PAGE 02: FORGOT PASSWORD / RESET PASSWORD
==================================================

Start Page 02 now.

Implement only Forgot Password / Reset Password. Do not start Employee, Leave, Attendance, Dashboard, or Payroll yet.

Required APIs:

1. POST /api/v1/auth/forgot-password
2. GET /api/v1/auth/reset-password/validate?token=
3. POST /api/v1/auth/reset-password

Business and security rules:

- forgot-password must always return 202, even if the email does not exist.
- Do not leak whether an email exists.
- Reset token must be random, opaque, and stored hashed only.
- Never store raw reset token in DB.
- Token TTL: 30 minutes.
- Token must be single-use only.
- Reset password must revoke all active sessions for that user.
- Reset password must not allow same old password if policy says prevent reuse.
- New password must meet policy:
  - minimum 10 characters
  - uppercase
  - lowercase
  - number
  - special character
- Add rate limiting:
  - forgot-password: stricter than normal APIs
  - reset-password: stricter than normal APIs
- Write audit logs:
  - PASSWORD_RESET_REQUESTED
  - PASSWORD_RESET_EMAIL_QUEUED
  - PASSWORD_RESET_TOKEN_VALIDATED
  - PASSWORD_RESET_COMPLETED
  - PASSWORD_RESET_FAILED
- Add application logs for important events without exposing tokens/passwords.
- Redact password, token, resetToken, authorization, and cookie from logs.
- Add BullMQ job for reset email.
- For local development, email provider can be a mock adapter that logs only a safe message, never the full token.
- Add proper config:
  RESET_PASSWORD_TOKEN_TTL_MINUTES=30
  RESET_PASSWORD_RATE_LIMIT_MAX=5
  RESET_PASSWORD_RATE_LIMIT_WINDOW=15m
  FRONTEND_RESET_PASSWORD_URL=http://localhost:5173/reset-password

Database requirements:

Use existing PasswordResetToken model if present. If not complete, update it.

Required fields:

id
userId
tenantId
tokenHash
expiresAt
usedAt
createdAt
createdByIp
userAgent

Indexes:

@@index([userId, expiresAt])
@@index([tenantId, tokenHash])
@@index([expiresAt])
@@index([usedAt])

Implementation flow:

POST /auth/forgot-password

Request:

{
"email": "employee@company.com"
}

Response must always be:

202 Accepted

{
"success": true,
"data": {
"message": "If an account exists, password reset instructions have been sent."
}
}

Internal behavior:

1. Resolve tenant from X-Tenant-Key.
2. Normalize email.
3. Look up active user by tenantId + email.
4. If user does not exist, still return 202.
5. If user exists:
   - create reset token
   - hash token
   - store tokenHash with expiry
   - invalidate old unused reset tokens for that user if needed
   - queue email job
   - write audit log
6. Never return token in API response.

GET /auth/reset-password/validate?token=

Response if valid:

{
"success": true,
"data": {
"valid": true,
"expiresAt": "...",
"emailMasked": "p\*\*\*@acme.test"
}
}

Response if invalid/expired/used:

400 or 401 with:

{
"success": false,
"error": {
"code": "RESET_TOKEN_INVALID",
"message": "Reset token is invalid or expired."
}
}

POST /auth/reset-password

Request:

{
"token": "opaque-token-from-email",
"newPassword": "NewStrong@123"
}

Response:

{
"success": true,
"data": {
"message": "Password has been reset successfully. Please login again."
}
}

Internal behavior:

1. Hash provided token.
2. Find unused, unexpired token.
3. If invalid, return generic error.
4. Validate password strength.
5. Hash new password with Argon2id.
6. Update user password.
7. Mark token as used.
8. Revoke all sessions for user.
9. Write audit log.
10. Return success.

Required files:

src/modules/auth/passwordReset.controller.js
src/modules/auth/passwordReset.service.js
src/modules/auth/passwordReset.repository.js
src/modules/auth/passwordReset.validator.js

Or keep inside auth module if current architecture prefers, but keep code clean and readable.

Required tests:

Unit tests:

- forgot password always returns generic response
- unknown email does not leak account existence
- known email creates hashed token
- token hash is stored, raw token is never stored
- reset token validation success
- reset token validation expired
- reset token validation used
- reset password success
- weak password rejected
- invalid token rejected
- reset password revokes all sessions
- audit logs created

Integration tests:

- POST /auth/forgot-password known email
- POST /auth/forgot-password unknown email
- GET /auth/reset-password/validate valid token
- GET /auth/reset-password/validate invalid token
- POST /auth/reset-password valid token
- POST /auth/reset-password invalid token
- login with old password fails after reset
- login with new password succeeds after reset

E2E tests:

- forgot password -> capture test token from DB/test helper -> validate token -> reset password -> login new password -> old sessions revoked

Postman:
Add requests:

- Forgot Password
- Validate Reset Token
- Reset Password
- Login With New Password
- Invalid Reset Token
- Weak Password

Swagger:
Add route-level schemas for:

- request body
- query params
- 202 response
- 200 response
- 400 response
- 401 response
- 429 response

Docs:
Update:

- docs/api/auth-api.md
- docs/api/ui-team-api-guide.md
- docs/backend/auth-session-internals.md
- docs/diagrams/auth-flow.md
- docs/diagrams/refresh-token-flow.md

Add:

- docs/diagrams/password-reset-flow.md

Use Mermaid flowchart for password reset.

Commit:

feat(auth): implement forgot password and reset password flow

Final Page 02 acceptance:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npx prisma migrate reset --force
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

Acceptance conditions:

- Lint: 0 errors
- Tests: all passing
- Statements/lines/functions: >= 90%
- Branch coverage: >= 85% accepted for now
- Newman: 0 failures
- No raw reset token stored in DB
- No password/token leaked in logs or API response
- Reset password revokes sessions
- Old password fails after reset
- New password works after reset

Page 02 review result: HOTFIX REQUIRED, THEN MOVE TO PAGE 03.

Good work. Password reset is mostly implemented, but do not mark Page 02 closed until these small but important fixes are done.

==================================================
PAGE 02 HOTFIX
==================================================

1. Fix reset-token validate endpoint

Current:
GET /api/v1/auth/validate-reset-token?token=

Required:
GET /api/v1/auth/reset-password/validate?token=

Either rename the route or support both routes temporarily.

2. Make reset token tenant-scoped

Update:

validateResetToken(rawToken)
completePasswordReset(rawToken, newPassword, ip, userAgent)

to:

validateResetToken(tenantId, rawToken)
completePasswordReset(tenantId, rawToken, newPassword, ip, userAgent)

Query reset tokens by:

tenantId + tokenHash

Do not validate or consume a token outside the current tenant.

3. Remove runtime console logs

Remove console.log and console.error from:

src/jobs/emailJob.js
src/modules/auth/passwordReset.service.js

Use Pino/application logger instead.

Do not log raw reset token.
Do not log full reset URL.
Do not log passwords.

4. Add missing audit events

Add:

PASSWORD_RESET_REQUESTED
PASSWORD_RESET_EMAIL_QUEUED
PASSWORD_RESET_TOKEN_VALIDATED
PASSWORD_RESET_COMPLETED
PASSWORD_RESET_FAILED

For unknown email, still return 202, but audit/log internally as non-sensitive event.

5. Improve email provider support

Keep dev mock email adapter, but add real SMTP/API provider support.

Recommended env variables:

EMAIL_PROVIDER=resend
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
RESEND_API_KEY=
BREVO_SMTP_USER=
BREVO_SMTP_PASS=

For now, Nodemailer SMTP is acceptable, but structure the code so we can later switch to Resend API or Brevo API without changing business logic.

6. Add tests for the hotfix

Add/verify tests:

- validate reset token with correct tenant succeeds
- validate reset token with wrong tenant fails
- reset password with wrong tenant fails
- reset password writes PASSWORD_RESET_COMPLETED
- validate token writes PASSWORD_RESET_TOKEN_VALIDATED
- email queued writes PASSWORD_RESET_EMAIL_QUEUED
- failures write PASSWORD_RESET_FAILED
- no raw token or reset URL appears in logs
- both old and new validate endpoint work if supporting backward compatibility

7. Update docs/Postman/Swagger

Update:

docs/api/auth-api.md
docs/api/ui-team-api-guide.md
docs/backend/auth-session-internals.md
docs/diagrams/password-reset-flow.md
docs/postman/EMS-API.postman_collection.json

Add the final endpoint:

GET /api/v1/auth/reset-password/validate?token=

8. Run final Page 02 checks

Run:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

Acceptance:

- All tests passing
- Newman passing
- No console.\* in src runtime code
- Reset token is tenant-scoped
- Email provider config exists
- Docs updated

Commit:

fix(auth): harden password reset tenant scope and email logging

==================================================
AFTER HOTFIX, START PAGE 03
==================================================

Start Page 03: OTP Verification.

Implement:

POST /api/v1/auth/verify-otp
POST /api/v1/auth/resend-otp

Rules:

- OTP must be 6 digits.
- OTP TTL: 10 minutes.
- OTP must be stored hashed only.
- Max failed attempts: 5.
- Max resend count: 3.
- Resend cooldown: 60 seconds.
- Expired OTP must fail.
- Consumed OTP must fail.
- OTP verification should complete pending MFA login if challenge is valid.
- Never return OTP in API response.
- Never log OTP.
- Add email job for OTP email.
- Add audit logs:
  OTP_CHALLENGE_CREATED
  OTP_VERIFICATION_SUCCEEDED
  OTP_VERIFICATION_FAILED
  OTP_RESENT
  OTP_RESEND_BLOCKED
  OTP_EXPIRED
  OTP_LOCKED
- Add application logs without exposing OTP.
- Add Swagger schemas.
- Add Postman requests.
- Add UI API docs.
- Add backend docs.
- Add Mermaid OTP flow diagram.
- Add unit, integration, and E2E tests.

Required APIs:

POST /api/v1/auth/verify-otp

Request:
{
"challengeId": "string",
"code": "123456"
}

Success:
{
"success": true,
"data": {
"accessToken": "jwt",
"user": {},
"permissions": []
}
}

POST /api/v1/auth/resend-otp

Request:
{
"challengeId": "string"
}

Success:
{
"success": true,
"data": {
"message": "OTP resent successfully.",
"cooldownSeconds": 60
}
}

Commit:

feat(auth): implement otp verification and resend flow

Page 02 Hotfix review result: APPROVED TO MOVE FORWARD.

Do not pause the project anymore. Start Page 03 now.

However, while starting Page 03, patch these small Page 02 carry-forward items:

1. Add tests for the new final endpoint:
   GET /api/v1/auth/reset-password/validate?token=

Current tests still mainly use the deprecated endpoint:
GET /api/v1/auth/validate-reset-token?token=

Keep backward compatibility, but frontend must use the new endpoint.

2. Add PASSWORD_RESET_FAILED audit logs for all reset failure paths:
   - invalid token
   - expired token
   - already-used token
   - wrong tenant
   - password update failure
   - session revocation failure
   - email queue failure

3. Update completion output/report so it reflects Page 02 work, not only Sprint 0.

4. Keep SMTP support, but add clean provider config now:

EMAIL_PROVIDER=smtp
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

Also prepare optional provider config:

EMAIL_PROVIDER=resend
RESEND_API_KEY=
RESEND_FROM=

EMAIL_PROVIDER=brevo
BREVO_SMTP_USER=
BREVO_SMTP_PASS=
BREVO_SMTP_HOST=smtp-relay.brevo.com
BREVO_SMTP_PORT=587

Do not log password reset URLs, raw tokens, OTPs, passwords, cookies, or authorization headers.

Commit these as:

fix(auth): finalize password reset audit coverage and endpoint tests

==================================================
START PAGE 03 NOW: OTP VERIFICATION
==================================================

Implement Page 03: OTP Verification.

Required APIs:

1. POST /api/v1/auth/verify-otp
2. POST /api/v1/auth/resend-otp

Important:
Do not build dashboard, employee, attendance, leave, payroll, or settings yet. Only OTP.

Business rules:

- OTP must be 6 digits.
- OTP TTL: 10 minutes.
- OTP must be stored hashed only.
- Never store raw OTP.
- Never return OTP in API response.
- Never log OTP.
- Max failed attempts: 5.
- Max resend count: 3.
- Resend cooldown: 60 seconds.
- Expired OTP must fail.
- Used/consumed OTP must fail.
- Locked OTP challenge must fail.
- OTP verification should complete pending MFA login.
- OTP email should be sent through the existing email queue.
- In development mode, use mock email provider.
- In staging/production, use SMTP/Brevo/Resend provider.

Use existing OtpChallenge table, but update if required.

Recommended OtpChallenge fields:

id
tenantId
userId
challengeId
codeHash
purpose
deliveryChannel
destinationMasked
attempts
maxAttempts
resendCount
maxResends
lastSentAt
expiresAt
consumedAt
lockedAt
createdAt
updatedAt

Indexes:

@@index([tenantId, challengeId])
@@index([tenantId, userId, consumedAt])
@@index([expiresAt])
@@index([lockedAt])

OTP challenge purposes:

LOGIN_MFA
PASSWORD_RESET_CONFIRMATION
HIGH_RISK_LOGIN

For this page, implement LOGIN_MFA only, but keep enum/design extensible.

Request/response contracts:

POST /api/v1/auth/verify-otp

Request:

{
"challengeId": "string",
"code": "123456"
}

Success:

{
"success": true,
"data": {
"accessToken": "jwt",
"user": {},
"permissions": []
}
}

Error examples:

OTP_INVALID
OTP_EXPIRED
OTP_ALREADY_USED
OTP_LOCKED
OTP_ATTEMPTS_EXCEEDED
OTP_CHALLENGE_NOT_FOUND

POST /api/v1/auth/resend-otp

Request:

{
"challengeId": "string"
}

Success:

{
"success": true,
"data": {
"message": "OTP resent successfully.",
"cooldownSeconds": 60
}
}

Error examples:

OTP_RESEND_COOLDOWN
OTP_RESEND_LIMIT_EXCEEDED
OTP_CHALLENGE_NOT_FOUND
OTP_ALREADY_USED
OTP_EXPIRED

Audit logs required:

OTP_CHALLENGE_CREATED
OTP_VERIFICATION_SUCCEEDED
OTP_VERIFICATION_FAILED
OTP_RESENT
OTP_RESEND_BLOCKED
OTP_EXPIRED
OTP_LOCKED

Application logs:

- Log challenge lifecycle safely.
- Never log raw OTP.
- Never log full email content containing OTP.
- Include requestId, tenantId, userId when available.

Files to create/update:

src/modules/auth/otp.controller.js
src/modules/auth/otp.service.js
src/modules/auth/otp.repository.js
src/modules/auth/otp.validator.js
src/modules/auth/auth.routes.js
src/jobs/emailJob.js
src/jobs/emailQueue.js
src/utils/otp.js
prisma/schema.prisma
docs/api/auth-api.md
docs/api/ui-team-api-guide.md
docs/backend/auth-session-internals.md
docs/diagrams/otp-flow.md
docs/postman/EMS-API.postman_collection.json

Tests required:

Unit tests:

- generate OTP returns 6 digits
- OTP hash stored, raw OTP never stored
- verify valid OTP succeeds
- invalid OTP increments attempts
- 5 failed attempts locks challenge
- expired OTP fails
- used OTP fails
- resend within cooldown fails
- resend after cooldown succeeds
- resend after max resends fails
- audit logs are written
- no raw OTP in logs

Integration tests:

- login with MFA enabled returns mfaRequired and challengeId
- verify OTP success returns access token
- verify OTP invalid returns OTP_INVALID
- verify OTP expired returns OTP_EXPIRED
- resend OTP success
- resend OTP cooldown failure
- employee cannot use another tenant challenge

E2E tests:

- admin login with MFA -> challenge -> verify OTP -> me
- failed OTP 5 times -> challenge locked
- resend OTP -> verify latest OTP

Postman:
Add:

- MFA Login
- Verify OTP
- Verify Invalid OTP
- Resend OTP
- Resend Cooldown
- Locked OTP

Swagger:
Add route-level schemas:

- request body
- 200 response
- 400 response
- 401 response
- 429 response
- error examples

Run before commit:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

Acceptance:

- Lint 0 errors
- Tests all passing
- Newman 0 failures
- OTP is hashed only
- OTP never logged
- Tenant isolation verified
- MFA login flow works end-to-end

Commit:

feat(auth): implement otp verification and resend flow

Create free staging deployment on Oracle Cloud Always Free.

Goal:
Expose EMS backend on a public HTTPS URL so the UI team can consume APIs directly.

Target URL example:
https://ems-api.yourdomain.com/api/v1
or temporary:
http://<oracle-compute-public-ip>:3000/api/v1

==================================================
ORACLE CLOUD SETUP
==================================================

1. Create Oracle Cloud Free Tier account.

2. Choose home region carefully.
   Use the region closest to users/team. For India team, choose India region if available.

3. Create VCN/network.

Use default VCN or create:

VCN CIDR:
10.0.0.0/16

Public subnet:
10.0.0.0/24

Private subnet:
10.0.1.0/24

4. Create MySQL HeatWave DB System.

Go to:
Menu → Databases → MySQL HeatWave → DB Systems → Create DB System

Choose:

- Always Free
- Standalone
- Private subnet
- Storage: 50 GB
- Admin user: ems_admin
- Strong password

Save:

- MySQL private IP
- port 3306
- username
- password

5. Allow backend compute to access MySQL.

In private subnet security list or NSG, allow inbound:

Source CIDR:
10.0.0.0/24

Protocol:
TCP

Ports:
3306, 33060

6. Create Compute VM.

Go to:
Menu → Compute → Instances → Create Instance

Choose Always Free shape:

- Prefer Ampere A1 if available
- Ubuntu 22.04 or Oracle Linux
- Public subnet
- Assign public IP
- Add SSH key

7. Open backend ports.

In public subnet security list or NSG, allow:

TCP 22 from your IP only
TCP 80 from 0.0.0.0/0
TCP 443 from 0.0.0.0/0
TCP 3000 temporarily from your IP only during testing

Do not expose MySQL port 3306 publicly.

8. SSH into compute.

ssh -i /path/to/key opc@<public-ip>

For Ubuntu image, user may be:

ssh -i /path/to/key ubuntu@<public-ip>

9. Install runtime.

sudo apt update
sudo apt install -y git curl build-essential nginx

Install Node 20:

curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

Install PM2:

sudo npm install -g pm2

10. Install Redis.

For testing, use Redis on same VM:

sudo apt install -y redis-server
sudo systemctl enable redis-server
sudo systemctl start redis-server

11. Clone backend.

git clone <repo-url> ems-backend
cd ems-backend
npm ci

12. Create production env.

nano .env

Use:

NODE_ENV=production
PORT=3000
API_PREFIX=/api/v1

DATABASE_URL="mysql://ems_admin:<password>@<mysql-private-ip>:3306/ems?connection_limit=10&pool_timeout=20"
REDIS_URL="redis://127.0.0.1:6379"

JWT_SECRET="<generate-strong-secret>"
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

DEFAULT_TENANT_KEY=acme
CORS_ORIGIN="http://localhost:5173,https://your-frontend-domain.com"

SESSION_COOKIE_NAME=refreshToken
SESSION_MAX_AGE_DAYS=7

RESET_PASSWORD_TOKEN_TTL_MINUTES=30
RESET_PASSWORD_RATE_LIMIT_MAX=5
RESET_PASSWORD_RATE_LIMIT_WINDOW="15 minutes"
FRONTEND_RESET_PASSWORD_URL="https://your-frontend-domain.com/reset-password"

EMAIL_PROVIDER=smtp
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="EMS <noreply@yourdomain.com>"

13. Create database.

Connect to MySQL from compute:

mysql -h <mysql-private-ip> -u ems_admin -p

Then:

CREATE DATABASE ems CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

14. Run migrations and seed.

npx prisma migrate deploy
npm run db:seed

15. Start backend with PM2.

pm2 start src/server.js --name ems-backend
pm2 save
pm2 startup

16. Configure Nginx reverse proxy.

sudo nano /etc/nginx/sites-available/ems-backend

Add:

server {
listen 80;
server_name ems-api.yourdomain.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

}

Enable:

sudo ln -s /etc/nginx/sites-available/ems-backend /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

17. Add HTTPS.

If domain is pointed to Oracle public IP:

sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d ems-api.yourdomain.com

18. Verify.

curl https://ems-api.yourdomain.com/docs
curl https://ems-api.yourdomain.com/api/v1/health

19. Give UI team:

API base URL:
https://ems-api.yourdomain.com/api/v1

Swagger:
https://ems-api.yourdomain.com/docs

Tenant header:
X-Tenant-Key: acme

20. Add deployment documentation.

Create:

docs/backend/oracle-cloud-free-deployment.md

Include:

- network diagram
- MySQL private IP setup
- security list rules
- environment variables
- PM2 commands
- Nginx config
- SSL setup
- backup/restore notes
- restart procedure
- logs procedure

Commit:

docs(deploy): add oracle cloud always free deployment guide

Page 03 review result: HOTFIX REQUIRED BEFORE MOVING TO PAGE 04.

Page 02 Password Reset is accepted enough to continue.

Page 03 OTP Verification is not complete yet because OTP APIs exist, but the MFA login flow is not connected.

Fix these items immediately, then move to Page 04.

==================================================
PAGE 03 OTP HOTFIX
==================================================

1. Connect OTP to login

In auth.service.js, replace:

if (user.mfaEnabled) {
// TODO: Generate OTP challenge and return mfaRequired
throw new AppError('MFA not yet implemented', 'MFA_REQUIRED', 400);
}

with real OTP challenge generation.

When user.mfaEnabled is true:

- validate email/password
- create OTP challenge
- queue OTP email
- do NOT create full authenticated session yet
- do NOT return access token yet
- return:

{
"success": true,
"data": {
"mfaRequired": true,
"challengeId": "...",
"destinationMasked": "m****\*\*\*****9@gmail.com",
"expiresIn": 600
}
}

2. Complete login after OTP verification

POST /api/v1/auth/verify-otp must:

- verify tenant-scoped challenge
- verify OTP code
- mark challenge consumed
- create session
- set refresh token cookie
- return access token, user, permissions, and sessionId

Expected success response:

{
"success": true,
"data": {
"accessToken": "...",
"user": {
"id": "...",
"email": "...",
"memberType": "EMPLOYEE"
},
"permissions": [],
"sessionId": "..."
}
}

3. Fix resend OTP

Remove authenticate middleware from:

POST /api/v1/auth/resend-otp

During MFA login, user does not have an access token yet.

Resend must work with:

{
"challengeId": "..."
}

Rules:

- tenant-scoped lookup
- no access token required
- do not reveal user email
- use destinationMasked from challenge
- rate limit route
- enforce cooldown

4. Enforce resend cooldown

If lastSentAt is less than 60 seconds ago, return:

{
"success": false,
"error": {
"code": "OTP_RESEND_COOLDOWN",
"message": "Please wait before requesting another OTP.",
"details": {
"cooldownSeconds": 60
}
}
}

5. Use cryptographically secure OTP

Replace Math.random OTP generation with:

crypto.randomInt(100000, 1000000).toString()

Use secure challengeId:

crypto.randomUUID()

or the existing secure ID helper if it uses crypto.

6. Rename or map audit events

Use these final event names:

OTP_CHALLENGE_CREATED
OTP_VERIFICATION_SUCCEEDED
OTP_VERIFICATION_FAILED
OTP_RESENT
OTP_RESEND_BLOCKED
OTP_EXPIRED
OTP_LOCKED
OTP_EMAIL_QUEUED
OTP_EMAIL_FAILED

7. Test real email sending

Configure SMTP using Brevo or Resend.

For testing, send email to:

mohammadsaeedafri9@gmail.com

Add or run a safe email test command:

npm run email:test -- --to mohammadsaeedafri9@gmail.com

The test email must not contain a real password reset token or real OTP unless it is generated through the real API flow.

Also test the real flows:

A. Password reset email:
POST /api/v1/auth/forgot-password

Body:
{
"email": "mohammadsaeedafri9@gmail.com"
}

B. OTP email:
Enable MFA for a seeded/test user with email:
mohammadsaeedafri9@gmail.com

Then call:
POST /api/v1/auth/login

Confirm the OTP email is received.

8. Add missing tests

Add/verify tests:

- login with mfaEnabled returns mfaRequired and challengeId
- login with mfaEnabled does not return accessToken before OTP
- verify OTP returns accessToken, user, permissions, sessionId
- verify OTP sets refresh cookie
- resend OTP works without access token
- resend OTP cooldown returns OTP_RESEND_COOLDOWN
- resend OTP max limit returns OTP_RESEND_LIMIT_EXCEEDED
- OTP uses crypto-secure generation
- wrong tenant cannot verify challenge
- raw OTP is never stored
- raw OTP is never logged

9. Update docs/Postman/Swagger

Update:

docs/api/auth-api.md
docs/api/ui-team-api-guide.md
docs/backend/auth-session-internals.md
docs/diagrams/otp-flow.md
docs/postman/EMS-API.postman_collection.json

Postman must include:

- MFA Login
- Verify OTP Success
- Verify OTP Invalid
- Resend OTP
- Resend OTP Cooldown
- Resend OTP Limit
- Wrong Tenant OTP Failure

10. Run checks

Run:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

Commit:

fix(auth): complete mfa otp login flow

==================================================
AFTER THIS, MOVE TO PAGE 04
==================================================

Start Page 04: HR Admin Dashboard APIs.

Do not start Employee onboarding yet. Page 04 is only the HR/Admin dashboard data shown in the wireframe.

Required APIs:

GET /api/v1/analytics/summary
GET /api/v1/analytics/attendance?range=7d|30d|90d
GET /api/v1/analytics/headcount-by-department
GET /api/v1/analytics/leave-summary?range=30d
GET /api/v1/analytics/recent-activity?limit=10
GET /api/v1/audit-logs?limit=10

Access:

- SUPER_ADMIN and HR_ADMIN only
- MANAGER should not access HR admin dashboard
- EMPLOYEE must get 403

Dashboard summary response:

{
"success": true,
"data": {
"totalEmployees": 0,
"activeEmployees": 0,
"onLeaveToday": 0,
"presentToday": 0,
"pendingLeaveRequests": 0,
"openRegularizationRequests": 0,
"departmentsCount": 0,
"newJoinersThisMonth": 0
}
}

Attendance chart response:

{
"success": true,
"data": {
"range": "30d",
"series": [
{
"date": "2026-05-17",
"present": 10,
"absent": 1,
"leave": 2,
"wfh": 3
}
]
}
}

Headcount by department response:

{
"success": true,
"data": [
{
"departmentId": "...",
"departmentName": "Engineering",
"employeeCount": 20,
"activeCount": 18
}
]
}

Leave summary response:

{
"success": true,
"data": {
"pending": 0,
"approved": 0,
"rejected": 0,
"withdrawn": 0
}
}

Recent activity response:

{
"success": true,
"data": [
{
"id": "...",
"action": "LOGIN",
"actorName": "HR Admin",
"entityType": "User",
"createdAt": "...",
"createdAtIstDisplay": "17/05/2026 04:50:23 PM IST"
}
]
}

Technical rules:

- Use Redis caching for dashboard summary and charts.
- Cache TTL: 60 seconds for summary, 5 minutes for department chart.
- Use tenantId in every query.
- Use efficient Prisma groupBy where possible.
- Avoid N+1 queries.
- Add indexes if any dashboard query needs it.
- Add unit, integration, and E2E tests.
- Add Swagger schemas.
- Add Postman requests.
- Update UI API docs.
- Update backend analytics docs.

Commit:

feat(analytics): implement hr admin dashboard apis

Create Oracle Cloud Always Free deployment documentation and staging setup.

Target:

Backend:
Oracle Always Free Compute VM

Database:
Oracle MySQL HeatWave Always Free

Redis:
Redis installed on the Compute VM for testing

Public URL:
http://<oracle-public-ip>:3000 initially
Later:
https://ems-api.<domain>/api/v1

Steps:

1. Create OCI account.
2. Create VCN with:
   - public subnet for backend compute
   - private subnet for MySQL
3. Create Always Free MySQL HeatWave DB system in private subnet.
4. Create Always Free Compute VM in public subnet.
5. Allow Compute subnet to access MySQL port 3306.
6. Do not expose MySQL publicly.
7. SSH into Compute VM.
8. Install Node.js 20, Git, PM2, Nginx, Redis.
9. Clone EMS backend repo.
10. Create .env on server.
11. Set DATABASE_URL to Oracle MySQL private IP.
12. Set REDIS_URL to local Redis.
13. Run:

npm ci
npx prisma migrate deploy
npm run db:seed
pm2 start src/server.js --name ems-backend
pm2 save

14. Configure Nginx reverse proxy.
15. Add SSL with Certbot when domain is ready.
16. Share with UI team:

API Base URL:
https://ems-api.<domain>/api/v1

Swagger:
https://ems-api.<domain>/docs

Tenant Header:
X-Tenant-Key: acme

17. Create docs:

docs/backend/oracle-cloud-free-deployment.md

Include:

- network diagram
- VCN/subnet rules
- MySQL setup
- Redis setup
- environment variables
- PM2 commands
- Nginx config
- SSL setup
- how UI team should call APIs
- backup notes
- troubleshooting notes

Commit:

docs(deploy): add oracle cloud always free setup guide

Review result: Page 03 is almost accepted, but do one short cleanup before Page 04.

Do not split focus. Complete the items below in this exact order, then start Page 04 HR Admin Dashboard.

==================================================
PART A — FINAL PAGE 03 CLEANUP
==================================================

1. Apply MFA to Admin Login also

Normal login now handles MFA, but admin login still bypasses MFA.

Fix:

POST /api/v1/auth/admin/login

If HR_ADMIN or SUPER_ADMIN has mfaEnabled=true:

- validate email/password
- validate admin memberType
- generate OTP challenge
- queue OTP email to the user email stored in DB
- return 202 with:

{
"success": true,
"data": {
"mfaRequired": true,
"challengeId": "...",
"destinationMasked": "m\*\*\*@company.com",
"expiresIn": 600
}
}

Do not create session before OTP is verified.

2. OTP email source must come from DB only

For OTP and password reset, always use:

User.email

Never accept a destination email from request body.

For future employee onboarding, document this rule now:

- HR enters Employee.workEmail during onboarding.
- If access account is created, User.email must be created from Employee.workEmail.
- Employee.personalEmail is optional and should not be used for login/MFA unless business later allows it.
- Password reset and OTP send to User.email only.
- User.email and Employee.workEmail must remain synced during onboarding/update flows.

Add this to:
docs/api/auth-api.md
docs/backend/auth-session-internals.md
docs/backend/backend-design-document.md

3. Add a non-production test user using my email

For staging/testing only, add a safe seed option or script to create/update a test user:

email:
mohammadsaeedafri9@gmail.com

Use it only in development/staging, not production.

Suggested script:

npm run seed:test-email-user -- --email mohammadsaeedafri9@gmail.com

The script should:

- find tenant by DEFAULT_TENANT_KEY
- create or update Employee.workEmail = mohammadsaeedafri9@gmail.com
- create or update User.email = mohammadsaeedafri9@gmail.com
- set mfaEnabled = true
- assign EMPLOYEE role at minimum
- print only safe output
- never print password if generated automatically

4. Add real test email command

Add:

npm run email:test -- --to mohammadsaeedafri9@gmail.com

This should send a harmless email:

Subject:
EMS Email Test

Body:
This is a safe test email from EMS backend.

Do not include OTP.
Do not include reset token.
Do not include password.
Do not include authorization/cookie values.

5. Configure free email provider

Use Brevo SMTP first because current code already uses Nodemailer SMTP.

Brevo free plan has no time limit and includes 300 emails/day. Resend is also good, with 3,000 emails/month and 100/day. Use Brevo first for fastest SMTP setup. Official references: Brevo free limit = 300/day, Resend free = 3,000/month and 100/day.

Add/update env:

EMAIL_PROVIDER=smtp
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=<brevo-login>
SMTP_PASS=<brevo-smtp-key>
SMTP_FROM="EMS <verified-sender@yourdomain.com>"

Also keep optional Resend config ready:

EMAIL_PROVIDER=resend
RESEND_API_KEY=
RESEND_FROM=

6. Test actual email delivery

Run the safe test email first:

npm run email:test -- --to mohammadsaeedafri9@gmail.com

Then test password reset email using the API:

POST /api/v1/auth/forgot-password

Headers:
X-Tenant-Key: acme

Body:
{
"email": "mohammadsaeedafri9@gmail.com"
}

Expected:

- API returns 202
- email is received at mohammadsaeedafri9@gmail.com
- raw reset token is not logged
- reset token is not returned in API response

Then test OTP email:

- ensure DB user email is mohammadsaeedafri9@gmail.com
- ensure mfaEnabled=true
- call POST /api/v1/auth/login
- verify 202 response with mfaRequired=true
- confirm OTP email is received at mohammadsaeedafri9@gmail.com
- verify OTP completes login

7. Fix final Postman collection

Use only this final collection:

docs/postman/EMS-API.postman_collection.json

Add missing requests:

Password Reset:

- Forgot Password
- Validate Reset Token
- Reset Password
- Invalid Reset Token
- Weak Password

OTP:

- MFA Login
- Admin MFA Login
- Verify OTP
- Verify Invalid OTP
- Resend OTP
- Resend OTP Cooldown
- Resend OTP Limit
- Wrong Tenant OTP Failure

Remove or deprecate the old duplicate collection:

docs/postman/EMS.postman_collection.json

8. Fix all test failures

Do not say “pre-existing failures are unrelated.”

Final result must be:

0 failing tests

Run and share:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

Acceptance:

- Lint 0 errors
- Tests 0 failing
- Newman 0 failing
- Normal MFA login works
- Admin MFA login works
- OTP sends to DB User.email only
- password reset sends to DB User.email only
- real safe test email sent to mohammadsaeedafri9@gmail.com
- no OTP/reset token/password logged

Commit:

fix(auth): finalize mfa email delivery and admin otp flow

==================================================
PART B — ORACLE CLOUD ALWAYS FREE SETUP GUIDE
==================================================

Create deployment guide now because UI team needs a public API URL.

Use Oracle Cloud Always Free for testing.

Oracle Always Free currently includes:

- one standalone MySQL HeatWave DB system
- 50 GB database storage
- 50 GB backup storage
- two AMD Compute VMs
- up to four Arm Ampere A1 compute instances

Create:

docs/backend/oracle-cloud-free-deployment.md

Include these exact sections:

1. Target architecture

Backend:
Oracle Always Free Compute VM

Database:
Oracle MySQL HeatWave Always Free

Redis:
Redis installed on Compute VM for testing

Public API:
https://ems-api.<domain>/api/v1
or initially:
http://<oracle-public-ip>:3000/api/v1

2. OCI network

VCN:
10.0.0.0/16

Public subnet:
10.0.0.0/24
for backend compute

Private subnet:
10.0.1.0/24
for MySQL

Security rules:

- allow SSH 22 only from developer IP
- allow HTTP 80 from 0.0.0.0/0
- allow HTTPS 443 from 0.0.0.0/0
- allow app port 3000 temporarily only from developer/frontend testing IP
- allow MySQL 3306 only from public subnet CIDR or compute private IP
- never expose MySQL publicly

3. MySQL HeatWave setup

- create Always Free MySQL DB System
- choose private subnet
- storage 50 GB
- username: ems_admin
- save private IP and port 3306
- create database:

CREATE DATABASE ems CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

4. Compute VM setup

Install:

sudo apt update
sudo apt install -y git curl build-essential nginx redis-server

Install Node 20:

curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

Install PM2:

sudo npm install -g pm2

5. App deployment

git clone <repo-url> ems-backend
cd ems-backend
npm ci

Create .env:

NODE_ENV=production
PORT=3000
API_PREFIX=/api/v1
DATABASE_URL="mysql://ems_admin:<password>@<mysql-private-ip>:3306/ems?connection_limit=10&pool_timeout=20"
REDIS_URL="redis://127.0.0.1:6379"
JWT_SECRET="<strong-secret>"
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d
DEFAULT_TENANT_KEY=acme
CORS_ORIGIN="http://localhost:5173,https://your-frontend-domain.com"
SESSION_COOKIE_NAME=refreshToken
SESSION_MAX_AGE_DAYS=7
FRONTEND_RESET_PASSWORD_URL="https://your-frontend-domain.com/reset-password"
EMAIL_PROVIDER=smtp
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER="<brevo-login>"
SMTP_PASS="<brevo-smtp-key>"
SMTP_FROM="EMS <verified-sender@yourdomain.com>"

Run:

npx prisma migrate deploy
npm run db:seed
pm2 start src/server.js --name ems-backend
pm2 save
pm2 startup

6. Nginx

Create:

/etc/nginx/sites-available/ems-backend

server {
listen 80;
server_name ems-api.<domain>;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

}

Enable:

sudo ln -s /etc/nginx/sites-available/ems-backend /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

7. SSL

If domain is available:

sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d ems-api.<domain>

8. UI team handoff

Give UI team:

API Base URL:
https://ems-api.<domain>/api/v1

Swagger:
https://ems-api.<domain>/docs

Tenant header:
X-Tenant-Key: acme

Auth:

- access token in Authorization header
- refresh token via HttpOnly cookie

9. Troubleshooting

Include:

- PM2 logs
- Nginx logs
- Redis status
- DB connectivity test
- Prisma migration troubleshooting
- CORS troubleshooting
- email SMTP test command

Commit:

docs(deploy): add oracle cloud always free setup guide

==================================================
PART C — START PAGE 04: HR ADMIN DASHBOARD
==================================================

After Part A and B, start Page 04.

Wireframe:
04 Dashboard — HR Admin
Visible to:
HR_ADMIN, SUPER_ADMIN

Do not build manager dashboard, employee dashboard, employees CRUD, attendance CRUD, leave CRUD, or payroll yet.

Implement only dashboard analytics APIs.

Required APIs:

1. GET /api/v1/analytics/summary
2. GET /api/v1/analytics/attendance?range=7d|30d|90d
3. GET /api/v1/analytics/headcount-by-department
4. GET /api/v1/analytics/recent-activity?limit=10

Optional but useful: 5. GET /api/v1/analytics/leave-summary?range=30d

Access control:

- SUPER_ADMIN allowed
- HR_ADMIN allowed
- MANAGER denied with 403
- EMPLOYEE denied with 403

Permissions:

- require analytics:read

Tenant:

- every query must use request.tenant.id
- no cross-tenant leakage

Caching:

- summary cache TTL: 60 seconds
- attendance chart cache TTL: 60 seconds
- headcount-by-department TTL: 5 minutes
- recent activity TTL: no cache or max 30 seconds

Response 1:

GET /api/v1/analytics/summary

{
"success": true,
"data": {
"totalEmployees": 1240,
"activeEmployees": 1087,
"presentToday": 900,
"onLeaveToday": 84,
"openRequests": 23,
"departmentsCount": 8,
"newJoinersThisMonth": 12
},
"meta": {
"cached": true,
"generatedAt": "2026-05-17T..."
}
}

Response 2:

GET /api/v1/analytics/attendance?range=30d

{
"success": true,
"data": {
"range": "30d",
"series": [
{
"date": "2026-05-17",
"present": 10,
"absent": 1,
"leave": 2,
"wfh": 3,
"halfDay": 0
}
]
}
}

Response 3:

GET /api/v1/analytics/headcount-by-department

{
"success": true,
"data": [
{
"departmentId": "...",
"departmentName": "Engineering",
"employeeCount": 412,
"activeCount": 390
}
]
}

Response 4:

GET /api/v1/analytics/recent-activity?limit=10

{
"success": true,
"data": [
{
"id": "...",
"actorName": "Priya S.",
"action": "created",
"entityType": "Employee",
"entityId": "...",
"resourceLabel": "Employee #E2041",
"createdAt": "2026-05-17T...",
"createdAtIstDisplay": "17/05/2026 04:50:23 PM IST"
}
]
}

Required files:

src/modules/analytics/analytics.routes.js
src/modules/analytics/analytics.controller.js
src/modules/analytics/analytics.service.js
src/modules/analytics/analytics.repository.js
src/modules/analytics/analytics.policy.js
src/modules/analytics/analytics.validator.js

Register routes in app.js.

Database/index review:
Before implementing queries, verify indexes support:

- employee count by tenant/status/department
- attendance by tenant/date/status
- leave by tenant/status/date
- audit logs by tenant/createdAt/action/entity

If indexes are missing, add migration.

Tests:

Unit:

- summary calculation
- attendance range validation
- headcount aggregation
- recent activity formatting
- cache key generation
- permission policy

Integration:

- HR Admin can access summary
- Super Admin can access summary
- Employee gets 403
- Manager gets 403
- missing token gets 401
- summary uses tenantId
- attendance validates range
- headcount returns department counts
- recent activity returns IST display

E2E:

- HR Admin login -> analytics summary -> attendance chart -> recent activity

Postman:
Add folder:
Analytics - HR Admin Dashboard

Requests:

- Login HR Admin
- Summary
- Attendance 30d
- Headcount by Department
- Recent Activity
- Employee forbidden analytics

Swagger:
Add route-level schemas for all analytics endpoints.

Docs:
Update:
docs/api/ui-team-api-guide.md
docs/api/analytics-api.md
docs/backend/backend-design-document.md
docs/diagrams/system-architecture.md

Create:
docs/diagrams/hr-admin-dashboard-flow.md

Commit:

feat(analytics): implement hr admin dashboard apis

Final checks:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

Priority task: Configure real Brevo SMTP email delivery, finish Page 03 cleanup, then proceed to Page 04 HR Admin Dashboard.

Do everything in this order. Do not split into separate tasks.

==================================================
PART 1 — CONFIGURE BREVO SMTP EMAIL
==================================================

We now have Brevo SMTP access for testing.

SMTP provider:
Brevo

SMTP server:
smtp-relay.brevo.com

SMTP port:
587

SMTP login:
ab99cb001@smtp-brevo.com

SMTP key name:
EMS-key

Important:
The actual SMTP key will be provided securely through environment variables only.

Never commit the SMTP key.
Never print the SMTP key.
Never add the SMTP key to docs.
Never add the SMTP key to Postman.
Never add the SMTP key to seed files.
Never log the SMTP key.
Never expose the SMTP key in API response.
Never place the SMTP key in frontend code.

Update .env.example like this, but keep secrets blank:

EMAIL_PROVIDER=smtp
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=ab99cb001@smtp-brevo.com
SMTP_PASS=
SMTP_FROM="EMS <verified-sender@yourdomain.com>"

Update local .env only:

EMAIL_PROVIDER=smtp
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=ab99cb001@smtp-brevo.com
SMTP_PASS="<BREVO_SMTP_KEY_FROM_OWNER>"
SMTP_FROM="EMS <verified-sender@yourdomain.com>"

Also support this safely:

EMAIL_PROVIDER=mock

for local development without real email.

==================================================
PART 2 — ADD SAFE EMAIL TEST COMMAND
==================================================

Add a safe test email script:

scripts/sendTestEmail.js

Add package script:

"email:test": "node scripts/sendTestEmail.js"

Usage:

npm run email:test -- --to mohammadsaeedafri9@gmail.com

The test email must send to:

mohammadsaeedafri9@gmail.com

Subject:

EMS Email Test

Body:

This is a safe test email from EMS backend.

Rules:

- Do not include OTP.
- Do not include reset token.
- Do not include password.
- Do not include cookies.
- Do not include authorization headers.
- Do not include SMTP key.
- Log only safe success/failure message.

Expected safe console/logger output:

Email test sent successfully to m******\*\*\*******9@gmail.com

or

Email test failed. Check SMTP configuration.

Do not print SMTP credentials.

==================================================
PART 3 — TEST PASSWORD RESET EMAIL
==================================================

Ensure there is a DB user with:

User.email = mohammadsaeedafri9@gmail.com

For testing/staging only, add a safe seed helper:

npm run seed:test-email-user -- --email mohammadsaeedafri9@gmail.com

This script should:

- use DEFAULT_TENANT_KEY
- create or update Employee.workEmail = mohammadsaeedafri9@gmail.com
- create or update User.email = mohammadsaeedafri9@gmail.com
- set user status ACTIVE
- optionally set mfaEnabled=true for OTP testing
- assign EMPLOYEE role if no role exists
- never print generated password
- never print sensitive values

Important data rule:
Password reset and OTP must always send to User.email from the database.

Do not accept OTP destination email from API request.
Do not accept password reset destination email except for forgot-password lookup.
Do not send OTP to arbitrary request email.
Do not send password reset to Employee.personalEmail unless business explicitly approves later.

Future onboarding rule:
When HR onboards an employee:

- HR enters Employee.workEmail.
- If login access is enabled, User.email must be created from Employee.workEmail.
- Employee.personalEmail is optional.
- User.email is the source of truth for login, password reset, and OTP.
- User.email and Employee.workEmail must stay synced during employee update flow.

Document this in:
docs/api/auth-api.md
docs/api/ui-team-api-guide.md
docs/backend/auth-session-internals.md
docs/backend/backend-design-document.md

Test password reset email:

POST /api/v1/auth/forgot-password

Headers:
X-Tenant-Key: acme
Content-Type: application/json

Body:
{
"email": "mohammadsaeedafri9@gmail.com"
}

Expected:

- API returns 202.
- Email arrives at mohammadsaeedafri9@gmail.com.
- API response does not include reset token.
- Logs do not include reset token.
- Logs do not include full reset URL.
- DB stores only hashed reset token.

==================================================
PART 4 — TEST OTP EMAIL
==================================================

Make sure test user has:

User.email = mohammadsaeedafri9@gmail.com
User.mfaEnabled = true
User.status = ACTIVE

Call:

POST /api/v1/auth/login

Headers:
X-Tenant-Key: acme
Content-Type: application/json

Body:
{
"email": "mohammadsaeedafri9@gmail.com",
"password": "<test-user-password>"
}

Expected:

- API returns 202.
- Response contains mfaRequired=true.
- Response contains challengeId.
- Response contains destinationMasked.
- Response does not contain accessToken before OTP verification.
- OTP email arrives at mohammadsaeedafri9@gmail.com.
- Raw OTP is not stored in DB.
- Raw OTP is not logged.
- OTP verification completes login.

Then test:

POST /api/v1/auth/verify-otp

Headers:
X-Tenant-Key: acme
Content-Type: application/json

Body:
{
"challengeId": "<challengeId>",
"code": "<otp-from-email>"
}

Expected:

- API returns accessToken.
- API sets refresh cookie.
- API returns user and permissions.
- OTP challenge is marked consumed.

==================================================
PART 5 — FINISH PAGE 03 CLEANUP
==================================================

Make sure these are complete:

1. Admin MFA login

POST /api/v1/auth/admin/login

If HR_ADMIN or SUPER_ADMIN has mfaEnabled=true:

- validate email/password
- validate admin role
- create OTP challenge
- send OTP to User.email from DB
- return 202 with mfaRequired=true and challengeId
- do not create session until OTP is verified

2. Resend OTP must not require access token

POST /api/v1/auth/resend-otp

Request:
{
"challengeId": "string"
}

Rules:

- tenant-scoped
- no access token required
- 60-second cooldown
- max 3 resends
- never reveal full email
- never log OTP

3. Use secure OTP

OTP generation:
crypto.randomInt(100000, 1000000).toString()

Challenge ID:
crypto.randomUUID()

4. Standard audit events

Use:

OTP_CHALLENGE_CREATED
OTP_VERIFICATION_SUCCEEDED
OTP_VERIFICATION_FAILED
OTP_RESENT
OTP_RESEND_BLOCKED
OTP_EXPIRED
OTP_LOCKED
OTP_EMAIL_QUEUED
OTP_EMAIL_FAILED

5. No failing tests

Do not say failures are unrelated.

Final result must be:
0 failing tests

==================================================
PART 6 — FINAL POSTMAN COLLECTION
==================================================

Use only:

docs/postman/EMS-API.postman_collection.json

Add or verify these requests:

Auth:

- Employee Login
- Admin Login
- Logout
- Logout All
- Refresh
- Me
- Sessions
- Revoke Session

Password Reset:

- Forgot Password
- Validate Reset Token
- Reset Password
- Invalid Reset Token
- Weak Password

OTP:

- MFA Login
- Admin MFA Login
- Verify OTP
- Verify Invalid OTP
- Resend OTP
- Resend OTP Cooldown
- Resend OTP Limit
- Wrong Tenant OTP Failure

Email:

- Safe Email Test documentation only, not SMTP key

Run:

newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

Expected:
0 failed assertions

==================================================
PART 7 — ORACLE CLOUD ALWAYS FREE DEPLOYMENT GUIDE
==================================================

Create:

docs/backend/oracle-cloud-free-deployment.md

This is needed so UI team can access a public API URL.

Include this architecture:

Backend:
Oracle Always Free Compute VM

Database:
Oracle MySQL HeatWave Always Free

Redis:
Redis installed on Compute VM for testing

Public API:
https://ems-api.<domain>/api/v1

Initial testing:
http://<oracle-public-ip>:3000/api/v1

Include:

1. VCN setup

VCN:
10.0.0.0/16

Public subnet:
10.0.0.0/24

Private subnet:
10.0.1.0/24

2. Security rules

Allow:

- SSH 22 only from developer IP
- HTTP 80 from public
- HTTPS 443 from public
- app port 3000 only temporarily for testing
- MySQL 3306 only from backend compute private IP or public subnet CIDR

Never expose MySQL publicly.

3. MySQL HeatWave setup

Create Always Free MySQL DB System in private subnet.

Create database:

CREATE DATABASE ems CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

4. Compute VM setup

Install:

sudo apt update
sudo apt install -y git curl build-essential nginx redis-server

Install Node.js 20:

curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

Install PM2:

sudo npm install -g pm2

5. Backend deployment

git clone <repo-url> ems-backend
cd ems-backend
npm ci
npx prisma migrate deploy
npm run db:seed
pm2 start src/server.js --name ems-backend
pm2 save
pm2 startup

6. Production .env example

Do not include real secrets.

NODE_ENV=production
PORT=3000
API_PREFIX=/api/v1
DATABASE_URL="mysql://ems_admin:<password>@<mysql-private-ip>:3306/ems?connection_limit=10&pool_timeout=20"
REDIS_URL="redis://127.0.0.1:6379"
JWT_SECRET="<strong-secret>"
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d
DEFAULT_TENANT_KEY=acme
CORS_ORIGIN="https://your-frontend-domain.com"
SESSION_COOKIE_NAME=refreshToken
SESSION_MAX_AGE_DAYS=7
FRONTEND_RESET_PASSWORD_URL="https://your-frontend-domain.com/reset-password"
EMAIL_PROVIDER=smtp
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=ab99cb001@smtp-brevo.com
SMTP_PASS="<brevo-smtp-key>"
SMTP_FROM="EMS <verified-sender@yourdomain.com>"

7. Nginx reverse proxy

server {
listen 80;
server_name ems-api.<domain>;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

}

8. SSL

Use Certbot:

sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d ems-api.<domain>

9. UI team handoff

API Base URL:
https://ems-api.<domain>/api/v1

Swagger:
https://ems-api.<domain>/docs

Required header:
X-Tenant-Key: acme

Auth:
Authorization: Bearer <accessToken>

Refresh:
HttpOnly cookie, frontend does not read it directly.

10. Troubleshooting

Include:

- PM2 logs
- Nginx logs
- Redis status
- MySQL connectivity
- Prisma migration issues
- CORS issues
- Brevo SMTP email testing
- how to rotate SMTP key

Commit:

docs(deploy): add oracle cloud always free setup guide

==================================================
PART 8 — START PAGE 04 HR ADMIN DASHBOARD
==================================================

After the email test, OTP cleanup, Postman cleanup, and Oracle deployment guide, start Page 04.

Wireframe:
Page 04 Dashboard — HR Admin

Allowed:
SUPER_ADMIN
HR_ADMIN

Denied:
MANAGER
EMPLOYEE

Permission:
analytics:read

Do not build:

- Manager dashboard
- Employee dashboard
- Employee CRUD
- Leave CRUD
- Attendance CRUD
- Payroll

Build only HR Admin dashboard analytics APIs.

Required APIs:

1. GET /api/v1/analytics/summary
2. GET /api/v1/analytics/attendance?range=7d|30d|90d
3. GET /api/v1/analytics/headcount-by-department
4. GET /api/v1/analytics/recent-activity?limit=10
5. GET /api/v1/analytics/leave-summary?range=30d

Response:

GET /api/v1/analytics/summary

{
"success": true,
"data": {
"totalEmployees": 1240,
"activeEmployees": 1087,
"presentToday": 900,
"onLeaveToday": 84,
"openRequests": 23,
"departmentsCount": 8,
"newJoinersThisMonth": 12
},
"meta": {
"cached": true,
"generatedAt": "2026-05-17T..."
}
}

GET /api/v1/analytics/attendance?range=30d

{
"success": true,
"data": {
"range": "30d",
"series": [
{
"date": "2026-05-17",
"present": 10,
"absent": 1,
"leave": 2,
"wfh": 3,
"halfDay": 0
}
]
}
}

GET /api/v1/analytics/headcount-by-department

{
"success": true,
"data": [
{
"departmentId": "...",
"departmentName": "Engineering",
"employeeCount": 412,
"activeCount": 390
}
]
}

GET /api/v1/analytics/recent-activity?limit=10

{
"success": true,
"data": [
{
"id": "...",
"actorName": "Priya S.",
"action": "created",
"entityType": "Employee",
"entityId": "...",
"resourceLabel": "Employee #E2041",
"createdAt": "2026-05-17T...",
"createdAtIstDisplay": "17/05/2026 04:50:23 PM IST"
}
]
}

Technical rules:

- Use tenantId in every query.
- Use Redis cache.
- summary TTL: 60 seconds
- attendance TTL: 60 seconds
- headcount TTL: 5 minutes
- recent activity TTL: 30 seconds max
- Use efficient Prisma queries.
- Avoid N+1 queries.
- Add/verify DB indexes.
- Add Swagger route schemas.
- Add Postman requests.
- Add UI API docs.
- Add backend docs.
- Add tests.

Files:

src/modules/analytics/analytics.routes.js
src/modules/analytics/analytics.controller.js
src/modules/analytics/analytics.service.js
src/modules/analytics/analytics.repository.js
src/modules/analytics/analytics.policy.js
src/modules/analytics/analytics.validator.js

Tests:

Unit:

- summary calculation
- attendance range validation
- headcount aggregation
- recent activity formatting
- cache key generation
- permission policy

Integration:

- HR Admin can access
- Super Admin can access
- Manager gets 403
- Employee gets 403
- missing token gets 401
- tenant isolation
- invalid range gets 400

E2E:

- HR Admin login -> summary -> attendance -> headcount -> recent activity

Postman:
Add folder:

Analytics - HR Admin Dashboard

Requests:

- HR Admin Login
- Summary
- Attendance 30d
- Headcount by Department
- Recent Activity
- Leave Summary
- Employee Forbidden Analytics

Docs:
Update:

docs/api/ui-team-api-guide.md
docs/api/analytics-api.md
docs/backend/backend-design-document.md
docs/diagrams/system-architecture.md

Create:

docs/diagrams/hr-admin-dashboard-flow.md

Commit:

feat(analytics): implement hr admin dashboard apis

==================================================
FINAL CHECKS
==================================================

Before pushing, run:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json
npm run email:test -- --to mohammadsaeedafri9@gmail.com

Final acceptance:

- 0 lint errors
- 0 failing tests
- 0 Newman failures
- safe test email received by mohammadsaeedafri9@gmail.com
- OTP email received by mohammadsaeedafri9@gmail.com
- password reset email received by mohammadsaeedafri9@gmail.com
- no raw OTP/token/password in logs
- User.email is DB source of truth for OTP and password reset
- Oracle Cloud deployment guide created
- Page 04 APIs implemented and documented

This is the SMTP key - [REDACTED_SMTP_KEY] key name - EMS-key Your SMTP Settings SMTP Server smtp-relay.brevo.com Port 587 Login ab99cb001@smtp-brevo.com

Priority: Fix Brevo email testing immediately, then continue Page 04 HR Admin Dashboard.

Do everything in this exact order.

==================================================
PART 1 — BREVO SMTP IP AUTHORIZATION
==================================================

Brevo is blocking unauthorized IP addresses for SMTP/API calls.

Before testing email, find the public IPv4 of the machine that will send email.

Run this on the same machine where the backend will run:

curl -4 https://api.ipify.org

Take the returned IP and tell the owner to add it in Brevo as:

<public-ip>/32

Example:

49.37.12.100/32

Important:

- Use CIDR format because Brevo requires IPv4 range/CIDR.
- For one exact IP, use /32.
- If testing from local machine, authorize local public IP.
- If testing from Oracle Cloud VM, authorize Oracle VM public IP.
- If using VPN, either turn VPN off or authorize the VPN public IP.
- If the IP appears in Brevo Unauthorized IP addresses, remove/unblock it or authorize it correctly.
- Do not expose MySQL or backend secrets while doing this.

==================================================
PART 2 — BREVO SMTP CONFIGURATION
==================================================

Brevo SMTP password is the SMTP key value.

Do not use Brevo account password.
Do not use API key for SMTP password.
Use SMTP key as SMTP_PASS.

SMTP settings:

EMAIL_PROVIDER=smtp
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=ab99cb001@smtp-brevo.com
SMTP_PASS=<BREVO_SMTP_KEY_FROM_OWNER>
SMTP_FROM="EMS <verified-sender@yourdomain.com>"

Important:

- The SMTP key must only go in local .env or server environment variables.
- Never commit SMTP key.
- Never print SMTP key.
- Never add SMTP key to docs.
- Never add SMTP key to Postman.
- Never log SMTP key.
- Never expose SMTP key in API response.
- Never put SMTP key in frontend code.

Update .env.example with empty secret:

EMAIL_PROVIDER=smtp
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=ab99cb001@smtp-brevo.com
SMTP_PASS=
SMTP_FROM="EMS <verified-sender@yourdomain.com>"

Also support mock provider:

EMAIL_PROVIDER=mock

==================================================
PART 3 — VERIFY BREVO SENDER
==================================================

Before sending email, make sure SMTP_FROM uses a sender that is verified in Brevo.

If no custom domain is verified yet, use a sender email verified inside Brevo.

Do not use random sender emails.

Check:

- Brevo sender is verified
- SMTP key is active
- IP is authorized
- SMTP_FROM domain/sender is allowed
- Gmail spam/promotions folder after sending

==================================================
PART 4 — ADD SAFE EMAIL TEST SCRIPT
==================================================

Add script:

scripts/sendTestEmail.js

Add package script:

"email:test": "node scripts/sendTestEmail.js"

Command:

npm run email:test -- --to mohammadsaeedafri9@gmail.com

Email:

To:
mohammadsaeedafri9@gmail.com

Subject:
EMS Email Test

Body:
This is a safe test email from EMS backend.

Rules:

- Do not include OTP.
- Do not include reset token.
- Do not include password.
- Do not include cookies.
- Do not include authorization headers.
- Do not include SMTP key.
- Log only masked recipient.

Safe success log:

Email test sent successfully to m********\*********9@gmail.com

Safe failure log:

Email test failed. Check SMTP configuration, sender verification, and Brevo authorized IP.

Do not print SMTP credentials.

==================================================
PART 5 — TEST PASSWORD RESET EMAIL
==================================================

Ensure DB has a user:

User.email = mohammadsaeedafri9@gmail.com
User.status = ACTIVE

Also ensure employee record is consistent:

Employee.workEmail = mohammadsaeedafri9@gmail.com

Create helper if missing:

npm run seed:test-email-user -- --email mohammadsaeedafri9@gmail.com

Rules:

- User.email is the source of truth for password reset and OTP.
- Employee.workEmail is entered by HR during onboarding.
- When login account is created, User.email must come from Employee.workEmail.
- Employee.personalEmail is optional and must not be used for OTP/password reset unless business approves later.
- Backend must never accept arbitrary OTP destination email from request body.

Test password reset:

POST /api/v1/auth/forgot-password

Headers:
X-Tenant-Key: acme
Content-Type: application/json

Body:
{
"email": "mohammadsaeedafri9@gmail.com"
}

Expected:

- API returns 202.
- Email arrives at mohammadsaeedafri9@gmail.com.
- API response does not include reset token.
- Logs do not include reset token.
- Logs do not include full reset URL.
- DB stores only hashed reset token.

==================================================
PART 6 — TEST OTP EMAIL
==================================================

Ensure test user has:

User.email = mohammadsaeedafri9@gmail.com
User.mfaEnabled = true
User.status = ACTIVE

Call:

POST /api/v1/auth/login

Headers:
X-Tenant-Key: acme
Content-Type: application/json

Body:
{
"email": "mohammadsaeedafri9@gmail.com",
"password": "<test-user-password>"
}

Expected:

- API returns 202.
- Response contains mfaRequired=true.
- Response contains challengeId.
- Response contains destinationMasked.
- Response does not contain accessToken before OTP verification.
- OTP email arrives at mohammadsaeedafri9@gmail.com.
- Raw OTP is not stored in DB.
- Raw OTP is not logged.

Then verify OTP:

POST /api/v1/auth/verify-otp

Headers:
X-Tenant-Key: acme
Content-Type: application/json

Body:
{
"challengeId": "<challengeId>",
"code": "<otp-from-email>"
}

Expected:

- API returns accessToken.
- API sets refresh cookie.
- API returns user and permissions.
- OTP challenge is marked consumed.

==================================================
PART 7 — IF EMAIL DOES NOT ARRIVE
==================================================

Check these in order:

1. Brevo authorized IP list has the backend sender IP as /32.
2. Brevo unauthorized IP list does not still block the sender IP.
3. SMTP key is active.
4. SMTP_FROM is a verified sender in Brevo.
5. SMTP_USER is ab99cb001@smtp-brevo.com.
6. SMTP_PASS is the SMTP key, not Brevo login password.
7. SMTP_HOST is smtp-relay.brevo.com.
8. SMTP_PORT is 587.
9. Check Gmail Spam, Promotions, Updates.
10. Check Brevo transactional email logs.
11. Check backend logs, but do not print secrets.

Add troubleshooting notes to:

docs/backend/email-provider-setup.md

==================================================
PART 8 — PAGE 03 FINAL CHECKS
==================================================

Make sure these are complete:

- normal MFA login works
- admin MFA login works
- resend OTP does not require access token
- resend OTP enforces 60-second cooldown
- max 3 resends enforced
- OTP uses crypto.randomInt
- challengeId uses crypto.randomUUID
- OTP sends only to User.email from DB
- raw OTP never stored
- raw OTP never logged
- password reset sends only to User.email from DB
- raw reset token never stored
- raw reset token never logged

Run:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json
npm run email:test -- --to mohammadsaeedafri9@gmail.com

Acceptance:

- 0 lint errors
- 0 failing tests
- 0 Newman failures
- safe email received
- password reset email received
- OTP email received
- no secret/token/OTP/password in logs

Commit:

fix(email): configure brevo smtp testing and verify transactional delivery

==================================================
PART 9 — THEN CONTINUE PAGE 04 HR ADMIN DASHBOARD
==================================================

After email is verified, continue Page 04 HR Admin Dashboard.

Build only these APIs:

1. GET /api/v1/analytics/summary
2. GET /api/v1/analytics/attendance?range=7d|30d|90d
3. GET /api/v1/analytics/headcount-by-department
4. GET /api/v1/analytics/recent-activity?limit=10
5. GET /api/v1/analytics/leave-summary?range=30d

Access:

- SUPER_ADMIN allowed
- HR_ADMIN allowed
- MANAGER denied 403
- EMPLOYEE denied 403

Permission:
analytics:read

Rules:

- tenantId in every query
- Redis cache
- summary TTL 60 seconds
- attendance TTL 60 seconds
- headcount TTL 5 minutes
- recent activity TTL 30 seconds max
- efficient Prisma queries
- no N+1
- Swagger schemas
- Postman requests
- UI docs
- tests

Commit:

feat(analytics): implement hr admin dashboard apis

Review result: Page 04 is NOT accepted yet. Page 03/email needs final proof, then Page 04 must be corrected according to the wireframe.

Do not move to Page 05 yet.

Important:
Always check WIREFRAMES.pdf before implementing any page. If confused, open the exact wireframe page and map every visible card/table/chart/button to backend APIs before coding.

==================================================
PART 1 — FINALIZE PAGE 03 EMAIL + OTP PROOF
==================================================

Before Page 04 is accepted, prove email and OTP work end-to-end.

1. Brevo SMTP test

Run:

curl -4 https://api.ipify.org

Tell the owner the returned IP so it can be authorized in Brevo as:

<public-ip>/32

Then run:

npm run email:test -- --to mohammadsaeedafri9@gmail.com

Expected:

- safe email is received at mohammadsaeedafri9@gmail.com
- output masks the email
- SMTP key is never printed
- SMTP password is never logged
- no OTP or reset token is included in the safe test email

2. Password reset email test

Ensure DB has:

User.email = mohammadsaeedafri9@gmail.com
Employee.workEmail = mohammadsaeedafri9@gmail.com

Then call:

POST /api/v1/auth/forgot-password

Headers:
X-Tenant-Key: acme
Content-Type: application/json

Body:
{
"email": "mohammadsaeedafri9@gmail.com"
}

Expected:

- API returns 202
- password reset email arrives
- API does not return reset token
- logs do not contain reset token
- DB stores only hashed token

3. OTP email test

Ensure:

User.email = mohammadsaeedafri9@gmail.com
User.mfaEnabled = true
User.status = ACTIVE

Call:

POST /api/v1/auth/login

Expected:

- 202
- mfaRequired=true
- challengeId returned
- no accessToken before OTP verification
- OTP email arrives at mohammadsaeedafri9@gmail.com

Then call:

POST /api/v1/auth/verify-otp

Expected:

- accessToken returned
- refresh cookie set
- user returned
- permissions returned
- OTP challenge consumed

4. Admin MFA must also work

Verify:

POST /api/v1/auth/admin/login

If HR_ADMIN or SUPER_ADMIN has mfaEnabled=true:

- return 202
- mfaRequired=true
- challengeId returned
- no session before OTP
- verify OTP completes admin login

If current admin login bypasses MFA, fix it.

5. Email source-of-truth rule

Document and enforce:

- OTP sends only to User.email from DB.
- Password reset sends only to User.email from DB.
- Backend must not accept arbitrary OTP destination email.
- Employee.personalEmail is not used for auth emails unless business approves later.
- During future HR onboarding, HR fills Employee.workEmail.
- When login access is created, User.email must be created from Employee.workEmail.
- User.email and Employee.workEmail must stay synced during employee update flow.

Update:

docs/api/auth-api.md
docs/api/ui-team-api-guide.md
docs/backend/auth-session-internals.md
docs/backend/backend-design-document.md

6. Fix runtime console logging

src/jobs/emailJob.js still has console.log in runtime mock mode.

Replace runtime console.\* with structured logger.

Scripts may print safe local CLI output, but runtime src code should not use console.log/console.error.

7. Fix seed test user output

scripts/seedTestEmailUser.js currently prints the test password.

Change it so:

- it never prints password by default
- it prints only masked email and safe status
- if a password is generated, write it only when --show-password-local is explicitly passed and NODE_ENV is not production

8. Run checks

Run and share:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json
npm run email:test -- --to mohammadsaeedafri9@gmail.com

Acceptance:

- 0 lint errors
- 0 failing tests
- 0 Newman failures
- real safe email received
- real password reset email received
- real OTP email received
- normal MFA works
- admin MFA works
- no token/OTP/password/SMTP key logged

Commit:

fix(auth): finalize otp mfa and brevo email verification

==================================================
PART 2 — REBUILD PAGE 04 ACCORDING TO WIREFRAME
==================================================

Page 04 wireframe:
Dashboard — HR Admin
Visible to:
HR_ADMIN, SUPER_ADMIN

Do not build:

- Payroll analytics
- Manager dashboard
- Employee dashboard
- Employee CRUD
- Attendance CRUD
- Leave CRUD
- Department CRUD

Payroll is later. Remove or disable /analytics/payroll for now.

Wireframe data sources are:

GET /api/v1/analytics/summary
GET /api/v1/analytics/attendance?range=30d
GET /api/v1/analytics/headcount-by-department
GET /api/v1/audit-logs?limit=10

Also add optional:
GET /api/v1/analytics/leave-summary?range=30d

Required final Page 04 APIs:

1. GET /api/v1/analytics/summary
2. GET /api/v1/analytics/attendance?range=7d|30d|90d
3. GET /api/v1/analytics/headcount-by-department
4. GET /api/v1/analytics/recent-activity?limit=10
5. GET /api/v1/analytics/leave-summary?range=30d

Do not use these as final names:

- /analytics/dashboard-summary
- /analytics/leave
- /analytics/payroll
- /analytics/department/:departmentId

If you want backward compatibility, you may keep old aliases temporarily, but the wireframe/UI team must use the final names above.

==================================================
PART 3 — REGISTER ANALYTICS ROUTES
==================================================

Analytics routes currently appear not registered in app.js.

Update app.js:

import analyticsRoutes from './modules/analytics/analytics.routes.js';

Then register inside /api/v1 tenant-scoped route group:

fastify.register(analyticsRoutes);

Verify all analytics routes are reachable.

==================================================
PART 4 — FIX ANALYTICS DATA QUERIES
==================================================

Do not use placeholders.

Use actual Prisma models from schema:

Employee
Department
AttendanceRecord
LeaveRequest
AuditLog

Do not call non-existing models like prisma.leave.

Do not use non-existing fields like employee.department. Use:

employee.departmentId
employee.department relation
Department.name

API 1:

GET /api/v1/analytics/summary

Return:

{
"success": true,
"data": {
"totalEmployees": 1240,
"activeEmployees": 1087,
"presentToday": 900,
"onLeaveToday": 84,
"openRequests": 23,
"departmentsCount": 8,
"newJoinersThisMonth": 12
},
"meta": {
"cached": true,
"generatedAt": "2026-05-17T..."
}
}

Data mapping:

totalEmployees:
Employee count where tenantId and deletedAt=null

activeEmployees:
Employee count where tenantId, deletedAt=null, employmentStatus=ACTIVE

presentToday:
AttendanceRecord count where tenantId, attendanceDate=today, status=PRESENT

onLeaveToday:
LeaveRequest count where tenantId, status=APPROVED, startDate<=today, endDate>=today

openRequests:
LeaveRequest count where tenantId, status=PENDING
plus AttendanceRegularizationRequest count where tenantId, status=PENDING if model exists

departmentsCount:
Department count where tenantId and deletedAt=null

newJoinersThisMonth:
Employee count where tenantId, joinedOn between startOfMonth and now, deletedAt=null

API 2:

GET /api/v1/analytics/attendance?range=7d|30d|90d

Return:

{
"success": true,
"data": {
"range": "30d",
"series": [
{
"date": "2026-05-17",
"present": 10,
"absent": 1,
"leave": 2,
"wfh": 3,
"halfDay": 0
}
]
}
}

Use AttendanceRecord grouped by attendanceDate and status.

Validate range:

- allowed: 7d, 30d, 90d
- default: 30d
- invalid returns 400

API 3:

GET /api/v1/analytics/headcount-by-department

Return:

{
"success": true,
"data": [
{
"departmentId": "...",
"departmentName": "Engineering",
"employeeCount": 412,
"activeCount": 390
}
]
}

Use Department + Employee counts by departmentId.
Avoid N+1. Use groupBy and map departments.

API 4:

GET /api/v1/analytics/recent-activity?limit=10

Return:

{
"success": true,
"data": [
{
"id": "...",
"actorName": "Priya S.",
"action": "created",
"entityType": "Employee",
"entityId": "...",
"resourceLabel": "Employee #E2041",
"createdAt": "2026-05-17T...",
"createdAtIstDisplay": "17/05/2026 04:50:23 PM IST"
}
]
}

Use AuditLog.

Limit:

- default 10
- max 50

API 5:

GET /api/v1/analytics/leave-summary?range=30d

Return:

{
"success": true,
"data": {
"pending": 0,
"approved": 0,
"rejected": 0,
"withdrawn": 0
}
}

Use LeaveRequest grouped by status.

==================================================
PART 5 — ACCESS CONTROL
==================================================

Analytics Page 04 is only for:

SUPER_ADMIN
HR_ADMIN

Denied:
MANAGER
EMPLOYEE

Permission:
analytics:read

Use existing RBAC/permission system. Do not only check memberType if permission middleware exists.

Every analytics endpoint must check:

- authenticated user
- tenant context
- analytics:read permission
- HR_ADMIN or SUPER_ADMIN memberType

Tests must prove:

- HR_ADMIN allowed
- SUPER_ADMIN allowed
- MANAGER denied 403
- EMPLOYEE denied 403
- missing token returns 401
- wrong tenant denied

==================================================
PART 6 — CACHE RULES
==================================================

Use Redis cache.

Correct TTLs:

summary: 60 seconds
attendance: 60 seconds
headcount-by-department: 5 minutes
recent-activity: 30 seconds max
leave-summary: 60 seconds

Do not use 1 hour or 2 hour cache for dashboard cards. Dashboard should feel fresh.

Cache key must include:
tenantId
endpoint name
query params
user scope if needed

Return meta:

{
"cached": true,
"generatedAt": "..."
}

==================================================
PART 7 — DOCUMENT TABLES USED BY EVERY API
==================================================

For every API, document exactly which DB tables/models are used.

Create/update:

docs/api/analytics-api.md
docs/api/ui-team-api-guide.md
docs/backend/database-design.md
docs/backend/backend-design-document.md
docs/diagrams/hr-admin-dashboard-flow.md

For each endpoint include:

- endpoint name
- method
- URL
- visible wireframe widget/card
- allowed roles
- permission required
- request headers
- query params
- response body
- error responses
- tables/models read
- tables/models written, if any
- indexes used
- cache key
- cache TTL
- frontend usage notes
- Postman request name

Example format:

Endpoint:
GET /api/v1/analytics/summary

Wireframe widget:
Page 04 stats cards:
Total Employees, Active Today, On Leave, Open Requests

Tables read:

- Employee
- AttendanceRecord
- LeaveRequest
- AttendanceRegularizationRequest
- Department

Tables written:

- none

Indexes used:

- Employee: tenantId + employmentStatus + deletedAt
- AttendanceRecord: tenantId + attendanceDate + status
- LeaveRequest: tenantId + status + startDate + endDate
- Department: tenantId + deletedAt

Cache:
analytics:summary:{tenantId}
TTL: 60 seconds

==================================================
PART 8 — TESTS FOR PAGE 04
==================================================

Add real analytics tests.

Do not claim Page 04 complete without tests.

Unit tests:

- summary calculation
- attendance range validation
- attendance series format
- headcount aggregation
- recent activity IST formatting
- leave summary status grouping
- cache key generation
- RBAC policy

Integration tests:

- GET /analytics/summary HR_ADMIN success
- GET /analytics/summary SUPER_ADMIN success
- GET /analytics/summary EMPLOYEE 403
- GET /analytics/summary MANAGER 403
- GET /analytics/summary missing token 401
- GET /analytics/attendance?range=30d success
- GET /analytics/attendance?range=invalid returns 400
- GET /analytics/headcount-by-department success
- GET /analytics/recent-activity?limit=10 success
- GET /analytics/leave-summary?range=30d success
- tenant isolation test

E2E:

- HR Admin login -> analytics summary -> attendance -> headcount -> recent activity

Postman:
Add folder:

Analytics - HR Admin Dashboard

Requests:

- HR Admin Login
- Summary
- Attendance 30d
- Headcount by Department
- Recent Activity
- Leave Summary
- Employee Forbidden Analytics
- Manager Forbidden Analytics
- Invalid Range Analytics

Newman must pass with 0 failures.

==================================================
PART 9 — WIREFRAME DISCIPLINE FOR ALL FUTURE PAGES
==================================================

Before coding any page:

1. Open WIREFRAMES.pdf.
2. Identify page number and role:
   - HR Admin
   - Manager
   - Employee
   - Public
3. List every card/table/chart/button visible.
4. Map each UI element to one API.
5. Define request/response payload.
6. Define roles and permissions.
7. Define tables/models touched.
8. Define indexes needed.
9. Define Postman requests.
10. Define tests.
11. Only then code.

If a page has different behavior for HR, Manager, and Employee, backend must enforce it. Do not rely on frontend hiding UI.

==================================================
PART 10 — FINAL CHECKS
==================================================

Run:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json
npm run email:test -- --to mohammadsaeedafri9@gmail.com

Acceptance:

- 0 lint errors
- 0 failing tests
- 0 Newman failures
- analytics routes reachable
- Page 04 endpoints match wireframe
- no placeholder analytics data
- payroll analytics not included in Page 04
- UI docs include tables/models per API
- Postman includes Page 04 requests
- safe email test works
- OTP and password reset emails work
- User.email remains source of truth for auth emails

Commit:

fix(analytics): align hr admin dashboard apis with wireframe

Review result: Page 04 is NOT accepted yet.

You are close structurally, but the analytics APIs still do not match the HR Admin Dashboard wireframe and the analytics tests are not proper.

Do not move to Page 05 yet.

Complete the following fast correction sprint.

==================================================
PART 1 — FINAL EMAIL PROOF
==================================================

Before claiming email is done, provide proof.

Run:

curl -4 https://api.ipify.org

Make sure that IP is authorized in Brevo as:

<public-ip>/32

Then run:

npm run email:test -- --to mohammadsaeedafri9@gmail.com

Then test real password reset email:

POST /api/v1/auth/forgot-password

Headers:
X-Tenant-Key: acme
Content-Type: application/json

Body:
{
"email": "mohammadsaeedafri9@gmail.com"
}

Then test real OTP email:

POST /api/v1/auth/login

using the test user:

User.email = mohammadsaeedafri9@gmail.com
User.mfaEnabled = true

Acceptance:

- safe test email received
- password reset email received
- OTP email received
- no SMTP key printed
- no OTP printed
- no reset token printed
- no password printed

Also update scripts/seedTestEmailUser.js:

- do not print full email by default
- mask the email in output
- do not print password by default
- only print password with --show-password-local and NODE_ENV !== production

==================================================
PART 2 — FIX PAGE 04 API CONTRACT TO MATCH WIREFRAME
==================================================

Open WIREFRAMES.pdf and use Page 04: Dashboard — HR Admin.

The Page 04 wireframe data sources are:

GET /api/v1/analytics/summary
GET /api/v1/analytics/attendance?range=30d
GET /api/v1/analytics/headcount-by-department
GET /api/v1/audit-logs?limit=10

We will keep recent activity under analytics too:

GET /api/v1/analytics/recent-activity?limit=10

Optional:
GET /api/v1/analytics/leave-summary?range=30d

Final required APIs:

1. GET /api/v1/analytics/summary
2. GET /api/v1/analytics/attendance?range=7d|30d|90d
3. GET /api/v1/analytics/headcount-by-department
4. GET /api/v1/analytics/recent-activity?limit=10
5. GET /api/v1/analytics/leave-summary?range=30d

Remove or replace any old docs mentioning:

- /analytics/dashboard-summary
- /analytics/payroll
- /analytics/department/:id

Payroll is not part of Page 04.

==================================================
PART 3 — FIX SUMMARY RESPONSE
==================================================

Current summary is missing required dashboard card fields.

Implement:

GET /api/v1/analytics/summary

Response:

{
"success": true,
"data": {
"totalEmployees": 1240,
"activeToday": 1087,
"onLeaveToday": 84,
"openRequests": 23
},
"meta": {
"cached": true,
"generatedAt": "2026-05-17T..."
}
}

Data mapping:

totalEmployees:
Employee count where tenantId and deletedAt=null

activeToday:
AttendanceRecord count where tenantId, attendanceDate=today, status=PRESENT

onLeaveToday:
LeaveRequest count where tenantId, status=APPROVED, startDate<=today, endDate>=today

openRequests:
LeaveRequest count where tenantId, status=PENDING
plus AttendanceRegularizationRequest count where tenantId, status=PENDING if model exists

Do not return inactiveEmployees as a main dashboard card unless UI asks for it later.

Tables read:

- Employee
- AttendanceRecord
- LeaveRequest
- AttendanceRegularizationRequest
- Department only if needed later

Tables written:

- none

==================================================
PART 4 — FIX ATTENDANCE RESPONSE
==================================================

Wireframe chart is “Attendance — last 30 days”.

Current response is byDepartment rate. Replace it with date series.

GET /api/v1/analytics/attendance?range=30d

Allowed range:
7d
30d
90d

Default:
30d

Invalid:
400 VALIDATION_ERROR

Response:

{
"success": true,
"data": {
"range": "30d",
"series": [
{
"date": "2026-05-17",
"present": 10,
"absent": 1,
"leave": 2,
"wfh": 3,
"halfDay": 0
}
]
},
"meta": {
"cached": true,
"generatedAt": "2026-05-17T..."
}
}

Tables read:

- AttendanceRecord

Tables written:

- none

Implementation:

- query AttendanceRecord between range start and today
- group by attendanceDate and status
- fill missing dates with zeros so frontend chart is stable
- return date in YYYY-MM-DD

==================================================
PART 5 — FIX HEADCOUNT RESPONSE
==================================================

GET /api/v1/analytics/headcount-by-department

Response:

{
"success": true,
"data": [
{
"departmentId": "...",
"departmentName": "Engineering",
"employeeCount": 412,
"activeCount": 390
}
],
"meta": {
"cached": true,
"generatedAt": "2026-05-17T..."
}
}

Tables read:

- Department
- Employee

Tables written:

- none

Use groupBy to avoid N+1 where possible.

==================================================
PART 6 — FIX RECENT ACTIVITY RESPONSE
==================================================

GET /api/v1/analytics/recent-activity?limit=10

Response:

{
"success": true,
"data": [
{
"id": "...",
"actorName": "Priya S.",
"action": "created",
"entityType": "Employee",
"entityId": "...",
"resourceLabel": "Employee #E2041",
"createdAt": "2026-05-17T...",
"createdAtIstDisplay": "17/05/2026 04:50:23 PM IST"
}
],
"meta": {
"cached": false,
"generatedAt": "2026-05-17T..."
}
}

Tables read:

- AuditLog
- User
- Employee optional, only if needed for actor name/resource label

Tables written:

- none

Limit:

- default 10
- max 50

Use IST display format:
DD/MM/YYYY hh:mm:ss A IST

==================================================
PART 7 — FIX LEAVE SUMMARY RESPONSE
==================================================

GET /api/v1/analytics/leave-summary?range=30d

Allowed range:
7d
30d
90d

Default:
30d

Response:

{
"success": true,
"data": {
"pending": 0,
"approved": 0,
"rejected": 0,
"withdrawn": 0
},
"meta": {
"cached": true,
"generatedAt": "2026-05-17T..."
}
}

Tables read:

- LeaveRequest

Tables written:

- none

Use current enum values from Prisma. If enum uses DENIED instead of REJECTED, map DENIED to rejected in the API response so UI gets stable wording.

==================================================
PART 8 — FIX CACHE METADATA
==================================================

Current meta always says:

cached: false

Fix it.

getCachedOrFetch must return:

{
data,
cached: true/false
}

Then service response meta must correctly show whether Redis cache was hit.

Cache TTLs:

summary: 60 seconds
attendance: 60 seconds
headcount-by-department: 300 seconds
recent-activity: 30 seconds
leave-summary: 60 seconds

Cache key must include:

- tenantId
- endpoint
- query params

==================================================
PART 9 — FIX ACCESS CONTROL
==================================================

Analytics Page 04 is only for:

SUPER_ADMIN
HR_ADMIN

Denied:
MANAGER
EMPLOYEE
AUDITOR

Also require permission:

analytics:read

If permission middleware already exists, use it. If not, add a small analytics policy that checks both:

- memberType is HR_ADMIN or SUPER_ADMIN
- user permissions include analytics:read

Do not rely only on frontend hiding UI.

==================================================
PART 10 — REAL ANALYTICS TESTS
==================================================

The current analytics unit test mostly tests hardcoded fake objects.

Replace or extend it with real unit tests that import actual analytics service/controller/policy.

Add integration tests for the real API routes.

Required unit tests:

- summary maps DB counts into totalEmployees, activeToday, onLeaveToday, openRequests
- attendance validates range
- attendance returns date series with zeros for missing dates
- headcount returns employeeCount and activeCount
- recent activity formats actorName, resourceLabel, createdAtIstDisplay
- leave summary maps statuses to pending/approved/rejected/withdrawn
- cache hit returns cached=true
- cache miss returns cached=false
- permission policy allows HR_ADMIN/SUPER_ADMIN
- permission policy rejects EMPLOYEE/MANAGER/AUDITOR

Required integration tests:

- HR_ADMIN can access /analytics/summary
- SUPER_ADMIN can access /analytics/summary
- EMPLOYEE gets 403
- MANAGER gets 403
- missing token gets 401
- invalid attendance range gets 400
- /analytics/attendance?range=30d returns series array
- /analytics/headcount-by-department returns department array
- /analytics/recent-activity returns recent rows
- /analytics/leave-summary?range=30d returns stable status object
- tenant isolation is enforced

Required E2E:

- HR Admin login -> analytics summary -> attendance -> headcount -> recent activity

Do not claim Page 04 complete without analytics integration and E2E tests.

==================================================
PART 11 — UPDATE POSTMAN
==================================================

Main collection:

docs/postman/EMS-API.postman_collection.json

Add folder:

Analytics - HR Admin Dashboard

Requests:

- HR Admin Login
- Analytics Summary
- Attendance 30d
- Attendance Invalid Range
- Headcount By Department
- Recent Activity
- Leave Summary
- Employee Forbidden Analytics
- Manager Forbidden Analytics

Newman must pass with 0 failures.

==================================================
PART 12 — DOCUMENT TABLES USED BY EVERY API
==================================================

For every API, document exactly what tables are called.

Update:

docs/api/analytics-api.md
docs/api/ui-team-api-guide.md
docs/backend/database-design.md
docs/backend/backend-design-document.md
docs/diagrams/hr-admin-dashboard-flow.md

For each endpoint include:

- wireframe page number
- visible widget/card/table/chart
- method and URL
- allowed roles
- permission required
- request headers
- query params
- response body
- error responses
- tables/models read
- tables/models written
- indexes used
- cache key
- cache TTL
- frontend usage notes
- Postman request name

Example:

Endpoint:
GET /api/v1/analytics/summary

Wireframe widget:
Page 04 HR Admin Dashboard cards:
Total Employees, Active Today, On Leave, Open Requests

Tables read:

- Employee
- AttendanceRecord
- LeaveRequest
- AttendanceRegularizationRequest

Tables written:

- none

Indexes used:

- Employee: tenantId + employmentStatus + deletedAt
- AttendanceRecord: tenantId + attendanceDate + status
- LeaveRequest: tenantId + status + startDate + endDate
- AttendanceRegularizationRequest: tenantId + status + createdAt

Cache:
analytics:summary:{tenantId}
TTL: 60 seconds

==================================================
PART 13 — WIRE нафRAME DISCIPLINE GOING FORWARD
==================================================

Before coding every page:

1. Open WIREFRAMES.pdf.
2. Identify page number and role.
3. List every visible UI element:
   - card
   - chart
   - table
   - button
   - filter
   - form field
4. Map each UI element to API.
5. Define request body/query params.
6. Define response body.
7. Define roles and permissions.
8. Define tables/models touched.
9. Define indexes needed.
10. Define Postman requests.
11. Define unit, integration, and E2E tests.
12. Only then code.

If HR, Manager, and Employee behavior differ, backend must enforce that difference.

==================================================
PART 14 — FINAL CHECKS
==================================================

Run:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json
npm run email:test -- --to mohammadsaeedafri9@gmail.com

Acceptance:

- 0 lint errors
- 0 failing tests
- 0 Newman failures
- analytics APIs match Page 04 wireframe
- analytics responses match frontend contract
- analytics routes are reachable
- no payroll analytics in Page 04
- no fake analytics tests
- docs include tables/models used by each API
- Postman includes analytics APIs
- safe email test passes
- OTP/password reset email proof shared

Commit:

fix(analytics): align hr admin dashboard with wireframe contracts

Review result: IP has now been authorized in Brevo. Test email again immediately, then finish Page 04 properly.

Do not move to Page 05 yet.

Your report is partially correct, but Page 04 is not accepted because:

- Analytics APIs are not in the main Postman collection.
- Analytics unit tests are still testing hardcoded fake objects instead of real service/repository/controller logic.
- Analytics integration/E2E tests are missing.
- analytics:read permission is not enforced.
- Headcount API uses N+1 queries.
- Attendance date series may miss today.
- IST formatting uses server timezone, not Asia/Kolkata.
- openRequests does not include pending AttendanceRegularizationRequest.
- Real Brevo email proof has not been shown yet.

Complete the following in order.

==================================================
PART 1 — TEST BREVO EMAIL NOW
==================================================

The IP has been authorized:

103.211.52.6/32

Run:

npm run email:test -- --to mohammadsaeedafri9@gmail.com

Expected:

- Email arrives at mohammadsaeedafri9@gmail.com.
- SMTP key is not printed.
- SMTP password is not printed.
- Recipient is masked in output.
- No OTP, reset token, cookie, password, or authorization header is included.

Share only safe output:

- success/failure
- masked recipient
- message ID if available

Do not share SMTP key.

If email still fails, check:

- EMAIL_PROVIDER=smtp
- SMTP_HOST=smtp-relay.brevo.com
- SMTP_PORT=587
- SMTP_USER=ab99cb001@smtp-brevo.com
- SMTP_PASS is the Brevo SMTP key, not account password
- SMTP_FROM is a verified Brevo sender
- Gmail Spam/Promotions folders
- Brevo transactional logs

==================================================
PART 2 — TEST PASSWORD RESET EMAIL
==================================================

Ensure test user exists:

User.email = mohammadsaeedafri9@gmail.com
Employee.workEmail = mohammadsaeedafri9@gmail.com
User.status = ACTIVE

Run if needed:

npm run seed:test-email-user -- --email mohammadsaeedafri9@gmail.com --mfa

Then call:

POST /api/v1/auth/forgot-password

Headers:
X-Tenant-Key: acme
Content-Type: application/json

Body:
{
"email": "mohammadsaeedafri9@gmail.com"
}

Expected:

- API returns 202.
- Password reset email arrives.
- API does not return reset token.
- Logs do not show reset token.
- Logs do not show full reset URL.
- DB stores only hashed token.

==================================================
PART 3 — TEST OTP EMAIL
==================================================

Ensure:

User.email = mohammadsaeedafri9@gmail.com
User.mfaEnabled = true
User.status = ACTIVE

Call:

POST /api/v1/auth/login

Headers:
X-Tenant-Key: acme
Content-Type: application/json

Body:
{
"email": "mohammadsaeedafri9@gmail.com",
"password": "<test-user-password>"
}

Expected:

- API returns 202.
- mfaRequired=true.
- challengeId returned.
- destinationMasked returned.
- accessToken is not returned before OTP verification.
- OTP email arrives.
- raw OTP is not stored.
- raw OTP is not logged.

Then verify:

POST /api/v1/auth/verify-otp

Body:
{
"challengeId": "<challengeId>",
"code": "<otp-from-email>"
}

Expected:

- accessToken returned.
- refresh cookie set.
- user returned.
- permissions returned.
- OTP challenge marked consumed.

Also verify admin MFA:

POST /api/v1/auth/admin/login

If HR_ADMIN/SUPER_ADMIN has mfaEnabled=true:

- returns 202
- mfaRequired=true
- no session before OTP
- OTP verification completes admin login

==================================================
PART 4 — FIX PAGE 04 ANALYTICS DATA ISSUES
==================================================

1. Fix openRequests

Current summary appears to count only pending LeaveRequest.

openRequests must include:

- LeaveRequest status=PENDING
- AttendanceRegularizationRequest status=PENDING if the model exists

Return:

{
"totalEmployees": 0,
"activeToday": 0,
"onLeaveToday": 0,
"openRequests": 0
}

2. Fix attendance range to include today

For range=7d:

- return exactly 7 dates
- include today
- include previous 6 days

For range=30d:

- include today
- include previous 29 days

For range=90d:

- include today
- include previous 89 days

Normalize start/end to day boundaries.

3. Fix IST formatting

Do not use server local timezone.

Use:

Intl.DateTimeFormat('en-IN', {
timeZone: 'Asia/Kolkata',
day: '2-digit',
month: '2-digit',
year: 'numeric',
hour: '2-digit',
minute: '2-digit',
second: '2-digit',
hour12: true
})

Output format should be:

DD/MM/YYYY hh:mm:ss AM/PM IST

Example:
18/05/2026 03:55:00 PM IST

4. Fix headcount N+1 queries

Current implementation loops departments and performs count queries per department.

Replace with:

- one query for departments
- one groupBy for total employees by departmentId
- one groupBy for ACTIVE employees by departmentId
- merge in memory

Do not run count per department.

5. Enforce analytics:read

Current controller only checks HR_ADMIN/SUPER_ADMIN.

Add permission check:

analytics:read

Final rule:

- user must be authenticated
- user must belong to tenant
- memberType must be HR_ADMIN or SUPER_ADMIN
- permissions must include analytics:read

If existing permission middleware exists, use it.
If not, add analytics.policy.js and test it.

6. Keep Page 04 route names exactly:

GET /api/v1/analytics/summary
GET /api/v1/analytics/attendance?range=7d|30d|90d
GET /api/v1/analytics/headcount-by-department
GET /api/v1/analytics/recent-activity?limit=10
GET /api/v1/analytics/leave-summary?range=30d

Do not add payroll analytics in Page 04.

==================================================
PART 5 — REPLACE FAKE ANALYTICS TESTS
==================================================

The current analytics unit test is not acceptable because it tests hardcoded fake objects.

Replace it with real tests that import actual modules.

Unit tests must import and test:

- analytics.service.js
- analytics.repository.js where practical with mocked Prisma
- analytics.controller.js
- analytics.policy.js if created
- analytics.validator.js

Required unit tests:

1. summary maps counts into:
   totalEmployees
   activeToday
   onLeaveToday
   openRequests

2. attendance range validation:
   7d, 30d, 90d allowed
   invalid range rejected

3. attendance date series:
   includes today
   returns exact number of days
   fills missing dates with zeros

4. headcount:
   returns departmentName
   employeeCount
   activeCount
   no N+1 count loop

5. recent activity:
   actorName formatting
   resourceLabel formatting
   true Asia/Kolkata IST display

6. leave summary:
   PENDING -> pending
   APPROVED -> approved
   DENIED -> rejected
   WITHDRAWN -> withdrawn

7. cache:
   cache hit returns cached=true
   cache miss returns cached=false

8. access control:
   HR_ADMIN allowed
   SUPER_ADMIN allowed
   MANAGER denied
   EMPLOYEE denied
   AUDITOR denied
   missing analytics:read denied

==================================================
PART 6 — ADD ANALYTICS INTEGRATION + E2E TESTS
==================================================

Add integration tests for real Fastify routes:

- HR_ADMIN can GET /analytics/summary
- SUPER_ADMIN can GET /analytics/summary
- EMPLOYEE gets 403
- MANAGER gets 403
- missing token gets 401
- user without analytics:read gets 403
- invalid attendance range gets 400
- /analytics/attendance?range=30d returns series array
- /analytics/headcount-by-department returns array
- /analytics/recent-activity returns array with createdAtIstDisplay
- /analytics/leave-summary?range=30d returns stable object
- tenant isolation enforced

Add E2E:

HR Admin login -> analytics summary -> attendance -> headcount -> recent activity -> leave summary

Do not claim Page 04 complete without these.

==================================================
PART 7 — UPDATE POSTMAN
==================================================

Main collection:

docs/postman/EMS-API.postman_collection.json

Add folder:

Analytics - HR Admin Dashboard

Requests:

- HR Admin Login
- Analytics Summary
- Attendance 30d
- Attendance Invalid Range
- Headcount By Department
- Recent Activity
- Leave Summary
- Employee Forbidden Analytics
- Manager Forbidden Analytics
- Missing Token Analytics

Run:

newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

Expected:
0 failed assertions

==================================================
PART 8 — DOCUMENT TABLES USED BY EVERY API
==================================================

Update:

docs/api/analytics-api.md
docs/api/ui-team-api-guide.md
docs/backend/database-design.md
docs/backend/backend-design-document.md
docs/diagrams/hr-admin-dashboard-flow.md

For each analytics API, document:

- wireframe page number
- visible widget/card/chart/table
- method
- URL
- roles allowed
- permission required
- headers
- query params
- response shape
- error responses
- tables/models read
- tables/models written
- indexes used
- cache key
- cache TTL
- Postman request name
- frontend usage notes

Example:

GET /api/v1/analytics/summary

Wireframe:
Page 04 HR Admin dashboard cards:
Total Employees, Active Today, On Leave, Open Requests

Tables read:

- Employee
- AttendanceRecord
- LeaveRequest
- AttendanceRegularizationRequest

Tables written:

- none

Indexes used:

- Employee: tenantId + employmentStatus + deletedAt
- AttendanceRecord: tenantId + attendanceDate + status
- LeaveRequest: tenantId + status + startDate + endDate
- AttendanceRegularizationRequest: tenantId + status + createdAt

Cache:
analytics:summary:{tenantId}
TTL: 60 seconds

==================================================
PART 9 — FINAL COMMANDS
==================================================

Run and share safe output:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json
npm run email:test -- --to mohammadsaeedafri9@gmail.com

Acceptance:

- 0 lint errors
- 0 failing tests
- 0 Newman failures
- safe Brevo test email received
- password reset email received
- OTP email received
- analytics Postman folder exists
- analytics integration/E2E tests exist
- Page 04 APIs match wireframe
- analytics:read is enforced
- no N+1 headcount count loop
- true Asia/Kolkata IST formatting
- no payroll analytics in Page 04

Commit:

fix(analytics): complete page 04 tests postman and email proof

Review result: You are on the right path, but Page 04 is not accepted until the 15 integration setup failures are fixed and Newman includes Analytics APIs.

Important Brevo confirmation:
Brevo SMTP does not use a separate normal password. SMTP_PASS must be the Brevo SMTP key value. Do not use the Brevo account password. Do not use API key. Use:

SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=ab99cb001@smtp-brevo.com
SMTP_PASS=<BREVO_SMTP_KEY_FROM_ENV_ONLY>

Never commit or print SMTP_PASS.

==================================================
PART 1 — FIX ANALYTICS TEST SCHEMA ALIGNMENT NOW
==================================================

Fix the remaining 15 integration setup failures.

The tests currently use fields that do not match Prisma schema.

Fix Employee creation:

Wrong:
email: 'emp@test.com'

Correct:
workEmail: 'emp@test.com'

Fix all Employee test records to use:

- tenantId
- employeeCode
- firstName
- lastName
- workEmail
- joinedOn
- employmentStatus if needed
- departmentId if needed

Fix AuditLog creation:

Wrong:
userId: hrAdminUser.id
description
changedFields
previousValues
newValues

Correct:
actorUserId: hrAdminUser.id
oldValuesJson
newValuesJson

Use schema fields only:

await prisma.auditLog.createMany({
data: [
{
tenantId,
actorUserId: hrAdminUser.id,
action: 'CREATE',
entityType: 'Employee',
entityId: employee.id,
oldValuesJson: {},
newValuesJson: { status: 'ACTIVE' },
createdAt: new Date()
}
]
});

Fix these files:

tests/unit/analytics.service.test.js
tests/integration/analytics.routes.test.js
tests/e2e/analytics.e2e.test.js

Search and remove all invalid fields:

grep -R "email: 'emp@test.com'" tests/
grep -R "userId:" tests/
grep -R "description:" tests/
grep -R "changedFields" tests/
grep -R "previousValues" tests/
grep -R "newValues" tests/

Replace with valid schema fields.

Run:

npm run test:integration
npm run test:e2e
npm run test

Acceptance:

- 0 failing tests
- no schema field mismatch
- analytics integration tests pass
- analytics E2E tests pass

==================================================
PART 2 — VERIFY BREVO EMAIL DELIVERY
==================================================

The IP was authorized by owner.

Run:

curl -4 https://api.ipify.org

Verify it returns:

103.211.52.6

or if it returns a new IP, tell owner to authorize the new IP as:

<public-ip>/32

Then run:

npm run email:test -- --to mohammadsaeedafri9@gmail.com

Expected:

- safe test email received
- recipient email masked in CLI output
- SMTP key not printed
- SMTP password not printed
- no OTP
- no reset token
- no password
- no cookie
- no authorization header

If sending fails, check:

EMAIL_PROVIDER=smtp
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=ab99cb001@smtp-brevo.com
SMTP_PASS=<Brevo SMTP key>
SMTP_FROM=<verified Brevo sender>

Also check:

- Brevo authorized IP list
- Brevo transactional logs
- Gmail inbox/spam/promotions

==================================================
PART 3 — TEST PASSWORD RESET EMAIL
==================================================

Ensure test user exists:

npm run seed:test-email-user -- --email mohammadsaeedafri9@gmail.com --mfa

The script must:

- use User.email = mohammadsaeedafri9@gmail.com
- use Employee.workEmail = mohammadsaeedafri9@gmail.com
- hide password by default
- mask email in output

Call:

POST /api/v1/auth/forgot-password

Headers:
X-Tenant-Key: acme
Content-Type: application/json

Body:
{
"email": "mohammadsaeedafri9@gmail.com"
}

Expected:

- 202 response
- reset email received
- reset token not returned
- reset token not logged
- full reset URL not logged
- DB stores only hashed token

==================================================
PART 4 — TEST OTP EMAIL
==================================================

Ensure:

User.email = mohammadsaeedafri9@gmail.com
User.mfaEnabled = true
User.status = ACTIVE

Call normal login:

POST /api/v1/auth/login

Expected:

- 202
- mfaRequired=true
- challengeId returned
- destinationMasked returned
- no accessToken before OTP
- OTP email received

Then verify OTP:

POST /api/v1/auth/verify-otp

Expected:

- accessToken returned
- refresh cookie set
- user returned
- permissions returned
- OTP challenge consumed

Also verify admin MFA:

POST /api/v1/auth/admin/login

If HR_ADMIN or SUPER_ADMIN has mfaEnabled=true:

- returns 202
- mfaRequired=true
- no session before OTP
- OTP verification completes admin login

==================================================
PART 5 — ADD ANALYTICS TO POSTMAN
==================================================

Main collection:

docs/postman/EMS-API.postman_collection.json

Add folder:

Analytics - HR Admin Dashboard

Requests:

1. HR Admin Login
2. Analytics Summary
   GET /api/v1/analytics/summary

3. Attendance 30d
   GET /api/v1/analytics/attendance?range=30d

4. Attendance Invalid Range
   GET /api/v1/analytics/attendance?range=invalid

5. Headcount By Department
   GET /api/v1/analytics/headcount-by-department

6. Recent Activity
   GET /api/v1/analytics/recent-activity?limit=10

7. Leave Summary
   GET /api/v1/analytics/leave-summary?range=30d

8. Employee Forbidden Analytics
   Expected 403

9. Manager Forbidden Analytics
   Expected 403

10. Missing Token Analytics
    Expected 401

Run:

newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

Acceptance:

- 0 failed assertions

==================================================
PART 6 — FINISH PAGE 04 ACCEPTANCE
==================================================

Page 04 wireframe is HR Admin Dashboard.

Required final APIs:

GET /api/v1/analytics/summary
GET /api/v1/analytics/attendance?range=7d|30d|90d
GET /api/v1/analytics/headcount-by-department
GET /api/v1/analytics/recent-activity?limit=10
GET /api/v1/analytics/leave-summary?range=30d

Required access:

- HR_ADMIN allowed
- SUPER_ADMIN allowed
- MANAGER denied
- EMPLOYEE denied
- AUDITOR denied
- analytics:read required

Required response contracts:

Summary:
{
"totalEmployees": 0,
"activeToday": 0,
"onLeaveToday": 0,
"openRequests": 0
}

Attendance:
{
"range": "30d",
"series": [
{
"date": "YYYY-MM-DD",
"present": 0,
"absent": 0,
"leave": 0,
"wfh": 0,
"halfDay": 0
}
]
}

Headcount:
[
{
"departmentId": "...",
"departmentName": "Engineering",
"employeeCount": 0,
"activeCount": 0
}
]

Recent Activity:
[
{
"id": "...",
"actorName": "Priya Sharma",
"action": "create",
"entityType": "Employee",
"entityId": "...",
"resourceLabel": "Employee #ABCDE",
"createdAt": "...",
"createdAtIstDisplay": "18/05/2026 03:55:00 PM IST"
}
]

Leave Summary:
{
"pending": 0,
"approved": 0,
"rejected": 0,
"withdrawn": 0
}

Also verify:

- attendance range includes today
- headcount avoids N+1
- cache metadata shows true/false correctly
- openRequests includes pending leave + pending attendance regularization
- IST formatting uses Asia/Kolkata, not server local time
- no payroll analytics in Page 04

==================================================
PART 7 — DOCUMENT TABLES FOR EACH API
==================================================

Update:

docs/api/analytics-api.md
docs/api/ui-team-api-guide.md
docs/backend/database-design.md
docs/backend/backend-design-document.md
docs/diagrams/hr-admin-dashboard-flow.md

For every Analytics API include:

- wireframe page number
- visible widget/card/chart/table
- method
- URL
- allowed roles
- permission required
- request headers
- query params
- response body
- error responses
- tables/models read
- tables/models written
- indexes used
- cache key
- cache TTL
- Postman request name
- frontend usage notes

Example:

GET /api/v1/analytics/summary

Wireframe:
Page 04 HR Admin cards:
Total Employees, Active Today, On Leave, Open Requests

Tables read:

- Employee
- AttendanceRecord
- LeaveRequest
- AttendanceRegularizationRequest

Tables written:

- none

Indexes used:

- Employee: tenantId + employmentStatus + deletedAt
- AttendanceRecord: tenantId + attendanceDate + status
- LeaveRequest: tenantId + status + startDate + endDate
- AttendanceRegularizationRequest: tenantId + status + createdAt

Cache:
analytics:summary:{tenantId}
TTL:
60 seconds

==================================================
PART 8 — FINAL PAGE 04 COMMANDS
==================================================

Run and share safe output:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json
npm run email:test -- --to mohammadsaeedafri9@gmail.com

Acceptance:

- 0 lint errors
- 0 failing unit tests
- 0 failing integration tests
- 0 failing E2E tests
- 0 Newman failures
- safe Brevo test email received
- password reset email received
- OTP email received
- analytics APIs match Page 04 wireframe
- docs list tables and indexes for every API

Commit:

fix(analytics): finalize page 04 analytics tests postman and email proof

==================================================
PART 9 — START PAGE 05 AFTER PAGE 04 PASSES
==================================================

After Page 04 passes, start Page 05: Manager Dashboard.

Wireframe:
Page 05 Dashboard — Manager

Role:
MANAGER

Allowed:
MANAGER
HR_ADMIN
SUPER_ADMIN

Employee:
denied unless viewing own employee dashboard on Page 06

Build only Manager Dashboard APIs. Do not build Employee Dashboard yet.

Required APIs:

1. GET /api/v1/manager/dashboard

Returns:
{
"success": true,
"data": {
"teamSize": 0,
"presentToday": 0,
"onLeaveToday": 0,
"pendingApprovals": 0,
"teamAttendanceRate": 0
}
}

Tables read:

- Employee
- AttendanceRecord
- LeaveRequest
- AttendanceRegularizationRequest

Scope:
Only direct reports where Employee.managerId = logged-in employee id.

2. GET /api/v1/manager/team

Query:
search
departmentId
status
page
limit

Returns paginated direct reports.

Tables read:

- Employee
- Department
- AttendanceRecord optional for today status

3. GET /api/v1/manager/team-attendance?range=7d|30d

Returns attendance series for direct reports only.

Tables read:

- Employee
- AttendanceRecord

4. GET /api/v1/manager/pending-approvals

Returns pending leave and attendance regularization requests for direct reports.

Tables read:

- LeaveRequest
- AttendanceRegularizationRequest
- Employee
- LeaveType

5. PATCH /api/v1/manager/leave-requests/:id/decision

Request:
{
"decision": "APPROVED",
"comment": "Approved"
}

Allowed decisions:
APPROVED
DENIED

Rules:

- Manager can approve/deny only direct report requests.
- Manager cannot approve own leave.
- Write audit log.
- Update leave balances transactionally if applicable.

6. PATCH /api/v1/manager/regularization-requests/:id/decision

Request:
{
"decision": "APPROVED",
"comment": "Approved"
}

Rules:

- Manager can approve/deny only direct report requests.
- Write audit log.

Access:

- MANAGER can access own team only
- HR_ADMIN and SUPER_ADMIN can access if query includes managerId or for admin oversight
- EMPLOYEE denied

Cache:

- dashboard summary TTL 60 seconds
- team list no cache or 30 seconds
- pending approvals TTL 30 seconds

Tests:
Unit:

- team scope calculation
- dashboard stats
- pending approvals merge
- direct report policy
- manager cannot approve outside team
- manager cannot approve own leave

Integration:

- manager dashboard success
- employee denied
- manager sees only direct reports
- approve direct report leave success
- approve outside team leave 403
- missing token 401
- invalid decision 400

E2E:

- manager login -> dashboard -> team -> pending approvals -> approve leave

Postman:
Add folder:
Manager Dashboard

Requests:

- Manager Login
- Manager Dashboard
- Manager Team
- Team Attendance
- Pending Approvals
- Approve Leave
- Deny Leave
- Employee Forbidden Manager Dashboard
- Outside Team Approval Forbidden

Docs:
Create/update:
docs/api/manager-dashboard-api.md
docs/api/ui-team-api-guide.md
docs/backend/database-design.md
docs/diagrams/manager-dashboard-flow.md

For every Page 05 API document:

- tables read
- tables written
- indexes used
- roles
- request/response
- frontend notes

Commit:

feat(manager): implement manager dashboard apis

Review result: Page 04 is not accepted yet from the actual ZIP. Move fast, but do not skip proof.

Brevo confirmation:
Brevo SMTP does not use a separate account password. SMTP_PASS must be the SMTP key value. Use:
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=ab99cb001@smtp-brevo.com
SMTP_PASS=<BREVO_SMTP_KEY_FROM_ENV_ONLY>

Never commit SMTP_PASS.
Never print SMTP_PASS.
Never add it to docs, Postman, seed files, screenshots, or logs.
The current key was exposed in chat, so use it for testing only and rotate it before staging/production.

==================================================
SPRINT: FINAL PAGE 04 ACCEPTANCE + ROBUST SEED + PAGE 05/06
==================================================

Do this in exact order.

==================================================
PART 1 — FIX PAGE 04 TEST FAILURES / SCHEMA MISMATCHES
==================================================

The current ZIP still has invalid Prisma fields in tests.

Search and fix:

grep -R "email: 'emp@test.com'" tests/
grep -R "userId:" tests/unit/analytics.service.test.js tests/integration/analytics.routes.test.js tests/e2e/analytics.e2e.test.js
grep -R "description:" tests/unit/analytics.service.test.js tests/integration/analytics.routes.test.js tests/e2e/analytics.e2e.test.js
grep -R "changedFields" tests/
grep -R "previousValues" tests/
grep -R "newValues" tests/

Fix Employee test records:

Wrong:
email: 'emp@test.com'

Correct:
workEmail: 'emp@test.com'

Required Employee fields:
tenantId
employeeCode
firstName
lastName
workEmail
joinedOn
employmentStatus if needed
departmentId if needed
managerId if needed

Fix AuditLog test records:

Wrong:
userId
description
changedFields
previousValues
newValues

Correct:
actorUserId
oldValuesJson
newValuesJson

Example:

await prisma.auditLog.createMany({
data: [
{
tenantId,
actorUserId: hrAdminUser.id,
action: 'CREATE',
entityType: 'Employee',
entityId: employee.id,
oldValuesJson: {},
newValuesJson: { status: 'ACTIVE' },
createdAt: new Date()
}
]
});

Run:

npm run test:unit
npm run test:integration
npm run test:e2e
npm run test

Acceptance:

- 0 failing tests
- no invalid Prisma schema fields in analytics tests
- analytics unit, integration, and E2E tests really pass

==================================================
PART 2 — ADD ANALYTICS TO POSTMAN
==================================================

Current main Postman collection still does not include Analytics APIs.

Update:

docs/postman/EMS-API.postman_collection.json

Add folder:

Analytics - HR Admin Dashboard

Requests:

1. HR Admin Login
2. Analytics Summary
   GET /api/v1/analytics/summary

3. Attendance 30d
   GET /api/v1/analytics/attendance?range=30d

4. Attendance Invalid Range
   GET /api/v1/analytics/attendance?range=invalid

5. Headcount By Department
   GET /api/v1/analytics/headcount-by-department

6. Recent Activity
   GET /api/v1/analytics/recent-activity?limit=10

7. Leave Summary
   GET /api/v1/analytics/leave-summary?range=30d

8. Employee Forbidden Analytics
   Expected 403

9. Manager Forbidden Analytics
   Expected 403

10. Missing Token Analytics
    Expected 401

Run:

newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json

Acceptance:

- 0 failed Newman assertions

==================================================
PART 3 — VERIFY BREVO EMAIL FOR REAL
==================================================

The SMTP key is the SMTP password.

First check public IP:

curl -4 https://api.ipify.org

If IP is not already authorized in Brevo, ask owner to authorize it as:

<public-ip>/32

Then run:

npm run email:test -- --to mohammadsaeedafri9@gmail.com

Expected:

- safe email arrives at mohammadsaeedafri9@gmail.com
- SMTP key is not printed
- SMTP password is not printed
- recipient is masked
- no OTP
- no reset token
- no password
- no cookie
- no authorization header

Then test password reset email:

POST /api/v1/auth/forgot-password

Headers:
X-Tenant-Key: acme
Content-Type: application/json

Body:
{
"email": "mohammadsaeedafri9@gmail.com"
}

Expected:

- 202 response
- reset email received
- token not returned
- token not logged
- DB stores only hashed token

Then test OTP email:

POST /api/v1/auth/login

Using:
User.email = mohammadsaeedafri9@gmail.com
User.mfaEnabled = true

Expected:

- 202
- mfaRequired=true
- challengeId returned
- OTP email received
- accessToken not returned before OTP
- raw OTP not stored
- raw OTP not logged

==================================================
PART 4 — BUILD ROBUST SEED DATA
==================================================

Current seed data is too small. Build a robust test dataset.

Update prisma/seed.js or create:

prisma/seedLargeDemo.js

Add package script:

"db:seed:large": "node prisma/seedLargeDemo.js"

Seed at least 60 employees across departments.

Departments:

- Engineering
- Sales
- Operations
- HR
- Finance
- Support
- Product
- QA

Users:

- 1 SUPER_ADMIN
- 2 HR_ADMIN
- 5 MANAGER
- 50+ EMPLOYEE

Employee data requirements:

- unique employeeCode
- firstName / lastName
- workEmail
- personalEmail optional
- phone
- joinedOn across different dates
- employmentStatus mix:
  ACTIVE
  ON_LEAVE
  INACTIVE
  RESIGNED if supported
- employmentType mix:
  FULL_TIME
  CONTRACT
  INTERN if supported
- departmentId assigned
- managerId assigned for direct reports

Important email rule:

- User.email is source of truth for login, OTP, and password reset.
- Employee.workEmail is HR-entered work email.
- When employee has login access, User.email must equal Employee.workEmail.
- Employee.personalEmail is not used for OTP/password reset unless business approves later.

Attendance seed:
Generate last 30 days for employees:

- PRESENT
- ABSENT
- LEAVE
- WFH
- HALF_DAY
- HOLIDAY if supported

Leave seed:
Create leave types:

- Annual Leave
- Sick Leave
- Casual Leave
- Comp Off

Create leave balances for all employees.

Create leave requests:

- 10 pending leave requests
- 10 approved leave requests
- 5 denied leave requests
- 5 withdrawn leave requests
- 5 currently on leave today
- some future leave
- some past leave

Attendance regularization:

- 10 pending requests
- 5 approved
- 5 denied

Documents:
For some employees:

- Offer Letter verified
- Aadhaar verified
- PAN verified
- Bank Account pending
- Experience Letter pending

Audit logs:
Create at least 100 audit logs:

- employee created
- leave approved
- document uploaded
- attendance regularized
- department updated
- login/logout events

Holidays:
Create 10 upcoming/past holidays.

Performance:
Seed should complete quickly.
Use createMany where possible.
Avoid one insert per row unless relations require it.

Acceptance:

- db:seed creates base small dataset
- db:seed:large creates 60+ employee demo dataset
- analytics dashboard has meaningful data
- manager dashboard has direct reports
- employee dashboard has attendance, leave, documents, team
- leave/attendance APIs have realistic data for testing

==================================================
PART 5 — PAGE 04 FINAL ACCEPTANCE
==================================================

Page 04 APIs must match wireframe exactly.

Required:

GET /api/v1/analytics/summary
GET /api/v1/analytics/attendance?range=7d|30d|90d
GET /api/v1/analytics/headcount-by-department
GET /api/v1/analytics/recent-activity?limit=10
GET /api/v1/analytics/leave-summary?range=30d

Summary response:

{
"success": true,
"data": {
"totalEmployees": 0,
"activeToday": 0,
"onLeaveToday": 0,
"openRequests": 0
},
"meta": {
"cached": false,
"generatedAt": "..."
}
}

Analytics rules:

- HR_ADMIN allowed
- SUPER_ADMIN allowed
- MANAGER denied
- EMPLOYEE denied
- AUDITOR denied
- analytics:read permission required
- every query scoped by tenantId
- no payroll analytics in Page 04
- Redis cache metadata must correctly show true/false
- headcount must avoid N+1 count loop
- attendance range must include today
- IST formatting must use Asia/Kolkata

==================================================
PART 6 — BUILD PAGE 05 MANAGER DASHBOARD
==================================================

After Page 04 final checks pass, start Page 05.

Wireframe:
Page 05 Dashboard - Manager

Role:
MANAGER

Allowed:
MANAGER
HR_ADMIN
SUPER_ADMIN

Employee:
Denied for Manager Dashboard.

Manager scope:
Manager sees only direct reports:
Employee.managerId = logged-in user's employeeId

Do not show all company data to manager.

Required APIs:

1. GET /api/v1/manager/dashboard

Response:
{
"success": true,
"data": {
"teamSize": 14,
"presentToday": 11,
"pendingApprovals": 5,
"averageAttendance": 94,
"onLeaveToday": 3,
"regularizationRequestsThisMonth": 2
},
"meta": {
"cached": false,
"generatedAt": "..."
}
}

Tables read:

- Employee
- AttendanceRecord
- LeaveRequest
- AttendanceRegularizationRequest

2. GET /api/v1/manager/team

Query:
search
departmentId
status
page
limit

Response:
Paginated direct reports only.

Tables read:

- Employee
- Department
- AttendanceRecord optional for today status

3. GET /api/v1/manager/team-attendance?range=7d

Response:
{
"success": true,
"data": {
"range": "7d",
"days": ["Mon", "Tue", "Wed", "Thu", "Fri"],
"employees": [
{
"employeeId": "...",
"employeeName": "Priya Sharma",
"cells": [
{ "date": "2026-05-18", "code": "P", "status": "PRESENT" }
]
}
]
}
}

Status codes:
P = Present
A = Absent
L = Leave
W = WFH
H = Half Day

4. GET /api/v1/manager/pending-approvals

Response:
{
"success": true,
"data": [
{
"id": "...",
"type": "LEAVE",
"employeeId": "...",
"employeeName": "Priya Sharma",
"title": "Annual leave",
"durationLabel": "3 days",
"status": "PENDING",
"submittedAt": "..."
}
]
}

Include:

- pending leave requests from direct reports
- pending attendance regularization from direct reports

5. PATCH /api/v1/manager/leave-requests/:id/decision

Request:
{
"decision": "APPROVED",
"comment": "Approved"
}

Allowed:
APPROVED
DENIED

Rules:

- Manager can approve/deny only direct reports
- Manager cannot approve own leave
- write audit log
- update leave balance transactionally if needed
- send notification/email job if configured

6. PATCH /api/v1/manager/regularization-requests/:id/decision

Request:
{
"decision": "APPROVED",
"comment": "Approved"
}

Rules:

- Manager can approve/deny only direct reports
- write audit log
- update attendance record if approved

Permissions:

- team:read for dashboard/team/team-attendance
- leave:approve for leave decisions
- attendance:approve or attendance:write for regularization decisions

Tests:
Unit:

- direct report scope
- team size calculation
- pending approvals merge
- manager cannot approve outside team
- manager cannot approve own leave
- status code mapping for attendance grid

Integration:

- manager dashboard success
- employee denied
- manager sees only direct reports
- HR_ADMIN can access with managerId if supported
- approve direct report leave success
- approve outside team leave 403
- invalid decision 400
- missing token 401

E2E:

- manager login -> dashboard -> team -> pending approvals -> approve leave

Postman:
Add folder:
Manager Dashboard

Requests:

- Manager Login
- Manager Dashboard
- Manager Team
- Team Attendance
- Pending Approvals
- Approve Leave
- Deny Leave
- Approve Regularization
- Employee Forbidden Manager Dashboard
- Outside Team Approval Forbidden

Docs:
Create:
docs/api/manager-dashboard-api.md
docs/diagrams/manager-dashboard-flow.md

Update:
docs/api/ui-team-api-guide.md
docs/backend/database-design.md

Document for each API:

- tables read
- tables written
- indexes used
- permissions
- response contract
- frontend notes

Commit:
feat(manager): implement manager dashboard apis

==================================================
PART 7 — BUILD PAGE 06 EMPLOYEE DASHBOARD IN SAME SPRINT
==================================================

After Manager Dashboard APIs are implemented, build Page 06.

Wireframe:
Page 06 Dashboard - Employee

Visible:
Every authenticated user for self view.

Employee scope:
User can see only their own data.

Required APIs:

1. GET /api/v1/employee/dashboard

Response:
{
"success": true,
"data": {
"todayAttendance": {},
"leaveBalances": [],
"upcomingHolidays": [],
"documents": [],
"team": []
}
}

2. GET /api/v1/attendance/today

Self only.

3. POST /api/v1/attendance/check-in

Request:
{
"workMode": "WFH",
"location": {
"lat": 0,
"lng": 0
}
}

Rules:

- prevent duplicate active check-in
- create attendance record
- write audit log

4. POST /api/v1/attendance/check-out

Rules:

- must have check-in
- calculate totalMinutes
- write audit log

5. GET /api/v1/leave/balance/me

6. GET /api/v1/holidays/upcoming?limit=3

7. GET /api/v1/employees/me/documents

8. GET /api/v1/employees/me/team

Rules:

- Employee can view own data only
- Manager/HR can still use their proper dashboards, but self dashboard remains self-scoped
- Employee cannot edit HR/company fields
- tenantId enforced everywhere

Tests:
Unit:

- attendance check-in duplicate prevention
- check-out without check-in fails
- leave balance mapping
- upcoming holiday filtering
- document visibility
- self-team mapping

Integration:

- employee dashboard success
- employee cannot access another employee dashboard
- check-in success
- duplicate check-in 409
- check-out success
- leave balance me success
- documents me success
- holidays upcoming success

E2E:

- employee login -> dashboard -> check-in -> check-out -> leave balance -> documents

Postman:
Add folder:
Employee Dashboard

Requests:

- Employee Login
- Employee Dashboard
- Attendance Today
- Check In
- Duplicate Check In
- Check Out
- Leave Balance Me
- Upcoming Holidays
- My Documents
- My Team

Docs:
Create:
docs/api/employee-dashboard-api.md
docs/diagrams/employee-dashboard-flow.md

Update:
docs/api/ui-team-api-guide.md
docs/backend/database-design.md

Commit:
feat(employee): implement employee dashboard apis

==================================================
PART 8 — PERFORMANCE REQUIREMENTS
==================================================

Add performance script:

"perf:api": "node scripts/perfApiSmoke.js"

Or use autocannon:

npm install -D autocannon

Performance budgets on seeded 60+ employee data:

Analytics:

- cached p95 < 20ms
- uncached p95 < 150ms

Manager dashboard:

- p95 < 150ms

Employee dashboard:

- p95 < 120ms

List endpoints:

- p95 < 200ms for 60 employees
- must use pagination

Add docs:
docs/backend/performance-testing.md

Include:

- how to run perf test
- seeded dataset size
- p50/p95/p99 results
- slow query notes
- index recommendations

==================================================
PART 9 — FINAL COMMANDS FOR THIS SPRINT
==================================================

Run and share safe output:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
npm run db:seed:large
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json
npm run email:test -- --to mohammadsaeedafri9@gmail.com
npm run perf:api

Acceptance:

- 0 lint errors
- 0 unit failures
- 0 integration failures
- 0 E2E failures
- 0 Newman failures
- real Brevo safe email received
- password reset email received
- OTP email received
- 60+ employees seeded
- mixed attendance/leave/documents data seeded
- Page 04 accepted
- Page 05 APIs complete
- Page 06 APIs complete
- UI docs include tables/indexes for every API
- performance budgets met

Final commits:
fix(analytics): finalize page 04 test and postman proof
feat(seed): add large demo dataset for robust qa
feat(manager): implement manager dashboard apis
feat(employee): implement employee dashboard apis
test(perf): add api performance smoke tests

Review result: do not call Pages 05/06 production-ready yet. Page 04 is close, but Page 05 and Page 06 have schema mismatches and missing proof.

We need to move fast, but no leniency. Frontend depends fully on these APIs, so every endpoint must be real, tested, documented, fast, and response-proofed.

From now on, every sprint must save API responses for review.

==================================================
PART 1 — SAVE ALL TEST RESPONSES FOR REVIEW
==================================================

Create this folder structure:

artifacts/
api-responses/
page-04-analytics/
page-05-manager-dashboard/
page-06-employee-dashboard/
page-07-employees-list/
newman/
performance/
screenshots-or-logs/

For every API you implement or modify, save:

1. Success response JSON
2. Validation error response JSON
3. Unauthorized 401 response JSON
4. Forbidden 403 response JSON
5. Not-found or conflict response JSON where applicable

Use filenames like:

artifacts/api-responses/page-04-analytics/analytics-summary-success.json
artifacts/api-responses/page-04-analytics/analytics-summary-employee-403.json
artifacts/api-responses/page-05-manager-dashboard/pending-approvals-success.json
artifacts/api-responses/page-05-manager-dashboard/approve-leave-success.json
artifacts/api-responses/page-05-manager-dashboard/approve-leave-outside-team-403.json
artifacts/api-responses/page-06-employee-dashboard/check-in-success.json
artifacts/api-responses/page-06-employee-dashboard/duplicate-check-in-409.json

Also save:

artifacts/newman/page-04-05-06-newman-report.json
artifacts/performance/page-04-05-06-performance.json

Add package scripts if needed:

"api:capture": "node scripts/captureApiResponses.js"
"perf:api": "node scripts/perfApiSmoke.js"

Acceptance:

- every endpoint has saved success + failure responses
- response files must be valid JSON
- no token, cookie, password, SMTP key, OTP, reset token, or secret in saved files
- saved responses should mask emails where needed

==================================================
PART 2 — FIX SEED DATA PROPERLY
==================================================

The current seed data is good in intention but still uses invalid schema fields in some places.

Fix prisma/seed.js and/or create prisma/seedLargeDemo.js.

Schema alignment:

AttendanceRecord fields:
Use:
checkInAt
checkOutAt
totalMinutes
workMode
locationJson

Do not use:
checkInTime
checkOutTime

LeaveRequest fields:
Use:
approverId
approverComment
decidedAt

Do not use:
approvedBy
approvedAt

AttendanceRegularizationRequest fields:
Use:
reviewerId
reviewerComment

Do not use:
approvedBy
approvedAt

AuditLog fields:
Use:
actorUserId
oldValuesJson
newValuesJson

Employee fields:
Use:
workEmail
joinedOn

Do not swallow seed errors silently.
Remove broad:

.catch(() => undefined)

or replace with targeted duplicate handling.

Add script:

"db:seed:large": "node prisma/seedLargeDemo.js"

Large seed requirements:

- 8 departments:
  Engineering, Sales, Operations, HR, Finance, Support, Product, QA

- Users:
  1 SUPER_ADMIN
  2 HR_ADMIN
  5 MANAGER
  60 EMPLOYEE

- Employees:
  60+ employees
  all with unique employeeCode
  workEmail
  personalEmail optional
  phone
  joinedOn spread across dates
  departmentId
  managerId for direct reports
  employmentStatus mix:
  ACTIVE
  ON_LEAVE
  INACTIVE
  RESIGNED if enum supports it

- Attendance:
  last 30 days
  statuses:
  PRESENT
  ABSENT
  LEAVE
  WFH
  HALF_DAY
  include checkInAt/checkOutAt where relevant
  include totalMinutes

- Leave:
  leave types:
  Annual Leave
  Sick Leave
  Casual Leave
  Comp Off
  leave balances for all employees
  leave requests:
  10 pending
  10 approved
  5 denied
  5 withdrawn
  5 currently on leave today
  future and past leaves

- Attendance regularization:
  10 pending
  5 approved
  5 denied

- Documents:
  Create EmployeeDocument rows for at least 20 employees:
  Offer Letter verified
  Aadhaar verified
  PAN verified
  Bank Account pending
  Experience Letter pending
  Use valid fields:
  uploadedById
  verifiedById
  verificationStatus

- Audit logs:
  at least 100 records:
  employee created
  leave approved
  document uploaded
  attendance regularized
  department updated
  login/logout events

- Holidays:
  at least 10 holidays

Acceptance:

- npm run db:seed passes
- npm run db:seed:large passes
- no silent failed records
- analytics dashboard shows meaningful non-zero data
- manager dashboard has pending approvals and team attendance
- employee dashboard has leave balances, documents, holidays, team

==================================================
PART 3 — FINALIZE PAGE 04 HR ADMIN DASHBOARD
==================================================

Page 04 wireframe must remain exact.

Required APIs:

GET /api/v1/analytics/summary
GET /api/v1/analytics/attendance?range=7d|30d|90d
GET /api/v1/analytics/headcount-by-department
GET /api/v1/analytics/recent-activity?limit=10
GET /api/v1/analytics/leave-summary?range=30d

Rules:

- HR_ADMIN allowed
- SUPER_ADMIN allowed
- MANAGER denied
- EMPLOYEE denied
- AUDITOR denied
- analytics:read required
- tenantId in every query
- no payroll analytics in Page 04
- Redis cache metadata accurate
- headcount no N+1 queries
- attendance includes today
- IST uses Asia/Kolkata
- openRequests = pending leave + pending attendance regularization

Postman:
Add/verify Analytics folder in:

docs/postman/EMS-API.postman_collection.json

Save response captures in:

artifacts/api-responses/page-04-analytics/

Tests:

- unit
- integration
- E2E

Acceptance:

- Newman 0 failures
- analytics response captures saved
- docs list tables/indexes per endpoint

==================================================
PART 4 — FIX PAGE 05 MANAGER DASHBOARD PROPERLY
==================================================

Page 05 wireframe:
Dashboard — Manager

Current manager module is not production-ready.

Fix route names to match stable backend standard.

Preferred final APIs:

GET /api/v1/manager/dashboard
GET /api/v1/manager/team
GET /api/v1/manager/team-attendance?range=7d|30d
GET /api/v1/manager/pending-approvals
PATCH /api/v1/manager/leave-requests/:id/decision
PATCH /api/v1/manager/regularization-requests/:id/decision
PATCH /api/v1/manager/leave-requests/bulk-decision

If old /dashboard/manager routes exist, keep aliases temporarily but document the final routes above for UI team.

Access:

- MANAGER allowed for own team only
- HR_ADMIN/SUPER_ADMIN allowed for oversight only if managerId query is supplied
- EMPLOYEE denied
- AUDITOR denied

Permissions:

- team:read for dashboard/team/team-attendance
- leave:approve for leave decisions
- attendance:approve for regularization decisions

Manager scope:
Employee.managerId = logged-in employee id

Manager dashboard response:

{
"success": true,
"data": {
"teamSize": 14,
"presentToday": 11,
"pendingApprovals": 5,
"averageAttendance": 94,
"onLeaveToday": 3,
"regularizationRequestsThisMonth": 2
},
"meta": {
"cached": false,
"generatedAt": "..."
}
}

Pending approvals response must match wireframe cards:

{
"success": true,
"data": [
{
"id": "...",
"type": "LEAVE",
"employeeId": "...",
"employeeName": "Priya S.",
"title": "Annual leave",
"durationLabel": "3 days",
"status": "PENDING",
"submittedAt": "..."
}
]
}

Team attendance grid response:

{
"success": true,
"data": {
"range": "7d",
"days": ["Mon", "Tue", "Wed", "Thu", "Fri"],
"employees": [
{
"employeeId": "...",
"employeeName": "P. Sharma",
"cells": [
{
"date": "2026-05-18",
"code": "P",
"status": "PRESENT"
}
]
}
]
}
}

Status code mapping:
P = PRESENT
A = ABSENT
L = LEAVE
W = WFH
H = HALF_DAY

Approval APIs:

PATCH /api/v1/manager/leave-requests/:id/decision

Request:
{
"decision": "APPROVED",
"comment": "Approved"
}

Allowed:
APPROVED
DENIED

Must:

- verify request belongs to direct report
- reject manager approving own leave
- reject if not PENDING
- update LeaveRequest.status
- set approverId to current user id
- set approverComment
- set decidedAt
- update leave balance transactionally if approved
- write AuditLog with actorUserId
- invalidate manager dashboard/pending cache
- return updated request
- ensure request disappears from pending approvals after approval

PATCH /api/v1/manager/regularization-requests/:id/decision

Must:

- verify direct report
- reject if not PENDING
- update status
- set reviewerId to current user id
- set reviewerComment
- if approved, update/create AttendanceRecord
- write AuditLog
- invalidate manager dashboard/pending cache

Bulk approve:
PATCH /api/v1/manager/leave-requests/bulk-decision

Request:
{
"requestIds": ["..."],
"decision": "APPROVED",
"comment": "Bulk approved"
}

Must:

- validate all requests belong to direct reports
- use transaction
- partial failures should return details if partial mode is enabled
- default should be all-or-nothing

Important stale-data rule:
After any approval/denial:

- pending approvals API must not show that item anymore
- open requests counts must update
- leave balance must update
- audit log must show decision
- cache must be invalidated

Tests:
Unit:

- direct report scope
- manager cannot approve outside team
- manager cannot approve own leave
- approval status transition
- stale pending prevention
- bulk decision all-or-nothing
- team attendance code mapping

Integration:

- manager dashboard success
- employee denied
- manager sees only direct reports
- approve direct report leave success
- approved leave disappears from pending approvals
- approve outside team 403
- approve already approved leave 409
- invalid decision 400
- missing token 401
- HR/Super Admin oversight if supported

E2E:

- manager login -> dashboard -> pending approvals -> approve leave -> pending approvals no longer includes approved item

Postman:
Add folder:
Manager Dashboard

Requests:

- Manager Login
- Manager Dashboard
- Manager Team
- Team Attendance
- Pending Approvals
- Approve Leave
- Deny Leave
- Bulk Approve Leave
- Approve Regularization
- Employee Forbidden Manager Dashboard
- Outside Team Approval Forbidden
- Already Approved Conflict

Save captures:
artifacts/api-responses/page-05-manager-dashboard/

Docs:
Create/update:
docs/api/manager-dashboard-api.md
docs/api/ui-team-api-guide.md
docs/backend/database-design.md
docs/diagrams/manager-dashboard-flow.md

Document:

- tables read
- tables written
- indexes used
- cache invalidation
- stale-data prevention
- frontend usage notes

==================================================
PART 5 — FIX PAGE 06 EMPLOYEE DASHBOARD PROPERLY
==================================================

Page 06 wireframe:
Dashboard — Employee

Final APIs should match wireframe data sources:

GET /api/v1/employee/dashboard
GET /api/v1/attendance/today
POST /api/v1/attendance/check-in
POST /api/v1/attendance/check-out
GET /api/v1/leave/balance/me
GET /api/v1/holidays/upcoming?limit=3
GET /api/v1/employees/me/documents
GET /api/v1/employees/me/team

If old /dashboard/employee routes exist, keep aliases temporarily but document the final routes above.

Fix schema field names:
Use:
checkInAt
checkOutAt
totalMinutes

Do not use:
checkInTime
checkOutTime

Employee dashboard response:

{
"success": true,
"data": {
"greetingName": "Priya",
"todayAttendance": {
"date": "2026-05-18",
"status": "PRESENT",
"checkInAt": "2026-05-18T09:14:00.000Z",
"checkOutAt": null,
"workMode": "WFH",
"totalMinutes": null,
"action": "CHECK_OUT"
},
"leaveBalance": [
{
"leaveType": "Casual",
"code": "CASUAL",
"balance": 6,
"used": 2,
"pending": 1,
"available": 4
}
],
"upcomingHolidays": [
{
"name": "Eid",
"holidayDate": "2026-06-25",
"isOptional": false
}
],
"documents": [
{
"id": "...",
"fileName": "Offer Letter.pdf",
"documentType": "OFFER_LETTER",
"verificationStatus": "VERIFIED"
}
],
"team": [
{
"employeeId": "...",
"name": "Aman K.",
"designation": "Manager",
"relationship": "MANAGER"
}
]
}
}

Check-in:

POST /api/v1/attendance/check-in

Request:
{
"workMode": "WFH",
"location": {
"lat": 28.6139,
"lng": 77.2090
}
}

Rules:

- prevent duplicate check-in
- if already checked in and not checked out, return 409 CHECK_IN_ALREADY_EXISTS
- create/update AttendanceRecord
- set checkInAt
- set workMode
- set locationJson
- status PRESENT or WFH depending workMode
- write audit log

Check-out:

POST /api/v1/attendance/check-out

Rules:

- must have checkInAt
- if no check-in, return 409 CHECK_IN_REQUIRED
- if already checked out, return 409 CHECK_OUT_ALREADY_EXISTS
- set checkOutAt
- calculate totalMinutes
- write audit log

Documents:
Use EmployeeDocument table.
Do not return empty array if records exist.

Holidays:
Return upcoming only.
Respect limit default 3 max 10.

Employee scope:

- authenticated user can view only own data
- cannot access other employee data
- cannot edit HR/company fields

Tests:
Unit:

- duplicate check-in prevention
- check-out without check-in
- totalMinutes calculation
- leave balance mapping
- upcoming holiday filtering
- document mapping
- team mapping

Integration:

- employee dashboard success
- attendance today success
- check-in success
- duplicate check-in 409
- check-out success
- check-out without check-in 409
- leave balance success
- holidays upcoming success
- documents success
- team success
- employee cannot access manager dashboard

E2E:

- employee login -> dashboard -> check-in -> duplicate check-in blocked -> check-out -> leave balance -> documents

Postman:
Add folder:
Employee Dashboard

Requests:

- Employee Login
- Employee Dashboard
- Attendance Today
- Check In
- Duplicate Check In
- Check Out
- Check Out Without Check In
- Leave Balance Me
- Upcoming Holidays
- My Documents
- My Team

Save captures:
artifacts/api-responses/page-06-employee-dashboard/

Docs:
Create/update:
docs/api/employee-dashboard-api.md
docs/api/ui-team-api-guide.md
docs/backend/database-design.md
docs/diagrams/employee-dashboard-flow.md

==================================================
PART 6 — START PAGE 07 EMPLOYEES LIST AFTER 05/06 ARE STABLE
==================================================

Because we need speed, after Page 05/06 pass, start Page 07 in the same large sprint.

Wireframe:
Page 07 Employees — List

Required APIs:

GET /api/v1/employees
GET /api/v1/employees/export
POST /api/v1/employees/bulk-deactivate
GET /api/v1/saved-views?module=employees
POST /api/v1/saved-views
PATCH /api/v1/saved-views/:id
DELETE /api/v1/saved-views/:id

GET /api/v1/employees query params:
search
departmentId
status
employmentType
joinedFrom
joinedTo
page
limit
sortBy
sortOrder

Response:
{
"success": true,
"data": [
{
"id": "...",
"employeeCode": "E2040",
"name": "Priya Sharma",
"email": "priya@company.com",
"department": "Engineering",
"role": "Sr Engineer",
"status": "ACTIVE",
"joinedOn": "2024-01-10"
}
],
"meta": {
"page": 1,
"limit": 10,
"total": 1240,
"totalPages": 124
}
}

Access:

- HR_ADMIN/SUPER_ADMIN can see all employees
- MANAGER can see direct reports only
- EMPLOYEE denied for list
- employees:read required

Export:

- employees:export required
- for large export, use background job
- save export job response

Bulk deactivate:

- HR_ADMIN/SUPER_ADMIN only
- employees:delete required
- require confirmationText
- soft deactivate only
- revoke user sessions
- audit log
- cache invalidation

Saved views:

- scoped by user + tenant + module=employees
- store filtersJson and columnsJson
- no cross-user leakage

Tests:
Unit/integration/E2E/Postman/docs/captures required.

Docs:
docs/api/employees-list-api.md
docs/diagrams/employees-list-flow.md
Update UI guide and DB design.

==================================================
PART 7 — PERFORMANCE REQUIREMENTS
==================================================

Add or fix:

npm run perf:api

Use either autocannon or a Node smoke script.

Performance budgets with 60+ employees:

Analytics:

- cached p95 < 20ms
- uncached p95 < 150ms

Manager dashboard:

- p95 < 150ms

Employee dashboard:

- p95 < 120ms

Employees list:

- p95 < 200ms with pagination
- search debounce frontend note: 300ms
- DB indexes required for search/filter/sort

Save output:
artifacts/performance/page-04-05-06-07-performance.json

==================================================
PART 8 — WIREンプFRAME COVERAGE MATRIX
==================================================

Create:

docs/WIREFRAME_COVERAGE_MATRIX.md

For every wireframe page, include:

Page
Role
UI widgets
Required APIs
Implementation status:
NOT_STARTED
IN_PROGRESS
IMPLEMENTED
TESTED
POSTMAN_DONE
DOCS_DONE
ACCEPTED
Tables touched
Notes/blockers

Current pages:

01 Login
02 Forgot Password
03 OTP Verification
04 HR Admin Dashboard
05 Manager Dashboard
06 Employee Dashboard
07 Employees List
08 Employee Profile
09 Employee Create/Edit
10 Departments
11 Attendance
12 Leave Requests
13 Holidays
14 Permissions
15 Settings

This matrix must be updated every sprint.

Important:
Innovation is welcome only after the wireframe-required functionality is complete. Do not build creative extras while required wireframe APIs are missing or broken.

==================================================
PART 9 — FINAL COMMANDS
==================================================

Run and save outputs:

npm run lint
npm run test:unit
npm run test:integration
npm run test:e2e
npm run test:coverage
npx prisma validate
npm run db:seed
npm run db:seed:large
newman run docs/postman/EMS-API.postman_collection.json -e docs/postman/EMS.postman_environment.json
npm run email:test -- --to mohammadsaeedafri9@gmail.com
npm run perf:api
npm run api:capture

Save outputs:
artifacts/test-results/lint.txt
artifacts/test-results/unit.txt
artifacts/test-results/integration.txt
artifacts/test-results/e2e.txt
artifacts/test-results/coverage.txt
artifacts/test-results/prisma-validate.txt
artifacts/test-results/seed-large.txt
artifacts/newman/page-04-05-06-07-newman-report.json
artifacts/performance/page-04-05-06-07-performance.json

Acceptance:

- 0 lint errors
- 0 unit failures
- 0 integration failures
- 0 E2E failures
- 0 Newman failures
- safe Brevo email received
- password reset email received
- OTP email received
- 60+ employees actually seeded
- seed uses correct schema fields
- no silent seed failures
- Page 04 accepted
- Page 05 accepted
- Page 06 accepted
- Page 07 implemented if time allows
- saved JSON responses exist for every endpoint
- no stale pending approvals after manager decision
- no stale analytics counts after approval/check-in/check-out
- cache invalidation works after mutations
- docs list tables/indexes for every API
- wireframe coverage matrix updated

Commits:
fix(seed): add robust large demo dataset with realistic hr data
fix(analytics): finalize page 04 response proof
feat(manager): implement manager dashboard with approval consistency
feat(employee): implement employee dashboard attendance self-service
feat(employees): implement employees list and saved views
test(api): capture endpoint responses and performance evidence
docs(wireframes): add coverage matrix
